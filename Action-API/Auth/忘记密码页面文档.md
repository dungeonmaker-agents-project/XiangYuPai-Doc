# 忘记密码页面文档

## 概述
本文档定义忘记密码完整流程的实现，包括三个页面的UI布局、交互行为、前端逻辑和API接口规范。

**流程概览:**
1. 输入手机号页面 → 获取验证码
2. 验证码输入页面 → 验证验证码
3. 设置新密码页面 → 重置密码完成

**相关文档:**
- 密码登录页面文档
- 验证码登录页面文档

**UI设计稿参考:**
- `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码.png`
- `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-获取验证码.png`
- `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-输入验证码.png`
- `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-输入新密码.png`
- `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-重置密码.png`
- `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-密码重置成功.png`

---

## 流程图

```
[忘记密码页 - 输入手机号]
  ↓ 用户输入手机号
  ↓ 点击"获取短信验证码"
  → 触发接口1: POST /auth/sms/send
  ↓ 成功返回
[验证码输入页]
  ↓ 显示："验证码已发送至 131 **** 6323"
  ↓ 用户输入6位验证码
  ↓ 输入完第6位（自动提交）
  → 触发接口2: POST /auth/password/reset/verify
  ↓ 验证成功
[设置新密码页]
  ↓ 用户输入新密码
  ↓ 点击"确认"
  → 触发接口3: POST /auth/password/reset/confirm
  ↓ 重置成功
[显示"密码重置成功" Toast]
  ↓ 2-3秒后
[返回登录页]
```

---

## API接口规范

### 接口1：发送验证码
**接口:** `POST /auth/sms/send`

**触发时机:** 用户点击"获取短信验证码"按钮

**请求参数:**
```typescript
{
  countryCode: string;      // 国家区号，例如："+86"
  phoneNumber: string;      // 手机号，例如："13147046323"
  purpose: "RESET_PASSWORD" // 固定值，表示用于重置密码
}
```

**响应数据:**
```typescript
{
  code: number;           // 200=成功，其他=失败
  message: string;        // 提示信息
  data: null
}
```

### 接口2：验证验证码
**接口:** `POST /auth/password/reset/verify`

**触发时机:** 用户输入完第6位验证码后自动触发

**请求参数:**
```typescript
{
  countryCode: string;       // 国家区号，例如："+86"
  phoneNumber: string;       // 手机号，例如："13147046323"
  verificationCode: string;  // 6位验证码
}
```

**响应数据:**
```typescript
{
  code: number;           // 200=成功，其他=失败
  message: string;        // 提示信息
  data: null
}
```

### 接口3：设置新密码
**接口:** `POST /auth/password/reset/confirm`

**触发时机:** 用户点击"确认"按钮

**请求参数:**
```typescript
{
  countryCode: string;       // 国家区号，例如："+86"
  phoneNumber: string;       // 手机号，例如："13147046323"
  verificationCode: string;  // 6位验证码（从上一步保存）
  newPassword: string;       // 新密码，6-20位字符，不可纯数字
}
```

**响应数据:**
```typescript
{
  code: number;           // 200=成功，其他=失败
  message: string;        // 提示信息
  data: null
}
```

### 错误码说明
| 状态码 | 说明 | 前端提示 |
|-------|------|---------|
| 200 | 成功 | 执行对应成功逻辑 |
| 400 | 参数错误 | "请检查输入信息" |
| 401 | 验证码错误 | "验证码错误，请重新输入" |
| 404 | 用户不存在 | "该手机号未注册" |
| 429 | 发送频繁 | "发送过于频繁，请稍后再试" |
| 500 | 服务器错误 | "操作失败，请稍后重试" |

---

## 页面1：输入手机号

### 页面布局

#### 顶部导航栏
```
[←]  忘记密码
```
- **返回按钮:** 左侧箭头图标
- **标题:** "忘记密码"
  - 字体大小：18pt
  - 颜色：深灰色 (#1a1a1a)
  - 位置：居中
- 高度：44px
- 背景：白色
- 底部分割线：1px 浅灰色

#### 主要内容区
**标题文字:**
```
忘记密码
```
- 字体：加粗
- 大小：28pt
- 颜色：深灰色/黑色 (#1a1a1a)
- 位置：距离导航栏 32px

**副标题:**
```
请输入注册时的手机号
```
- 字体：常规
- 大小：14pt
- 颜色：灰色 (#666)
- 位置：紧跟标题下方 8px

#### 手机号输入区
**布局:**
```
[+86 ▼]  [请输入手机号_________________ ⊗]
```

**国家区号选择器:**
- 默认值：`+86`
- 显示：区号 + 下拉箭头
- 宽度：80px
- 高度：48px
- 字体大小：16pt
- 底部边框：1px 浅灰色分割线

**手机号输入框:**
- 占位符："请输入手机号"
- 输入类型：`tel`
- 最大长度：11位
- 字体大小：16pt
- 底部边框：1px 浅灰色分割线
- 间距：距离副标题 40px

**清空按钮:**
- 位置：输入框右侧
- 显示条件：有内容时显示
- 图标：X符号
- 颜色：浅灰色

#### 获取验证码按钮
```
┌──────────────────────────────────────┐
│        获取短信验证码                 │
└──────────────────────────────────────┘
```

**样式:**
- 文字："获取短信验证码"
- 宽度：100% (左右边距 24px)
- 高度：52px
- 圆角：26px
- 字体大小：18pt

**状态:**
1. **默认/启用:**
   - 背景：紫色到蓝色渐变
   - 文字：白色
   - 条件：手机号格式正确

2. **禁用:**
   - 背景：浅灰色
   - 文字：灰色
   - 条件：手机号未输入或格式错误

3. **倒计时:**
   - 文字："59秒后重新获取验证码"
   - 背景：浅灰色
   - 不可点击

**间距:**
- 距离手机号输入框：40px

### 交互行为

#### 1. 返回操作
- 点击左上角返回按钮
- 返回到登录页面
- 不保存当前输入

#### 2. 手机号输入
**输入时:**
- 只允许数字
- 实时验证格式
- 有内容显示清空按钮
- 格式正确启用获取验证码按钮

**失去焦点:**
- 验证手机号格式
- 格式错误显示提示：
  ```
  ⚠ 请输入正确的手机号格式
  ```

#### 3. 获取短信验证码
**点击按钮:**

**验证:**
1. 手机号不能为空
2. 手机号格式正确

**调用接口:**
- 接口：`POST /auth/sms/send`
- 参数：countryCode, phoneNumber, purpose: "RESET_PASSWORD"

**成功响应:**
1. 显示Toast："验证码已发送"
2. 按钮进入倒计时状态："59秒后重新获取验证码"
3. 开始60秒倒计时
4. **跳转到验证码输入页**
5. 传递数据：countryCode, phoneNumber

**失败响应:**
- 显示错误提示：
  - "该手机号未注册" (404)
  - "发送失败，请稍后重试" (500)
  - "发送过于频繁，请稍后再试" (429)
- 保持在当前页面

---

## 页面2：验证码输入

### 页面布局

#### 顶部导航栏
```
[←]  验证码验证
```
- **返回按钮:** 返回到输入手机号页面
- **标题:** "验证码验证"
- 样式同页面1

#### 主要内容区
**提示信息:**
```
验证码已发送至 131 4704 6323
```
- 文字："验证码已发送至 131 4704 6323"
  - 字体大小：14pt
  - 颜色：灰色 (#999)
- 显示完整手机号（不脱敏）
- 格式：带空格分隔 `131 4704 6323`
- 间距：距离标题 16px

#### 验证码输入区
```
[3] [6] [1] [2] [3] [4]
```

**6个独立输入框:**
- 样式同验证码登录页面
- 宽度：48px
- 高度：56px
- 间距：12px
- 自动聚焦第一个输入框
- 输入后自动跳到下一个
- 删除后自动跳回上一个
- 输入完第6位自动提交

**间距:**
- 距离提示信息：48px

#### 重新发送
```
没收到验证码？ 重新发送
```
- 位置：验证码输入区下方 24px
- 文字："没收到验证码？" + "重新发送"
- "重新发送"为可点击链接
- 链接颜色：紫色 (#9333ea)
- 有60秒倒计时限制

### 交互行为

#### 1. 返回操作
- 点击返回按钮
- 返回到输入手机号页面
- 保留手机号输入
- 清除验证码

#### 2. 验证码输入
**自动聚焦:**
- 页面加载后自动聚焦第一个输入框

**输入行为:**
- 只允许数字
- 每个输入框1位
- 输入后自动跳到下一个
- 删除后自动跳回上一个

**输入完成:**
- 输入完第6位后自动收起键盘
- 自动调用验证接口

**调用接口:**
- 接口：`POST /auth/password/reset/verify`
- 参数：countryCode, phoneNumber, verificationCode

**显示加载:**
- 验证码输入框禁用
- 显示加载提示

**成功响应:**
1. 显示Toast："验证成功"
2. **跳转到设置新密码页**
3. 传递数据：countryCode, phoneNumber, verificationCode

**失败响应:**
1. 显示Toast："验证码错误，请重新输入"
2. 清空所有输入框
3. 聚焦到第一个输入框
4. 允许重新输入

#### 3. 重新发送
**点击"重新发送":**

**倒计时检查:**
- 如果还在倒计时中：显示"请59秒后重试"
- 如果倒计时结束：允许重新发送

**调用接口:**
- 同页面1的获取验证码接口
- 接口：`POST /auth/sms/send`

**成功:**
- 显示Toast："验证码已重新发送"
- 开始新的60秒倒计时
- "重新发送"变为"59秒后重新发送"

---

## 页面3：设置新密码

### 页面布局

#### 顶部导航栏
```
[←]  设置新密码
```
- **返回按钮:** 返回到验证码输入页
- **标题:** "设置新密码"
- 样式同前两页

#### 主要内容区
**标题文字:**
```
设置新密码
```
- 字体：加粗
- 大小：28pt
- 颜色：深灰色 (#1a1a1a)
- 位置：距离导航栏 32px

**副标题:**
```
请设置6-20位字符的新密码
```
- 字体：常规
- 大小：14pt
- 颜色：灰色 (#666)
- 位置：紧跟标题下方 8px

#### 新密码输入区
**布局 (隐藏状态):**
```
[●●●●●●●●_________________________________ 👁]
```

**布局 (显示状态):**
```
[newPassword123__________________________ 👁]
```

**新密码输入框:**
- 占位符："请输入新密码"
- 输入类型：默认 `password`，可切换为 `text`
- 长度限制：6-20位
- 字体大小：16pt
- 底部边框：1px 浅灰色分割线
- 间距：距离副标题 40px

**显示/隐藏按钮:**
- 位置：输入框右侧
- 图标：闭眼/睁眼
- 点击切换密码显示状态

**密码要求提示:**
```
密码要求：
• 6-20位字符
• 不可纯数字（至少包含一个字母或符号）
```
- 位置：输入框下方 16px
- 字体大小：12pt
- 颜色：浅灰色 (#999)
- 行间距：4px

#### 确认按钮
```
┌──────────────────────────────────────┐
│              确认                     │
└──────────────────────────────────────┘
```

**样式:**
- 文字："确认"
- 宽度：100% (左右边距 24px)
- 高度：52px
- 圆角：26px
- 字体大小：18pt

**状态:**
1. **启用:**
   - 背景：紫色到蓝色渐变
   - 文字：白色
   - 条件：密码格式正确

2. **禁用:**
   - 背景：浅灰色
   - 文字：灰色
   - 条件：密码未输入或格式错误

3. **加载:**
   - 背景：渐变色
   - 文字："确认中..." 或显示加载图标
   - 不可点击

**间距:**
- 距离密码要求提示：40px

### 交互行为

#### 1. 返回操作
- 点击返回按钮
- 弹出确认对话框："确定要放弃重置密码吗？"
  - 按钮1："取消" - 关闭对话框
  - 按钮2："确定" - 返回验证码输入页

#### 2. 密码输入
**输入时:**
- 实时计算长度
- 达到20位停止输入
- 启用/禁用确认按钮

**失去焦点:**
- 验证密码格式
- 格式错误显示提示：
  ```
  ⚠ 密码长度为6-20位字符
  ⚠ 密码不能为纯数字
  ```

**显示/隐藏切换:**
- 点击眼睛图标
- 切换输入类型 password ⇄ text
- 切换图标 闭眼 ⇄ 睁眼
- 保持光标位置

#### 3. 提交新密码
**点击"确认"按钮:**

**验证:**
1. 密码不能为空
2. 密码长度6-20位
3. 密码不可纯数字

**调用接口:**
- 接口：`POST /auth/password/reset/confirm`
- 参数：countryCode, phoneNumber, verificationCode, newPassword

**显示加载:**
- 按钮进入加载状态
- 禁用密码输入框
- 禁用返回按钮

**成功响应:**
1. 显示Toast："密码重置成功" (2-3秒)
2. 延迟2-3秒后跳转到登录页
3. 清除所有临时数据
4. 可选：自动填充手机号到登录页

**失败响应:**
1. 恢复按钮和输入框状态
2. 显示错误提示：
   - "验证码已过期，请重新获取" (401)
   - "密码格式不正确" (400)
   - "重置失败，请稍后重试" (500)
3. 允许重新输入或返回

---

## 数据传递

### 跨页面数据流
```
页面1 → 页面2:
  - countryCode
  - phoneNumber

页面2 → 页面3:
  - countryCode
  - phoneNumber
  - verificationCode

页面3 → 登录页:
  - phoneNumber (可选自动填充)
```

### 数据管理方案

**方案1：状态管理 (推荐)**
```typescript
// 使用全局状态管理 (Redux/Zustand/Context)
interface ResetPasswordState {
  countryCode: string;
  phoneNumber: string;
  verificationCode: string;
}

// 各页面读取和更新状态
```

**方案2：路由参数**
```typescript
// URL传参
/forgot-password/verify?phone=13147046323&code=+86
/forgot-password/reset?phone=13147046323&code=+86&vcode=361234
```

**方案3：sessionStorage**
```typescript
// 临时存储
sessionStorage.setItem('resetPasswordData', JSON.stringify({
  countryCode: '+86',
  phoneNumber: '13147046323',
  verificationCode: '361234'
}));

// 完成后清除
sessionStorage.removeItem('resetPasswordData');
```

---

## 前端验证规则

### 手机号验证
```typescript
const isValidPhoneNumber = (phone: string, countryCode: string) => {
  if (countryCode === '+86') {
    return /^1[3-9]\d{9}$/.test(phone);
  }
  return phone.length >= 8 && phone.length <= 15;
}
```

### 验证码验证
```typescript
const isValidVerificationCode = (code: string) => {
  return /^\d{6}$/.test(code);
}
```

### 新密码验证
```typescript
const isValidNewPassword = (password: string) => {
  // 长度检查
  if (password.length < 6 || password.length > 20) {
    return { valid: false, error: '密码长度为6-20位字符' };
  }

  // 不可纯数字
  if (/^\d+$/.test(password)) {
    return { valid: false, error: '密码不能为纯数字' };
  }

  return { valid: true };
}
```

### 手机号格式化
```typescript
const formatPhoneNumber = (phone: string) => {
  if (phone.length === 11) {
    // 显示完整手机号，添加空格分隔
    return `${phone.slice(0, 3)} ${phone.slice(3, 7)} ${phone.slice(7)}`;
  }
  return phone;
}

// 示例：13147046323 → 131 4704 6323
```

---

## 样式规范

### 颜色系统
```css
/* 主色调 */
--primary-purple: #9333ea;
--primary-blue: #3b82f6;

/* 渐变色 */
--gradient-start: #b794f6;
--gradient-end: #a5b4fc;

/* 文字颜色 */
--text-primary: #1a1a1a;
--text-secondary: #666666;
--text-placeholder: #c0c0c0;
--text-disabled: #999999;

/* 边框颜色 */
--border-light: #e5e5e5;
--border-focus: #9333ea;

/* 错误颜色 */
--error: #ef4444;
--success: #10b981;

/* 背景色 */
--bg-white: #ffffff;
--bg-disabled: #f5f5f5;
```

### 导航栏样式
```css
.navbar {
  height: 44px;
  padding: 0 16px;
  background: var(--bg-white);
  border-bottom: 1px solid var(--border-light);
  display: flex;
  align-items: center;
}

.navbar-back {
  width: 44px;
  height: 44px;
  display: flex;
  align-items: center;
  justify-content: center;
}

.navbar-title {
  flex: 1;
  text-align: center;
  font-size: 18pt;
  font-weight: 500;
  color: var(--text-primary);
}
```

---

## 动画效果

### 页面过渡
```css
/* 页面进入 */
.page-enter {
  animation: slideInRight 0.3s ease;
}

@keyframes slideInRight {
  from {
    transform: translateX(100%);
    opacity: 0;
  }
  to {
    transform: translateX(0);
    opacity: 1;
  }
}

/* 页面退出 */
.page-exit {
  animation: slideOutRight 0.3s ease;
}

@keyframes slideOutRight {
  from {
    transform: translateX(0);
    opacity: 1;
  }
  to {
    transform: translateX(100%);
    opacity: 0;
  }
}
```

### 成功提示
```css
/* Toast淡入淡出 */
.toast-success {
  background: var(--success);
  color: white;
  animation: fadeInOut 2.5s ease;
}

@keyframes fadeInOut {
  0%, 100% { opacity: 0; }
  10%, 90% { opacity: 1; }
}
```

---

## 测试用例

### 功能测试
- [ ] 页面1：手机号输入和验证正常
- [ ] 页面1：获取验证码成功并跳转
- [ ] 页面1：倒计时显示准确
- [ ] 页面2：验证码输入自动跳转
- [ ] 页面2：验证码自动提交
- [ ] 页面2：重新发送功能正常
- [ ] 页面3：密码显示/隐藏切换
- [ ] 页面3：密码验证准确
- [ ] 页面3：提交成功并返回登录页
- [ ] 数据在三个页面间正确传递

### 边界测试
- [ ] 未注册手机号处理
- [ ] 验证码错误处理
- [ ] 验证码过期处理
- [ ] 网络异常处理
- [ ] 快速重复点击按钮
- [ ] 返回时数据保留/清除

### 用户体验测试
- [ ] 页面过渡动画流畅
- [ ] 返回确认对话框友好
- [ ] 成功提示清晰可见
- [ ] 错误提示准确具体

---

## 开发清单

### 必须实现
- [x] 三个页面布局和样式
- [x] 导航栏和返回功能
- [x] 手机号输入和验证
- [x] 获取验证码和倒计时
- [x] 验证码输入和自动跳转
- [x] 验证码验证
- [x] 新密码输入和验证
- [x] 密码显示/隐藏切换
- [x] 三个接口调用
- [x] 跨页面数据传递
- [x] 错误处理
- [x] 成功提示和跳转

### 可选优化
- [ ] 页面过渡动画
- [ ] 返回确认对话框
- [ ] 自动填充手机号到登录页
- [ ] 剪贴板识别验证码
- [ ] 密码强度提示
- [ ] 生物识别登录引导

---

## 相关文档

- **设计稿:**
  - `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码.png`
  - `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-获取验证码.png`
  - `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-输入验证码.png`
  - `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-输入新密码.png`
  - `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-重置密码.png`
  - `UI页面/登录页/登陆页-忘记密码/登陆页-忘记密码-密码重置成功.png`
- **相关页面文档:**
  - 密码登录页面文档
  - 验证码登录页面文档
- **关联页面:**
  - 用户协议页面
  - 隐私政策页面
