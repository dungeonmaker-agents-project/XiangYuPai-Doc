# 密码登录页面文档

## 概述
本文档定义密码登录页面的完整实现，包括UI布局、交互行为、前端逻辑和API接口规范。

**相关文档:**
- 验证码登录页面文档
- 忘记密码页面文档

**UI设计稿参考:**
- `UI页面/登录页/登陆页-密码登陆.png`
- `UI页面/登录页/登陆页-密码登陆-隐藏密码.png`
- `UI页面/登录页/登陆页-密码登陆-显示密码.png`

---

## API接口规范

### 接口定义
**接口:** `POST /auth/login/password`

**触发时机:** 用户点击"登陆"按钮

**请求参数:**
```typescript
{
  countryCode: string;      // 国家区号，例如："+86"
  phoneNumber: string;      // 手机号，例如："13147046323"
  password: string;         // 密码，6-20位字符，不可纯数字
  agreeToTerms: boolean;    // 用户协议勾选状态，必须为true
}
```

**响应数据:**
```typescript
{
  code: number;           // 200=成功，其他=失败
  message: string;        // 提示信息
  data: {
    token: string;        // 登录凭证
    userId: string;       // 用户ID
    nickname: string;     // 用户昵称
    avatar?: string;      // 用户头像
  } | null
}
```

**错误码说明:**
| 状态码 | 说明 | 前端提示 |
|-------|------|---------|
| 200 | 登录成功 | 跳转到主页 |
| 400 | 参数错误 | "请检查输入信息" |
| 401 | 认证失败 | "手机号或密码错误" |
| 403 | 账号禁用 | "账号已被禁用，请联系客服" |
| 500 | 服务器错误 | "登录失败，请稍后重试" |
| 网络错误 | 网络异常 | "网络连接失败，请检查网络设置" |

---

## 页面布局

### 顶部状态栏
- 系统状态栏：显示时间、信号、电池等
- 背景：白色/浅色

### 欢迎区域
```
您好！
欢迎使用探店
```
- **主标题:** "您好！"
  - 字体：加粗
  - 大小：32-36pt
  - 颜色：深灰色/黑色 (#1a1a1a)
  - 位置：距离顶部 80-100px

- **副标题:** "欢迎使用探店"
  - 字体：常规
  - 大小：32-36pt
  - 颜色：深灰色/黑色 (#1a1a1a)
  - 位置：紧跟主标题下方

### 表单区域

#### 1. 手机号输入区
**布局:**
```
[+86 ▼]  [请输入手机号_________________ ⊗]
```

**国家区号选择器:**
- 默认值：`+86`
- 显示：区号 + 下拉箭头图标
- 宽度：80px
- 高度：48px
- 字体大小：16pt
- 颜色：深灰色 (#333)
- 背景：透明
- 底部边框：1px 浅灰色分割线

**手机号输入框:**
- 占位符："请输入手机号"
- 占位符颜色：浅灰色 (#c0c0c0)
- 输入类型：`tel` (数字键盘)
- 最大长度：11位 (根据区号动态调整)
- 字体大小：16pt
- 输入文字颜色：黑色 (#1a1a1a)
- 自动聚焦：否
- 底部边框：1px 浅灰色分割线
- 间距：距离欢迎文字 60-80px

**清空按钮 (X图标):**
- 位置：输入框右侧
- 显示条件：有输入内容时显示
- 图标：圆形背景 + X符号
- 颜色：浅灰色 (#c0c0c0)
- 尺寸：20x20px
- 点击区域：44x44px (增大可点击区域)

#### 2. 密码输入区
**布局 (隐藏状态):**
```
[●●●●●●●●_________________________________ 👁]
```

**布局 (显示状态):**
```
[you990929_________________________________ 👁]
```

**密码输入框:**
- 占位符："请输入6-20位密码"
- 占位符颜色：浅灰色 (#c0c0c0)
- 输入类型：
  - 默认：`password` (圆点显示)
  - 切换后：`text` (明文显示)
- 长度限制：6-20位字符
- 字体大小：16pt
- 输入文字颜色：黑色 (#1a1a1a)
- 底部边框：1px 浅灰色分割线
- 间距：距离手机号输入框 24px

**显示/隐藏切换按钮:**
- 位置：输入框右侧
- 图标：
  - 隐藏状态：闭眼图标 (slash-eye)
  - 显示状态：睁眼图标 (eye)
- 颜色：浅灰色 (#888)
- 尺寸：24x24px
- 点击区域：44x44px

#### 3. 登陆按钮
```
┌──────────────────────────────────────┐
│              登陆                     │
└──────────────────────────────────────┘
```

**样式:**
- 文字："登陆"
- 宽度：100% (左右边距 24px)
- 高度：52px
- 圆角：26px (完全圆角)
- 字体大小：18pt
- 字体粗细：中等加粗

**状态:**
1. **默认状态 (可点击):**
   - 背景：紫色到蓝色渐变
     - 起始色：#b794f6
     - 结束色：#a5b4fc
   - 文字颜色：白色 (#ffffff)
   - 阴影：无或轻微阴影

2. **禁用状态:**
   - 背景：浅灰色渐变
     - 起始色：#e0e0e0
     - 结束色：#d0d0d0
   - 文字颜色：灰色 (#999)
   - 不可点击

3. **加载状态:**
   - 背景：保持渐变色
   - 文字：隐藏或变为"登陆中..."
   - 显示：加载动画/旋转图标
   - 不可点击

**启用条件:**
- 手机号已输入且格式正确
- 密码已输入且长度符合要求
- 用户协议已勾选

**间距:**
- 距离密码输入框：40px

#### 4. 辅助链接区
```
验证码登陆                    忘记密码？
```

**验证码登陆链接:**
- 文字："验证码登陆"
- 位置：左侧
- 字体大小：14pt
- 颜色：紫色 (#9333ea)
- 点击效果：高亮
- 功能：切换到验证码登录页面

**忘记密码链接:**
- 文字："忘记密码？"
- 位置：右侧
- 字体大小：14pt
- 颜色：浅灰色 (#888)
- 点击效果：高亮
- 功能：跳转到忘记密码流程

**间距:**
- 距离登陆按钮：16px
- 两个链接间距：左右分布，中间留白

### 底部用户协议
```
登陆即表明同意《探店用户协议》和《隐私政策》
```

**布局:**
- 位置：页面底部，距离底部安全区域 32px
- 对齐：居中
- 字体大小：12pt

**样式:**
- 普通文字颜色：浅灰色 (#999)
- 协议链接颜色：紫色 (#9333ea)
- 链接：《探店用户协议》《隐私政策》可点击

**注意:**
- 无需勾选框
- 文字说明即可
- 点击协议链接打开对应页面

---

## 交互行为

### 1. 国家区号选择
**触发:** 点击区号选择器

**行为:**
1. 打开底部弹出选择器 (Bottom Sheet)
2. 显示国家/地区列表：
   - 中国大陆 +86
   - 中国香港 +852
   - 中国澳门 +853
   - 中国台湾 +886
   - 澳大利亚 +61
   - 韩国 +82
   - 泰国 +66
3. 选中当前区号
4. 点击某项后：
   - 更新区号显示
   - 关闭选择器
   - 更新手机号最大长度限制

### 2. 手机号输入
**输入时:**
- 实时验证格式
- 只允许输入数字
- 达到最大长度时自动停止输入
- 有内容时显示清空按钮

**失去焦点时:**
- 验证手机号格式
- 格式错误显示提示：
  ```
  ⚠ 请输入正确的手机号格式
  ```
  - 位置：输入框下方
  - 颜色：红色 (#ef4444)
  - 字体大小：12pt

**清空按钮:**
- 点击后清空输入内容
- 隐藏清空按钮
- 输入框聚焦状态保持

### 3. 密码输入
**输入时:**
- 默认显示圆点 (●)
- 实时计算长度
- 达到20位时停止输入

**失去焦点时:**
- 验证密码格式
- 格式错误显示提示：
  ```
  ⚠ 密码长度为6-20位字符
  ⚠ 密码不能为纯数字
  ```
  - 位置：输入框下方
  - 颜色：红色 (#ef4444)
  - 字体大小：12pt

**显示/隐藏切换:**
- 点击眼睛图标切换
- 切换动画：
  - 图标平滑切换 (200ms)
  - 文字显示切换 (200ms)
- 切换后保持光标位置

### 4. 表单验证

**实时验证 (输入时):**
- 手机号：只能输入数字
- 密码：允许字母、数字、符号

**提交前验证:**
1. 手机号不能为空
2. 手机号格式正确 (+86时为11位数字)
3. 密码不能为空
4. 密码长度6-20位
5. 密码不可纯数字 (至少包含一个字母或符号)

**验证不通过:**
- 登陆按钮保持禁用状态
- 显示对应错误提示

**验证通过:**
- 登陆按钮启用 (渐变色)
- 可点击提交

### 5. 提交登录

**点击登陆按钮:**
1. 再次验证所有字段
2. 验证通过后：
   - 按钮进入加载状态
   - 禁用所有输入框
   - 调用登录接口 `POST /auth/login/password`
   - 参数：
     ```typescript
     {
       countryCode: string;    // 例如："+86"
       phoneNumber: string;    // 例如："13147046323"
       password: string;       // 例如："you990929"
       agreeToTerms: true      // 固定为true
     }
     ```

**成功响应:**
1. 显示成功提示 (Toast/轻提示)
2. 保存登录token
3. 延迟200-300ms后跳转到主页
4. 清除登录页面栈 (防止返回)

**失败响应:**
1. 恢复按钮状态
2. 启用输入框
3. 根据错误码显示提示：
   - **400 参数错误:** "请检查输入信息"
   - **401 认证失败:** "手机号或密码错误"
   - **403 账号禁用:** "账号已被禁用，请联系客服"
   - **500 服务器错误:** "登录失败，请稍后重试"
   - **网络错误:** "网络连接失败，请检查网络设置"
4. 允许重新输入

### 6. 页面跳转

**点击"验证码登陆":**
- 路由跳转到验证码登录页面
- 不保存当前输入内容

**点击"忘记密码？":**
- 路由跳转到忘记密码页面
- 不保存当前输入内容

**点击用户协议/隐私政策:**
- 打开对应协议页面 (WebView或新页面)
- 返回后保留当前输入内容

---

## 前端验证规则

### 手机号验证
```typescript
// +86 中国大陆
const isValidPhoneNumber = (phone: string, countryCode: string) => {
  if (countryCode === '+86') {
    return /^1[3-9]\d{9}$/.test(phone); // 11位，1开头
  }
  // 其他国家区号规则...
  return phone.length >= 8 && phone.length <= 15;
}
```

### 密码验证
```typescript
const isValidPassword = (password: string) => {
  if (password.length < 6 || password.length > 20) {
    return { valid: false, error: '密码长度为6-20位字符' };
  }

  // 检查是否纯数字
  if (/^\d+$/.test(password)) {
    return { valid: false, error: '密码不能为纯数字' };
  }

  return { valid: true };
}
```

### 表单可提交验证
```typescript
const canSubmit = () => {
  const phoneValid = phoneNumber && isValidPhoneNumber(phoneNumber, countryCode);
  const passwordValid = password && isValidPassword(password).valid;

  return phoneValid && passwordValid;
}
```

---

## 样式规范

### 颜色系统
```css
/* 主色调 */
--primary-purple: #9333ea;
--primary-blue: #3b82f6;

/* 渐变色 */
--gradient-start: #b794f6;
--gradient-end: #a5b4fc;

/* 文字颜色 */
--text-primary: #1a1a1a;
--text-secondary: #666666;
--text-placeholder: #c0c0c0;
--text-disabled: #999999;

/* 边框颜色 */
--border-light: #e5e5e5;
--border-focus: #9333ea;

/* 错误颜色 */
--error: #ef4444;

/* 背景色 */
--bg-white: #ffffff;
--bg-disabled: #f5f5f5;
```

### 间距系统
```css
/* 页面边距 */
--page-padding: 24px;

/* 元素间距 */
--spacing-xs: 8px;
--spacing-sm: 12px;
--spacing-md: 16px;
--spacing-lg: 24px;
--spacing-xl: 40px;
--spacing-2xl: 60px;
```

### 字体系统
```css
/* 字体大小 */
--font-xs: 12pt;
--font-sm: 14pt;
--font-md: 16pt;
--font-lg: 18pt;
--font-xl: 32pt;

/* 字体粗细 */
--font-normal: 400;
--font-medium: 500;
--font-semibold: 600;
--font-bold: 700;
```

### 圆角系统
```css
--radius-sm: 4px;
--radius-md: 8px;
--radius-lg: 12px;
--radius-full: 26px; /* 按钮完全圆角 */
```

---

## 响应式适配

### 屏幕尺寸
- **小屏幕** (宽度 < 375px): 减小边距至 16px
- **标准屏幕** (375px - 428px): 默认样式
- **大屏幕** (> 428px): 最大内容宽度 428px，居中显示

### 安全区域
```css
/* 顶部安全区域 */
padding-top: env(safe-area-inset-top);

/* 底部安全区域 */
padding-bottom: env(safe-area-inset-bottom);
```

### 横屏适配
- 保持纵向布局
- 可选：添加最大高度限制，启用滚动

---

## 动画效果

### 按钮点击
```css
transition: transform 0.15s ease;

/* 按下 */
transform: scale(0.98);

/* 释放 */
transform: scale(1);
```

### 输入框聚焦
```css
transition: border-color 0.2s ease;

/* 聚焦 */
border-bottom-color: var(--border-focus);
```

### 错误提示
```css
/* 淡入 */
animation: fadeIn 0.2s ease;

@keyframes fadeIn {
  from { opacity: 0; transform: translateY(-4px); }
  to { opacity: 1; transform: translateY(0); }
}
```

### 加载动画
```css
/* 旋转图标 */
animation: spin 1s linear infinite;

@keyframes spin {
  from { transform: rotate(0deg); }
  to { transform: rotate(360deg); }
}
```

---

## 无障碍支持

### 语义化标签
```html
<form aria-label="密码登录表单">
  <label for="phone">手机号</label>
  <input id="phone" type="tel" aria-required="true" />

  <label for="password">密码</label>
  <input id="password" type="password" aria-required="true" />

  <button type="submit" aria-label="登录">登陆</button>
</form>
```

### 屏幕阅读器
- 输入框添加 `aria-label` 或关联 `<label>`
- 错误提示添加 `aria-live="polite"`
- 按钮状态添加 `aria-disabled="true"`

### 键盘导航
- Tab键顺序：手机号 → 密码 → 登陆按钮 → 验证码登陆 → 忘记密码
- Enter键提交表单

---

## 性能优化

### 防抖处理
```typescript
// 输入验证防抖 300ms
const debouncedValidate = debounce(validateInput, 300);
```

### 懒加载
- 国家区号列表按需加载
- 协议页面按需加载

### 图片优化
- 图标使用SVG或Icon Font
- 避免使用大型背景图

---

## 状态管理

### 页面状态
```typescript
interface LoginPageState {
  countryCode: string;        // 国家区号
  phoneNumber: string;        // 手机号
  password: string;           // 密码
  showPassword: boolean;      // 密码是否显示
  isSubmitting: boolean;      // 是否正在提交
  errors: {
    phoneNumber?: string;     // 手机号错误信息
    password?: string;        // 密码错误信息
  };
}
```

### 初始状态
```typescript
const initialState: LoginPageState = {
  countryCode: '+86',
  phoneNumber: '',
  password: '',
  showPassword: false,
  isSubmitting: false,
  errors: {},
};
```

---

## 测试用例

### 功能测试
- [ ] 国家区号选择正常
- [ ] 手机号输入验证正确
- [ ] 密码显示/隐藏切换正常
- [ ] 清空按钮功能正常
- [ ] 表单验证准确
- [ ] 登录接口调用成功
- [ ] 错误提示显示正确
- [ ] 页面跳转正常

### 边界测试
- [ ] 手机号输入超长字符
- [ ] 密码输入特殊字符
- [ ] 网络异常处理
- [ ] 快速重复点击登陆按钮

### 兼容性测试
- [ ] iOS系统 (13+)
- [ ] Android系统 (8+)
- [ ] 不同屏幕尺寸
- [ ] 横屏模式

---

## 开发清单

### 必须实现
- [x] 页面布局和样式
- [x] 表单输入和验证
- [x] 密码显示/隐藏切换
- [x] 登录接口调用
- [x] 错误处理和提示
- [x] 页面跳转
- [x] 加载状态

### 可选优化
- [ ] 记住手机号功能
- [ ] 生物识别登录 (指纹/Face ID)
- [ ] 自动填充密码
- [ ] 登录失败次数限制
- [ ] 图形验证码 (安全增强)

---

## 相关文档

- **设计稿:**
  - `UI页面/登录页/登陆页-密码登陆.png`
  - `UI页面/登录页/登陆页-密码登陆-隐藏密码.png`
  - `UI页面/登录页/登陆页-密码登陆-显示密码.png`
- **相关页面文档:**
  - 验证码登录页面文档
  - 忘记密码页面文档
- **关联页面:**
  - 用户协议页面
  - 隐私政策页面
