# 08-组局中心列表页面

## 一、页面概述

### 页面信息
- **路由路径**: `/activity/list`
- **页面名称**: 组局中心
- **用户角色**: 登录用户
- **页面类型**: 列表页面

### 页面功能
组局中心列表页面展示所有可参加的活动。用户可以:
1. 浏览活动列表
2. 使用排序和筛选功能
3. 点击活动查看详情
4. 发布新的组局活动

### 进入方式
- 从首页点击"组局中心"入口

---

## 二、页面UI结构

### 2.1 顶部导航栏
```
[返回] | 组局中心 | [发布组局]
```

### 2.2 筛选栏
```
[智能排序 ▼] | [不限性别 ▼] | [成员 ▼]
```

### 2.3 活动卡片列表
```
+----------------------------------+
| [头像]  组织者昵称               |
|         [可线上][2元]            |
|                   300 人/时 →   |
|         ⏰ 6月09日08:00          |
|         📍 地址...               |
|         👥 2人                   |
+----------------------------------+
```

---

## 三、接口列表

### 3.1 页面加载时调用

#### 1. 获取活动列表
**接口:** `GET /api/activity/list`

**请求参数:**
```typescript
{
  pageNum: number;
  pageSize: number;
  sortBy?: string;
  filters?: {
    gender?: 'all' | 'male' | 'female';
    memberCount?: string;
    activityType?: string[];
    cityCode?: string;
    districtCode?: string;
  };
}
```

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    total: number;
    hasMore: boolean;
    filters: {
      sortOptions: Array<{value: string; label: string}>;
      genderOptions: Array<{value: string; label: string}>;
      memberOptions: Array<{value: string; label: string}>;
      activityTypes: Array<{value: string; label: string; icon: string}>;
    };
    list: Array<{
      activityId: number;
      organizer: {
        userId: number;
        avatar: string;
        nickname: string;
      };
      title?: string;
      activityType: {type: string; label: string; icon: string};
      tags: Array<{text: string; type: string; color?: string}>;
      price: {amount: number; unit: string; displayText: string};
      schedule: {startTime: string; displayText: string};
      location: {address: string; district?: string};
      participants: {registered: number; limit: number; displayText: string};
      status: 'open' | 'full' | 'closed';
    }>;
  }
}
```

---

## 四、数据流

### 4.1 页面打开流程
```
1. 用户点击"组局中心"入口
2. 调用GET /api/activity/list (pageNum=1)
3. 显示Loading骨架屏
4. 收到响应 → 渲染活动列表
```

### 4.2 筛选流程
```
1. 用户选择筛选条件
2. 清空当前列表
3. 重置pageNum=1
4. 调用接口获取筛选后列表
```

---

## 五、路由参数

### 跳转目标

#### 点击活动卡片
```typescript
this.$router.push({
  path: '/activity/detail',
  query: { activityId: activity.activityId }
});
```

#### 点击发布组局
```typescript
this.$router.push({ path: '/activity/publish' });
```

---

## 六、使用的后端服务

### ActivityService
- `GET /api/activity/list` - 获取活动列表

---

**文档版本**: v1.0
**最后更新**: 2025-01-XX
**维护者**: 前端团队
