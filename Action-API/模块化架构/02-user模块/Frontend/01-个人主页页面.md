# 01-个人主页页面

## 一、页面概述

- **路由路径**: `/user/profile` 或 `/profile`
- **页面功能**: 查看自己的个人主页，包括动态、收藏、点赞和资料信息
- **用户角色**: 已登录用户（查看自己的主页）
- **页面类型**: 用户中心页面

## 二、页面结构

### 2.1 顶部个人信息卡片

```
┌────────────────────────────────────────┐
│  [头像]   昵称                  [编辑]  │
│          简介文字                       │
│          📍 居住地                      │
│                                         │
│  [关注 123]  [粉丝 456]  [获赞 789]   │
└────────────────────────────────────────┘
```

**头像区域:**
- 圆形头像，80x80px
- 点击可查看大图
- 右下角显示在线状态指示器（绿点）

**基本信息:**
- 昵称：粗体，18pt
- 简介：常规字体，14pt，灰色
- 居住地：图标 + 文字，12pt

**编辑按钮:**
- 位置：右上角
- 图标：铅笔图标
- 点击跳转到编辑资料页面

**数据统计:**
- 三个可点击的统计项
- 关注数：点击查看关注列表
- 粉丝数：点击查看粉丝列表
- 获赞数：显示总获赞数

### 2.2 Tab导航栏

```
┌────────────────────────────────────────┐
│  [动态]  [收藏]  [点赞]  [资料]       │
└────────────────────────────────────────┘
```

**Tab样式:**
- 4个Tab：动态、收藏、点赞、资料
- 选中状态：紫色下划线，加粗
- 未选中：灰色文字
- 切换有滑动动画

### 2.3 Tab 1: 动态列表

**内容卡片:**
```
┌────────────────────────────────────────┐
│  [头像] 昵称                   2小时前  │
│                                         │
│  动态文字内容...                        │
│                                         │
│  [图片1] [图片2] [图片3]               │
│                                         │
│  💬 12   ❤️ 234   ⭐ 56              │
└────────────────────────────────────────┘
```

**显示内容:**
- 用户发布的所有动态/朋友圈
- 按时间倒序排列
- 支持分页加载（下拉刷新、上拉加载更多）

**交互功能:**
- 点赞：点击爱心图标
- 收藏：点击星星图标
- 评论：点击评论图标
- 点击卡片：查看动态详情

**空状态:**
```
还没有发布任何动态
[去发布]
```

### 2.4 Tab 2: 收藏列表

**内容:**
- 显示用户收藏的动态
- 卡片样式同动态列表
- 显示原作者信息

**空状态:**
```
还没有收藏任何内容
```

### 2.5 Tab 3: 点赞列表

**内容:**
- 显示用户点赞的动态
- 卡片样式同动态列表
- 显示原作者信息

**空状态:**
```
还没有点赞任何内容
```

### 2.6 Tab 4: 资料信息

```
┌────────────────────────────────────────┐
│  基本资料                               │
│  ────────────────────────────          │
│  性别        男                         │
│  生日        1995-06-15                 │
│  居住地      广东省广州市               │
│  身高        175 cm                     │
│  体重        65 kg                      │
│  职业        软件工程师                 │
│  微信号      wechat123                  │
│                                         │
│  个性签名                               │
│  ────────────────────────────          │
│  这是我的个性签名...                    │
│                                         │
│  我的技能                               │
│  ────────────────────────────          │
│  [技能卡片1] [技能卡片2]               │
└────────────────────────────────────────┘
```

**基本资料:**
- 显示所有已填写的资料字段
- 未填写的字段不显示

**个性签名:**
- 多行文本显示
- 最多200字

**我的技能:**
- 技能卡片展示
- 点击查看技能详情
- 显示技能类型（线上/线下）、价格

---

## 三、接口列表

### 3.1 获取个人主页头部信息

**接口:** `GET /api/user/profile/header`

**触发时机:** 页面加载时

**请求头:**
```
Authorization: Bearer <token>
```

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    userId: string;
    nickname: string;
    avatar: string;
    bio?: string;
    residence?: string;
    isOnline: boolean;
    stats: {
      followingCount: number;  // 关注数
      fansCount: number;       // 粉丝数
      likesCount: number;      // 获赞总数
    };
  };
}
```

### 3.2 获取动态列表

**接口:** `GET /api/user/profile/posts`

**触发时机:** 点击"动态"Tab

**请求头:**
```
Authorization: Bearer <token>
```

**请求参数:**
```typescript
{
  page: number;      // 页码，从1开始
  pageSize: number;  // 每页数量，默认20
}
```

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    posts: Array<{
      postId: string;
      userId: string;
      nickname: string;
      avatar: string;
      content: string;
      images?: string[];
      video?: {
        url: string;
        coverUrl: string;
        duration: number;
      };
      createdAt: string;
      stats: {
        commentsCount: number;
        likesCount: number;
        favoritesCount: number;
      };
      isLiked: boolean;
      isFavorited: boolean;
    }>;
    total: number;
    hasMore: boolean;
  };
}
```

### 3.3 获取收藏列表

**接口:** `GET /api/user/profile/favorites`

**触发时机:** 点击"收藏"Tab

**请求头:**
```
Authorization: Bearer <token>
```

**请求参数:**
```typescript
{
  page: number;
  pageSize: number;
}
```

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    favorites: Array<{
      favoriteId: string;
      post: {
        postId: string;
        userId: string;
        nickname: string;
        avatar: string;
        content: string;
        images?: string[];
        createdAt: string;
        stats: {
          commentsCount: number;
          likesCount: number;
          favoritesCount: number;
        };
        isLiked: boolean;
        isFavorited: boolean;
      };
      favoritedAt: string;
    }>;
    total: number;
    hasMore: boolean;
  };
}
```

### 3.4 获取点赞列表

**接口:** `GET /api/user/profile/likes`

**触发时机:** 点击"点赞"Tab

**请求头:**
```
Authorization: Bearer <token>
```

**请求参数:**
```typescript
{
  page: number;
  pageSize: number;
}
```

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    likes: Array<{
      likeId: string;
      post: {
        postId: string;
        userId: string;
        nickname: string;
        avatar: string;
        content: string;
        images?: string[];
        createdAt: string;
        stats: {
          commentsCount: number;
          likesCount: number;
          favoritesCount: number;
        };
        isLiked: boolean;
        isFavorited: boolean;
      };
      likedAt: string;
    }>;
    total: number;
    hasMore: boolean;
  };
}
```

### 3.5 获取个人资料信息

**接口:** `GET /api/user/profile/info`

**触发时机:** 点击"资料"Tab

**请求头:**
```
Authorization: Bearer <token>
```

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    userId: string;
    nickname: string;
    avatar: string;
    gender?: "male" | "female" | "other";
    birthday?: string;
    residence?: string;
    height?: number;
    weight?: number;
    occupation?: string;
    wechat?: string;
    bio?: string;
    skills?: Array<{
      skillId: string;
      skillName: string;
      skillType: "online" | "offline";
      price: number;
      priceUnit: string;
      coverImage: string;
      isOnline: boolean;
    }>;
  };
}
```

### 3.6 点赞动态

**接口:** `POST /api/moments/{momentId}/like`

**触发时机:** 点击动态的点赞按钮

**请求头:**
```
Authorization: Bearer <token>
```

**路径参数:**
- `momentId`: 动态ID

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    isLiked: boolean;
    likesCount: number;
  };
}
```

### 3.7 取消点赞

**接口:** `DELETE /api/moments/{momentId}/like`

**触发时机:** 再次点击已点赞的动态

**请求头:**
```
Authorization: Bearer <token>
```

**路径参数:**
- `momentId`: 动态ID

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    isLiked: boolean;
    likesCount: number;
  };
}
```

### 3.8 收藏动态

**接口:** `POST /api/moments/{momentId}/favorite`

**触发时机:** 点击动态的收藏按钮

**请求头:**
```
Authorization: Bearer <token>
```

**路径参数:**
- `momentId`: 动态ID

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    isFavorited: boolean;
    favoritesCount: number;
  };
}
```

### 3.9 取消收藏

**接口:** `DELETE /api/moments/{momentId}/favorite`

**触发时机:** 再次点击已收藏的动态

**请求头:**
```
Authorization: Bearer <token>
```

**路径参数:**
- `momentId`: 动态ID

**响应数据:**
```typescript
{
  code: number;
  message: string;
  data: {
    isFavorited: boolean;
    favoritesCount: number;
  };
}
```

---

## 四、数据流

### 页面加载流程
```
用户进入个人主页
  ↓
GET /api/user/profile/header
  ↓
显示头部信息（头像、昵称、统计数据）
  ↓
默认选中"动态"Tab
  ↓
GET /api/user/profile/posts (page=1)
  ↓
显示动态列表
```

### Tab切换流程
```
用户点击Tab
  ↓
切换Tab选中状态
  ↓
根据Tab类型调用不同接口:
  - 动态: GET /api/user/profile/posts
  - 收藏: GET /api/user/profile/favorites
  - 点赞: GET /api/user/profile/likes
  - 资料: GET /api/user/profile/info
  ↓
显示对应内容
```

### 点赞/收藏流程
```
用户点击点赞/收藏按钮
  ↓
检查当前状态
  ├─ 未点赞/收藏: POST /api/moments/{momentId}/like|favorite
  └─ 已点赞/收藏: DELETE /api/moments/{momentId}/like|favorite
  ↓
更新UI状态（图标颜色、数量）
  ↓
更新列表中的数据
```

### 分页加载流程
```
用户滑动到列表底部
  ↓
检查是否还有更多数据 (hasMore)
  ↓
如果有: 调用接口加载下一页
  - page = currentPage + 1
  ↓
追加数据到列表末尾
  ↓
更新hasMore状态
```

---

## 五、状态管理

```typescript
interface ProfilePageState {
  // 头部信息
  header: {
    userId: string;
    nickname: string;
    avatar: string;
    bio?: string;
    residence?: string;
    isOnline: boolean;
    stats: {
      followingCount: number;
      fansCount: number;
      likesCount: number;
    };
  } | null;

  // 当前Tab
  currentTab: 'posts' | 'favorites' | 'likes' | 'info';

  // 动态列表
  posts: {
    data: Post[];
    page: number;
    hasMore: boolean;
    isLoading: boolean;
    total: number;
  };

  // 收藏列表
  favorites: {
    data: Favorite[];
    page: number;
    hasMore: boolean;
    isLoading: boolean;
    total: number;
  };

  // 点赞列表
  likes: {
    data: Like[];
    page: number;
    hasMore: boolean;
    isLoading: boolean;
    total: number;
  };

  // 资料信息
  info: {
    data: UserInfo | null;
    isLoading: boolean;
  };

  // UI状态
  isRefreshing: boolean;
  error?: string;
}

interface Post {
  postId: string;
  userId: string;
  nickname: string;
  avatar: string;
  content: string;
  images?: string[];
  video?: VideoInfo;
  createdAt: string;
  stats: {
    commentsCount: number;
    likesCount: number;
    favoritesCount: number;
  };
  isLiked: boolean;
  isFavorited: boolean;
}

interface VideoInfo {
  url: string;
  coverUrl: string;
  duration: number;
}

interface Favorite {
  favoriteId: string;
  post: Post;
  favoritedAt: string;
}

interface Like {
  likeId: string;
  post: Post;
  likedAt: string;
}

interface UserInfo {
  userId: string;
  nickname: string;
  avatar: string;
  gender?: "male" | "female" | "other";
  birthday?: string;
  residence?: string;
  height?: number;
  weight?: number;
  occupation?: string;
  wechat?: string;
  bio?: string;
  skills?: Skill[];
}

interface Skill {
  skillId: string;
  skillName: string;
  skillType: "online" | "offline";
  price: number;
  priceUnit: string;
  coverImage: string;
  isOnline: boolean;
}
```

---

## 六、前端验证

本页面主要为展示页面，无需前端验证。

---

## 七、交互行为

### 7.1 头部信息区交互

**点击头像:**
1. 显示头像大图
2. 支持左右滑动查看相册
3. 点击空白处关闭

**点击编辑按钮:**
1. 跳转到编辑资料页面
2. 路由: `/user/profile/edit`

**点击关注数:**
1. 跳转到关注列表页面
2. 路由: `/user/following`

**点击粉丝数:**
1. 跳转到粉丝列表页面
2. 路由: `/user/fans`

**点击获赞数:**
1. 不跳转，仅显示总数

### 7.2 Tab切换交互

**点击Tab:**
1. 切换Tab选中状态
2. 显示滑动动画
3. 加载对应Tab的内容
4. 首次加载显示加载状态
5. 已加载过则直接显示缓存数据

**Tab缓存策略:**
- 动态列表：缓存前3页数据
- 收藏列表：缓存前3页数据
- 点赞列表：缓存前3页数据
- 资料信息：始终缓存

### 7.3 列表交互

**下拉刷新:**
1. 下拉列表顶部
2. 显示刷新图标和文字
3. 释放触发刷新
4. 重新加载第1页数据
5. 清除旧数据
6. 显示刷新成功提示

**上拉加载更多:**
1. 滑动到列表底部（距底部100px触发）
2. 检查hasMore状态
3. 如果hasMore = true:
   - 显示"加载中..."
   - 调用接口加载下一页
   - 追加数据到列表
4. 如果hasMore = false:
   - 显示"没有更多了"

**点击动态卡片:**
1. 跳转到动态详情页
2. 路由: `/moments/{momentId}`
3. 传递动态完整数据

**点赞操作:**
1. 点击爱心图标
2. 立即更新UI（图标变红色，数量+1）
3. 调用点赞接口
4. 成功：保持UI状态
5. 失败：恢复UI状态，显示错误提示

**取消点赞:**
1. 再次点击已点赞的爱心图标
2. 立即更新UI（图标变灰色，数量-1）
3. 调用取消点赞接口
4. 成功：保持UI状态
5. 失败：恢复UI状态，显示错误提示

**收藏操作:**
- 同点赞操作逻辑

**评论操作:**
1. 点击评论图标
2. 跳转到动态详情页
3. 自动聚焦到评论输入框

### 7.4 资料Tab交互

**查看技能详情:**
1. 点击技能卡片
2. 跳转到技能详情页
3. 路由: `/skills/{skillId}`

**编辑技能:**
1. 在技能卡片上显示编辑按钮
2. 点击跳转到技能编辑页
3. 路由: `/user/skills/{skillId}/edit`

### 7.5 错误处理

**网络错误:**
1. 显示错误提示："网络异常，请稍后重试"
2. 显示重试按钮
3. 点击重试重新加载

**空数据:**
1. 显示空状态插图和文字
2. 动态Tab：显示"去发布"按钮
3. 收藏/点赞Tab：显示鼓励文字

**Token过期:**
1. 提示："登录已过期，请重新登录"
2. 跳转到登录页

---

## 八、测试要点

### 功能测试
- [ ] 头部信息正确显示
- [ ] 统计数据准确
- [ ] Tab切换正常
- [ ] 动态列表正确加载
- [ ] 收藏列表正确加载
- [ ] 点赞列表正确加载
- [ ] 资料信息正确显示
- [ ] 点赞操作成功
- [ ] 收藏操作成功
- [ ] 分页加载正常

### 边界测试
- [ ] 空列表显示正确
- [ ] 网络异常处理
- [ ] Token过期处理
- [ ] 快速切换Tab
- [ ] 快速点击点赞/收藏
- [ ] 分页到最后一页
- [ ] 单条动态有大量图片

### 性能测试
- [ ] 列表滚动流畅
- [ ] Tab切换无卡顿
- [ ] 图片懒加载正常
- [ ] 内存占用合理
- [ ] 缓存策略有效

### 用户体验测试
- [ ] 加载状态友好
- [ ] 错误提示清晰
- [ ] 动画效果流畅
- [ ] 空状态引导明确
- [ ] 点赞/收藏反馈及时

---

## 九、使用的后端服务

- **UserService** (用户服务)
  - `GET /api/user/profile/header` - 获取个人主页头部信息
  - `GET /api/user/profile/posts` - 获取动态列表
  - `GET /api/user/profile/favorites` - 获取收藏列表
  - `GET /api/user/profile/likes` - 获取点赞列表
  - `GET /api/user/profile/info` - 获取个人资料信息

- **ContentService** (内容服务)
  - `POST /api/moments/{momentId}/like` - 点赞动态
  - `DELETE /api/moments/{momentId}/like` - 取消点赞
  - `POST /api/moments/{momentId}/favorite` - 收藏动态
  - `DELETE /api/moments/{momentId}/favorite` - 取消收藏

---

## 十、相关页面

- [02-编辑资料页面](./02-编辑资料页面.md) - 编辑个人资料
- [03-他人主页页面](./03-他人主页页面.md) - 查看他人主页
- [04-粉丝列表页面](./04-粉丝列表页面.md) - 粉丝列表
- [05-关注列表页面](./05-关注列表页面.md) - 关注列表
- [06-技能管理页面](./06-技能管理页面.md) - 管理技能

---

**使用后端服务:** UserService + ContentService (user模块 + content模块)
**最后更新:** 2025-11-14
