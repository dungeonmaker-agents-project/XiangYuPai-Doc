# 02-user模块 - 接口一致性验证报告

> **验证时间**: 2025-11-14
> **验证范围**: 后端接口文档 vs 实际实现
> **状态**: ⚠️ 发现不一致项，需要调整

---

## 一、配置差异

### 1.1 端口配置

| 项目 | 文档值 | 实现值 | 状态 | 建议 |
|------|--------|--------|------|------|
| 服务端口 | 8002 | 9401 | ❌ 不一致 | 保持9401，更新文档 |
| 数据库名 | user_db | xypai_user | ❌ 不一致 | 保持xypai_user，更新文档 |

**修复建议**: 更新文档中的配置信息以匹配实现。

---

## 二、API路径差异

### 2.1 个人资料管理接口 ✅ 基本一致

| 文档API | 实现API | 状态 | 说明 |
|---------|---------|------|------|
| `GET /api/user/profile/header` | `GET /api/user/profile/header` | ✅ 一致 | |
| `GET /api/user/profile/edit` | `GET /api/user/profile/edit` | ✅ 一致 | |
| `POST /api/user/avatar/upload` | `POST /api/user/profile/avatar/upload` | ⚠️ 路径稍有差异 | 建议统一为 `/api/user/profile/avatar/upload` |
| `PUT /api/user/profile/nickname` | `PUT /api/user/profile/nickname` | ✅ 一致 | |
| `PUT /api/user/profile/gender` | `PUT /api/user/profile/gender` | ✅ 一致 | |
| `PUT /api/user/profile/birthday` | `PUT /api/user/profile/birthday` | ✅ 一致 | |
| `PUT /api/user/profile/residence` | `PUT /api/user/profile/residence` | ✅ 一致 | |
| `PUT /api/user/profile/height` | `PUT /api/user/profile/height` | ✅ 一致 | |
| `PUT /api/user/profile/weight` | `PUT /api/user/profile/weight` | ✅ 一致 | |
| `PUT /api/user/profile/occupation` | `PUT /api/user/profile/occupation` | ✅ 一致 | |
| `PUT /api/user/profile/wechat` | `PUT /api/user/profile/wechat` | ✅ 一致 | |
| `PUT /api/user/profile/bio` | `PUT /api/user/profile/bio` | ✅ 一致 | |

### 2.2 他人主页接口 ⚠️ 路径结构不同

| 文档API | 实现API | 状态 | 建议修复 |
|---------|---------|------|----------|
| `GET /api/users/{userId}/profile` | `GET /api/user/profile/other/{targetUserId}` | ❌ 不一致 | 需要添加新endpoint |
| `POST /api/users/{userId}/follow` | `POST /api/user/relation/follow/{followingId}` | ❌ 不一致 | 需要添加新endpoint |
| `DELETE /api/users/{userId}/follow` | `DELETE /api/user/relation/follow/{followingId}` | ❌ 不一致 | 需要添加新endpoint |
| `GET /api/users/{userId}/moments` | ❌ 未实现 | ❌ 缺失 | 应由ContentService提供 |
| `GET /api/users/{userId}/skills` | `GET /api/user/skill/user/{userId}` | ⚠️ 路径不同 | 需要添加别名路由 |
| `GET /api/users/{userId}/profile-detail` | ❌ 未实现 | ⚠️ 缺失 | 可与profile合并 |
| `POST /api/users/{userId}/report` | `POST /api/user/relation/report` | ❌ 结构不同 | 需要调整 |
| `POST /api/users/{userId}/block` | `POST /api/user/relation/block` | ❌ 结构不同 | 需要调整 |

### 2.3 关注粉丝接口 ❌ 路径差异较大

| 文档API | 实现API | 状态 | 建议修复 |
|---------|---------|------|----------|
| `GET /api/users/fans` | `GET /api/user/relation/fans` | ❌ 不一致 | 需要添加别名路由 |
| `GET /api/users/following` | `GET /api/user/relation/following` | ❌ 不一致 | 需要添加别名路由 |
| `GET /api/users/fans/search` | `GET /api/user/relation/fans?keyword=xxx` | ⚠️ 合并到一个endpoint | 可接受 |
| `GET /api/users/following/search` | `GET /api/user/relation/following?keyword=xxx` | ⚠️ 合并到一个endpoint | 可接受 |
| `POST /api/users/follow` | `POST /api/user/relation/follow/{followingId}` | ❌ 结构不同 | 需要调整 |
| `DELETE /api/users/follow/{targetUserId}` | `DELETE /api/user/relation/follow/{followingId}` | ⚠️ 参数名不同 | 可接受 |
| `GET /api/users/{userId}/follow-stats` | ❌ 未实现 | ⚠️ 缺失 | 可从profile获取 |

### 2.4 技能管理接口 ❌ 结构差异显著

| 文档API | 实现API | 状态 | 建议修复 |
|---------|---------|------|----------|
| `GET /api/user/skills` | `GET /api/user/skill/my` | ❌ 路径不同 | 需要添加别名 |
| `GET /api/skills/config` | ❌ 未实现 | ❌ 缺失 | **需要新增** |
| `POST /api/user/skills/online` | `POST /api/user/skill` (统一endpoint) | ❌ 不一致 | **需要拆分** |
| `POST /api/user/skills/offline` | `POST /api/user/skill` (统一endpoint) | ❌ 不一致 | **需要拆分** |
| `POST /api/skills/images/upload` | ❌ 未实现 | ❌ 缺失 | **需要新增** |
| `GET /api/user/skills/{skillId}` | `GET /api/user/skill/{skillId}` | ⚠️ 单复数差异 | 需要统一 |
| `PUT /api/user/skills/online/{skillId}` | `PUT /api/user/skill/{skillId}` (统一endpoint) | ❌ 不一致 | **需要拆分** |
| `PUT /api/user/skills/offline/{skillId}` | `PUT /api/user/skill/{skillId}` (统一endpoint) | ❌ 不一致 | **需要拆分** |
| `DELETE /api/user/skills/{skillId}` | `DELETE /api/user/skill/{skillId}` | ⚠️ 单复数差异 | 需要统一 |
| `PUT /api/user/skills/{skillId}/status` | `PUT /api/user/skill/{skillId}/toggle?isOnline=xxx` | ⚠️ 参数传递方式不同 | 建议改为RequestBody |
| `GET /api/skills/available` | ❌ 未实现 | ⚠️ 缺失 | 可通过my+filter实现 |
| `GET /api/skills/{skillId}/booking-detail` | `GET /api/user/skill/{skillId}` | ⚠️ 功能重复 | 可合并 |
| `GET /api/skills/{skillId}/reviews` | ❌ 未实现 | ❌ 缺失 | 应由OrderService提供 |

---

## 三、缺失的接口

### 3.1 应由UserService实现的缺失接口

| API | 功能 | 优先级 | 建议 |
|-----|------|--------|------|
| `GET /api/skills/config` | 获取技能配置（游戏列表、服务类型） | 🔴 高 | **必须新增** |
| `POST /api/skills/images/upload` | 上传技能图片 | 🔴 高 | **必须新增** |
| `GET /api/users/{userId}/follow-stats` | 获取关注统计 | 🟡 中 | 可从profile获取，优先级低 |
| `GET /api/skills/available` | 获取可用技能列表 | 🟡 中 | 可通过筛选my实现 |

### 3.2 应由其他服务提供的接口（文档错误）

这些接口在UserService文档中定义，但实际应由其他服务提供：

| API | 功能 | 正确归属 | 说明 |
|-----|------|----------|------|
| `GET /api/user/profile/posts` | 获取用户动态 | ContentService | ❌ 文档错误 |
| `GET /api/user/profile/favorites` | 获取收藏列表 | ContentService | ❌ 文档错误 |
| `GET /api/user/profile/likes` | 获取点赞列表 | ContentService | ❌ 文档错误 |
| `GET /api/users/{userId}/moments` | 获取他人动态 | ContentService | ❌ 文档错误 |
| `GET /api/skills/{skillId}/reviews` | 获取技能评价 | OrderService | ❌ 文档错误 |

---

## 四、数据结构差异

### 4.1 UserProfileVo 对比

**文档定义:**
```typescript
{
  userId: string;
  nickname: string;
  avatar: string;
  bio?: string;
  residence?: string;
  isOnline: boolean;
  stats: {
    followingCount: number;
    fansCount: number;
    likesCount: number;
  };
  followStatus: "none" | "following" | "mutual";
  privacy: {
    canViewProfile: boolean;
    canViewMoments: boolean;
    canViewSkills: boolean;
  };
}
```

**实现:**
```java
{
  userId: Long;
  nickname: String;
  avatar: String;
  gender: String;           // ✅ 新增
  birthday: LocalDate;      // ✅ 新增
  residence: String;
  height: Integer;          // ✅ 新增
  weight: Integer;          // ✅ 新增
  occupation: String;       // ✅ 新增
  wechat: String;           // ✅ 新增
  bio: String;
  isOnline: Boolean;
  stats: UserStatsVo;       // ✅ 更完整
  followStatus: String;
  privacy: PrivacyVo;
}
```

**评估**: ✅ 实现比文档更完整，文档需要更新

### 4.2 UserStatsVo 对比

**文档定义:**
```typescript
{
  followingCount: number;
  fansCount: number;
  likesCount: number;
}
```

**实现:**
```java
{
  followingCount: Integer;
  fansCount: Integer;
  likesCount: Integer;
  momentsCount: Integer;     // ✅ 新增
  postsCount: Integer;       // ✅ 新增
  collectionsCount: Integer; // ✅ 新增
  skillsCount: Integer;      // ✅ 新增
  ordersCount: Integer;      // ✅ 新增
}
```

**评估**: ✅ 实现更完善

---

## 五、修复优先级建议

### 🔴 P0 - 必须立即修复

1. **新增技能配置接口** `GET /api/skills/config`
   - 前端创建技能时需要游戏列表和服务类型配置
   - 建议返回静态配置或从配置表读取

2. **新增图片上传接口** `POST /api/skills/images/upload`
   - 技能展示图片上传必需
   - 返回图片URL供前端使用

3. **拆分技能创建/更新接口**
   - 分离 `POST /api/user/skills/online` 和 `POST /api/user/skills/offline`
   - 分离 `PUT /api/user/skills/online/{id}` 和 `PUT /api/user/skills/offline/{id}`
   - 或保持统一endpoint，但明确文档说明

### 🟡 P1 - 高优先级（1-2天内）

4. **添加API路由别名**
   - 为保持向前兼容，添加文档中的路径作为别名
   - 例如: `GET /api/users/fans` → `GET /api/user/relation/fans`

5. **统一路径命名**
   - `/api/user/skill` vs `/api/user/skills` 统一为 `/api/user/skills`
   - `/api/users/{userId}` vs `/api/user/profile/other/{userId}` 添加别名

6. **调整举报/拉黑接口**
   - 改为 `POST /api/users/{userId}/report`
   - 改为 `POST /api/users/{userId}/block`

### 🟢 P2 - 中优先级（1周内）

7. **更新文档**
   - 更新端口号: 8002 → 9401
   - 更新数据库名: user_db → xypai_user
   - 移除不属于UserService的接口（posts/favorites/likes/moments/reviews）

8. **完善数据结构文档**
   - 更新UserProfileVo包含完整字段
   - 更新UserStatsVo包含所有统计字段

---

## 六、推荐的修复方案

### 方案A: 调整实现以匹配文档（推荐）

**优点:**
- 前端无需修改
- API设计更符合RESTful规范
- 文档已经评审通过

**缺点:**
- 需要修改已实现的Controller
- 测试工作量较大

**工作量**: 2-3天

### 方案B: 更新文档以匹配实现

**优点:**
- 后端无需改动
- 实现已完成且经过测试

**缺点:**
- 前端需要同步更新
- API路径不够规范

**工作量**: 0.5天（仅文档）

### 方案C: 混合方案（最佳平衡）

**核心思路**: 保持当前实现，添加路由别名支持文档中的路径

**优点:**
- 同时支持两套路径，兼容性最好
- 后端改动最小（仅添加路由映射）
- 前端可以选择使用任一套路径

**缺点:**
- 路由表略显冗余

**工作量**: 1天

---

## 七、具体修复代码示例

### 7.1 新增技能配置接口

```java
@RestController
@RequestMapping("/api/skills")
public class SkillConfigController {

    @GetMapping("/config")
    public R<SkillConfigVo> getSkillConfig() {
        // 游戏列表
        List<GameInfo> games = Arrays.asList(
            new GameInfo("王者荣耀", Arrays.asList("青铜", "白银", "黄金", "铂金", "钻石", "星耀", "王者")),
            new GameInfo("和平精英", Arrays.asList("青铜", "白银", "黄金", "铂金", "钻石", "皇冠", "王牌")),
            new GameInfo("英雄联盟", Arrays.asList("黑铁", "青铜", "白银", "黄金", "铂金", "钻石", "大师", "王者"))
        );

        // 服务类型列表
        List<ServiceType> serviceTypes = Arrays.asList(
            new ServiceType("家政服务", "cleaning"),
            new ServiceType("维修服务", "repair"),
            new ServiceType("摄影服务", "photography"),
            new ServiceType("教学辅导", "tutoring")
        );

        return R.ok(new SkillConfigVo(games, serviceTypes));
    }
}
```

### 7.2 新增图片上传接口

```java
@RestController
@RequestMapping("/api/skills")
public class SkillImageController {

    private final IOssService ossService;

    @PostMapping("/images/upload")
    public R<String> uploadSkillImage(@RequestParam("image") MultipartFile file) {
        // 验证文件
        if (file.getSize() > 5 * 1024 * 1024) {
            return R.fail("图片大小不能超过5MB");
        }

        // 上传到OSS
        String imageUrl = ossService.upload(file, "skills/");

        return R.ok(imageUrl);
    }
}
```

### 7.3 添加路由别名

```java
// 在RelationController中添加
@PostMapping("/api/users/{userId}/follow")
public R<Void> followUserAlias(@PathVariable Long userId) {
    Long currentUserId = LoginHelper.getUserId();
    return relationService.followUser(currentUserId, userId);
}

@GetMapping("/api/users/fans")
public TableDataInfo<UserRelationVo> getFansListAlias(
    @RequestParam(required = false) String keyword,
    PageQuery pageQuery
) {
    Long userId = LoginHelper.getUserId();
    return relationService.getFansList(userId, keyword, pageQuery);
}
```

---

## 八、总结

### 当前状态

- ✅ **核心功能**: 90%完成
- ⚠️ **API一致性**: 70%一致
- ❌ **缺失接口**: 2个关键接口（config, image upload）

### 建议行动

1. **立即新增**: 技能配置接口、图片上传接口（0.5天）
2. **添加别名路由**: 支持文档中的API路径（0.5天）
3. **更新文档**: 修正端口、数据库名、数据结构（0.5天）
4. **测试验证**: 完整API测试（1天）

**总工作量**: 2.5天

---

**验证人**: System
**审核状态**: 待审核
**下一步**: 制定详细修复计划
