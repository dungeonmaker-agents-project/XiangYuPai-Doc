# 🌳 发现页 结构指导文档

> **页面名称：** 发现页（Discover）
> **所属模块：** Discover
> **页面类型：** 列表页（Tab切换 + 瀑布流）

---

## 📋 STEP 1: 页面数据模型

### 设计理念

```
数据驱动UI，先定义"页面需要什么数据"，再考虑"数据从哪来"

┌─────────────────────────────────────────────────────────────────┐
│  STEP 1: 数据模型                    STEP 6: 数据实现            │
│  ─────────────────                   ─────────────────           │
│  PageModel {                    →    GET /api/discover/config    │
│    TabData                           GET /api/discover/list      │
│    ListData                          POST /api/discover/like     │
│  }                                   数据库表设计                 │
└─────────────────────────────────────────────────────────────────┘
```

### PageModel（页面级数据模型）

```json
{
  "模型信息": {
    "页面名称": "发现页",
    "页面标识": "DiscoverPage",
    "所属模块": "Discover",
    "页面类型": "列表页"
  },

  "数据依赖图": "TabData + ListData → 渲染页面 → 用户操作 → ActionResult",

  "PageModel": {
    "_说明": "页面级数据模型 [L1]，组合所有区域数据",

    "TabData": {
      "_说明": "Tab区域数据 → [→TabNavArea]",
      "tabs": "Array<TabItem> - Tab列表 - [UI:TabNavArea]",
      "currentTab": "String(follow|hot|nearby) - 当前选中Tab - [状态:控制列表筛选]"
    },

    "ListData": {
      "_说明": "列表区域数据 → [→ContentListArea]",
      "list": "Array<ContentItem> - 内容列表 - [UI:ContentListArea]",
      "hasMore": "Boolean - 是否有更多 - [控制:加载更多/到底提示]",
      "total": "Number? - 总数 - [可选]"
    },

    "PageState": {
      "_说明": "页面状态数据",
      "isLoading": "Boolean - 加载中 - [状态:骨架屏/Loading]",
      "isRefreshing": "Boolean - 刷新中 - [状态:下拉刷新指示器]",
      "isError": "Boolean - 加载失败 - [状态:错误提示]",
      "errorMessage": "String? - 错误信息 - [UI:错误提示文案]"
    }
  }
}
```

### AreaModel（区域级数据模型）

```json
{
  "AreaModel": {
    "_说明": "区域级数据模型 [L2]",

    "TabNavArea": {
      "tabs": "Array<TabItem> - Tab列表",
      "TabItem": {
        "key": "String(follow|hot|nearby) - Tab标识 - [用于:筛选参数]",
        "label": "String - Tab文案 - [UI:Tab.文字]",
        "isActive": "Boolean - 是否选中 - [状态:选中样式(加粗+下划线)]"
      }
    },

    "ContentListArea": {
      "list": "Array<ContentItem> - 内容列表",
      "layoutType": "String(waterfall) - 布局类型 - [控制:瀑布流2列]"
    }
  }
}
```

### ItemModel（列表项数据模型）

```json
{
  "ItemModel": {
    "_说明": "列表项数据模型 [L3]",

    "ContentItem": {
      "_说明": "内容卡片数据 → [→ContentCard]",

      "id": "String - 内容ID - [用于:跳转详情、操作参数]",
      "type": "String(image|video) - 内容类型 - [条件:video时显示播放按钮]",

      "mediaData": {
        "_说明": "媒体区域 → [→ContentCard.媒体区]",
        "coverUrl": "String - 封面URL - [UI:卡片.封面图]",
        "aspectRatio": "Number - 宽高比 - [计算:高度=宽度/ratio，瀑布流动态高度]",
        "isVideo": "Boolean - 是否视频 - [条件:true时显示播放图标]"
      },

      "textData": {
        "_说明": "文本区域 → [→ContentCard.文本区]",
        "title": "String - 标题 - [UI:卡片.标题] [样式:14sp,最多2行,省略]"
      },

      "authorData": {
        "_说明": "作者信息 → [→ContentCard.作者区]",
        "userId": "String - 用户ID - [用于:跳转用户主页]",
        "avatar": "String - 头像URL - [UI:卡片.头像] [样式:24x24圆形]",
        "nickname": "String - 昵称 - [UI:卡片.昵称] [样式:12sp,#666666]"
      },

      "statsData": {
        "_说明": "统计数据 → [→ContentCard.统计区]",
        "likeCount": "Number - 点赞数 - [UI:卡片.点赞] [格式:>=10000显示1.2w]",
        "isLiked": "Boolean - 是否已赞 - [状态:点赞图标] [true=红心♥,false=空心♡]"
      },

      "metaData": {
        "_说明": "元信息",
        "createTime": "String(ISO8601) - 创建时间 - [格式:刚刚/X分钟前/MM-DD]"
      }
    }
  }
}
```

### SharedModel（共享数据模型）

```json
{
  "SharedModel": {
    "_说明": "跨页面复用的数据模型 [L4]",

    "UserInfo": {
      "_说明": "用户基础信息（多处复用）",
      "userId": "String - 用户ID",
      "nickname": "String - 昵称",
      "avatar": "String - 头像URL"
    },

    "PaginationInfo": {
      "_说明": "分页信息",
      "page": "Number - 当前页码",
      "pageSize": "Number - 每页数量",
      "hasMore": "Boolean - 是否有更多",
      "total": "Number? - 总数"
    },

    "ActionResult": {
      "_说明": "操作结果",
      "success": "Boolean - 是否成功",
      "message": "String? - 提示信息",
      "newValue": "Any? - 新值（如新的点赞数）"
    },

    "TabBarData": {
      "_说明": "底部导航数据（全局共享）",
      "tabs": "Array<TabBarItem> - 导航列表",
      "TabBarItem": {
        "key": "String(home|discover|message|profile) - 标识",
        "label": "String - 文案",
        "icon": "String - 图标",
        "iconActive": "String - 选中图标",
        "isActive": "Boolean - 是否选中",
        "badge": "Number? - 角标数"
      }
    }
  }
}
```

### 数据模型汇总表

| 模型名 | 层级 | 对应组件 | 说明 |
|--------|------|----------|------|
| PageModel | L1 | DiscoverPage | 页面级数据，组合所有区域 |
| TabData | L2 | TabNavArea | Tab导航数据 |
| ListData | L2 | ContentListArea | 内容列表数据 |
| PageState | L2 | - | 页面状态（加载/刷新/错误） |
| TabItem | L3 | TabItem | 单个Tab项数据 |
| ContentItem | L3 | ContentCard | 单个内容卡片数据 |
| UserInfo | L4 | - | 用户信息（跨页面复用） |
| PaginationInfo | L4 | - | 分页信息（跨页面复用） |
| ActionResult | L4 | - | 操作结果（跨页面复用） |
| TabBarData | L4 | TabBarArea | 底部导航（全局共享） |

---

## 🔄 STEP 2: 流程图

### 页面完整流程

```
开始
 ↓
【进入发现页】
 ├─→ 路由跳转: /discover
 ├─→ TabBar点击: 从其他Tab切换
 └─→ 深链接: xypai://discover
 ↓
【页面初始化】
 │
 ├─→ 调用 [API:discoverConfig] → 获取Tab配置
 │    └─→ 渲染 [→TabNavArea] Tab列表(关注/热门/同城)
 │
 ├─→ 调用 [API:discoverList] (tab=follow, page=1) → 获取首屏数据
 │    └─→ 渲染 [→ContentListArea] 瀑布流内容列表
 │
 └─→ 初始化监听
      ├─→ 滚动监听 → [触发:ContentListArea]
      └─→ 下拉刷新监听 → [触发:ContentListArea]
 ↓
用户操作
 │
 ├─→ 【ContentListArea 列表交互】
 │    │
 │    ├─→ 下拉刷新 [触发:ContentListArea]
 │    │    ├─→ 调用 [API:discoverList] (tab=currentTab, page=1)
 │    │    └─→ 清空重置 [状态:ContentListArea]
 │    │
 │    ├─→ 上拉加载 [触发:ContentListArea]
 │    │    ├─→ 调用 [API:discoverList] (tab=currentTab, page+1)
 │    │    └─→ 追加数据 [状态:ContentListArea]
 │    │
 │    └─→ 滚动浏览 → 图片懒加载
 │
 ├─→ 【ContentCard 单项操作】
 │    │
 │    ├─→ 点击卡片 [触发:ContentCard]
 │    │    └─→ 跳转 → 【内容详情页】(携带 contentId)
 │    │
 │    ├─→ 点击头像/昵称 [触发:ContentCard.AuthorInfo]
 │    │    └─→ 跳转 → 【用户主页】(携带 userId)
 │    │
 │    └─→ 点击点赞按钮 [触发:ContentCard.LikeButton]
 │         ├─→ 乐观更新 [状态:ContentCard] isLiked切换 + likeCount±1
 │         ├─→ 调用 [API:likeAction] (contentId, action=like/unlike)
 │         ├─→ 成功 → 保持状态
 │         └─→ 失败 → 回滚 [状态:ContentCard] + Toast提示
 │
 ├─→ 【TabNavArea Tab切换】
 │    └─→ 点击Tab [触发:TabNavArea]
 │         ├─→ 更新选中态 [状态:TabNavArea]
 │         ├─→ 调用 [API:discoverList] (tab=selectedTab, page=1)
 │         └─→ 替换内容 [状态:ContentListArea]
 │
 ├─→ 【HeaderArea 操作】
 │    └─→ 点击相机图标 [触发:HeaderArea.CameraButton]
 │         └─→ 跳转 → 【内容发布页】
 │
 └─→ 【TabBarArea 导航跳转】
      └─→ 点击其他Tab [触发:TabBarArea]
           └─→ 跳转 → 【首页/消息/我的】
 ↓
【状态管理】 [状态:ContentListArea]
 ├─→ isLoading && page=1 → 显示骨架屏
 ├─→ isLoading && page>1 → 显示底部Loading
 ├─→ isError → 显示错误提示 → 点击重试
 ├─→ list.length=0 → 显示空状态插画
 └─→ hasMore=false → 显示"已经到底啦~"
 ↓
结束
```

### 流程-组件关联表

| 流程节点 | 触发组件 | 调用接口 | 影响组件 | 状态变化 |
|----------|----------|----------|----------|----------|
| 页面初始化 | - | discoverConfig, discoverList | TabNavArea, ContentListArea | loading→loaded |
| 下拉刷新 | ContentListArea | discoverList(page=1) | ContentListArea | refreshing→loaded |
| 上拉加载 | ContentListArea | discoverList(page+1) | ContentListArea | loadingMore→loaded |
| 点击卡片 | ContentCard | - | - | 跳转内容详情页 |
| 点击头像 | ContentCard.AuthorInfo | - | - | 跳转用户主页 |
| 点击点赞 | ContentCard.LikeButton | likeAction | ContentCard | isLiked/likeCount切换 |
| Tab切换 | TabNavArea | discoverList(tab=X) | ContentListArea | 内容替换 |
| 点击相机 | HeaderArea | - | - | 跳转发布页 |
| 底部导航 | TabBarArea | - | - | 页面切换 |

---

## 🌳 STEP 3: 树状图

```
【Discover模块】★★★
│
├── 【发现页】📱 [L1] [拆分:4个L2]
│   │   文件: Discover/DiscoverPage/index.tsx
│   │
│   ├── 📍 TabNavArea [L2] [固定顶部]
│   │   │   文件: TabNavArea.tsx
│   │   │   布局: flex-row, center, gap-32px
│   │   │   高度: 44px
│   │   │
│   │   ├── 《TabItem》[L3] [1模板x3实例] [data:tabs]
│   │   │   ├── 关注 [data:tabs[0].label] [条件:isActive→加粗+下划线]
│   │   │   ├── 热门 [data:tabs[1].label]
│   │   │   └── 同城 [data:tabs[2].label]
│   │   │
│   │   └── 📷 CameraButton [L3] [位置:TR]
│   │       └── 相机图标 → 点击跳转发布页
│   │
│   ├── 🏷️ ContentListArea [L2] [flex:1, 可滚动]
│   │   │   文件: ContentListArea/
│   │   │   ├── index.tsx
│   │   │   ├── types.ts
│   │   │   └── useContentListArea.ts
│   │   │
│   │   └── 📋 WaterfallContainer [布局:grid-cols-2, gap-8px]
│   │       └── 《ContentCard》[L3] [1模板xN实例] [data:list]
│   │           │   文件: ContentCard/
│   │           │   ├── index.tsx
│   │           │   ├── types.ts
│   │           │   └── onLike.ts
│   │           │
│   │           ├── 🖼️ MediaSection [位置:TC] [相对高度:由aspectRatio决定]
│   │           │   ├── CoverImage [data:mediaData.coverUrl]
│   │           │   └── PlayIcon [↗️右上角] [条件:type=video] [data:mediaData.isVideo]
│   │           │
│   │           ├── 📝 TextSection [位置:MC] [padding:8px]
│   │           │   └── Title [data:textData.title] [样式:14sp,#333,最多2行省略]
│   │           │
│   │           └── 👤 InfoSection [位置:BC] [flex-row, space-between] [padding:8px]
│   │               ├── AuthorInfo [flex-row, center, gap-6px]
│   │               │   ├── Avatar [data:authorData.avatar] [尺寸:24x24圆形]
│   │               │   └── Nickname [data:authorData.nickname] [样式:12sp,#666]
│   │               │
│   │               └── LikeButton [flex-row, center, gap-4px]
│   │                   ├── LikeIcon [data:statsData.isLiked] [true→♥红色,false→♡灰色]
│   │                   └── LikeCount [data:statsData.likeCount] [样式:12sp,#999]
│   │
│   └── 📍 TabBarArea [L2] [固定底部] ← 复用 Common/TabBar
│       │   文件: Common/components/TabBar/
│       │   布局: flex-row, 均分 | 高度: 83px(含安全区)
│       │
│       └── 《TabBarItem》[L3] [1模板x4实例] [data:TabBarData.tabs]
│           ├── 首页 [data:tabs[0]] [icon:🏠]
│           ├── 发现 [data:tabs[1]] [icon:🔮] [isActive:true→紫色高亮]
│           ├── 消息 [data:tabs[2]] [icon:💬]
│           └── 我的 [data:tabs[3]] [icon:👤]
│
└── 🔧 服务层（对应 STEP 6 的接口）
    ├── 📡 GET /api/discover/config    → 配置服务 [→ PageModel.TabData]
    ├── 📡 GET /api/discover/list      → 列表服务 [→ PageModel.ListData]
    └── 📡 POST /api/discover/like     → 点赞服务 [→ SharedModel.ActionResult]
```

### 🔄 列表控制（ContentListArea专用）

```
🔄 ListControl [适用于 ContentListArea]
├── 加载更多: 上滑 [距底100px] → 调用列表接口(page+1)
├── 下拉刷新: [阈值60px] → 调用列表接口(page=1)
├── 骨架屏: [条件:isLoading且page=1] 显示占位卡片
├── 加载指示: [条件:isLoading且page>1] 显示底部loading
├── 空状态: [条件:list.length=0] 显示"暂无内容"插画
└── 到底提示: [条件:hasMore=false] "已经到底啦~"
```

### 组件拆分统计

```
层级统计：
├── L1-页面: 1个 (DiscoverPage)
├── L2-区域: 3个 (TabNavArea, ContentListArea, TabBarArea)
└── L3-基础: 4个 (TabItem, ContentCard, TabBarItem, CameraButton)

总计: 8个组件
复杂度: 中等页面
```

---

## 📍 STEP 4: 布局定位

### 页面整体布局

```
┌─────────────────────────────────────────────────────────────────┐
│  📱 DiscoverPage [L1]                                           │
│  布局: flex-col | 高度: 100vh                                    │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  StatusBar [系统状态栏] 高度: 44px                          │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  TabNavArea [L2] 高度: 44px                                 │  │ 固定
│  │  ┌────────┐  ┌────────┐  ┌────────┐           ┌────┐     │  │
│  │  │  关注   │  │  热门   │  │  同城   │           │  📷 │     │  │
│  │  │  ━━━━  │  │        │  │        │           │     │     │  │
│  │  └────────┘  └────────┘  └────────┘           └────┘     │  │
│  │   [选中]      [默认]      [默认]              [TR]        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  ContentListArea [L2] flex:1 scroll-y                      │  │
│  │                                                             │  │
│  │  ┌─────────────────┐  ┌─────────────────┐                 │  │
│  │  │  ┌───────────┐  │  │  ┌───────────┐  │                 │  │
│  │  │  │           │  │  │  │           │  │                 │  │
│  │  │  │   Cover   │▶│  │  │   Cover   │  │                 │  │
│  │  │  │           │  │  │  │           │  │                 │  │ flex:1
│  │  │  └───────────┘  │  │  └───────────┘  │                 │  │ 可滚动
│  │  │  请你们看雪      │  │  请你们看雪      │                 │  │
│  │  │  👤 用户名称 ♡88│  │  👤 用户名称 ♥88│                 │  │
│  │  └─────────────────┘  └─────────────────┘                 │  │
│  │       [Card 1]             [Card 2]                        │  │
│  │                                                             │  │
│  │  ┌─────────────────┐  ┌─────────────────┐                 │  │
│  │  │  ┌───────────┐  │  │  ┌───────────┐  │                 │  │
│  │  │  │   Cover   │  │  │  │   Cover   │  │  ... x N        │  │
│  │  │  └───────────┘  │  │  └───────────┘  │                 │  │
│  │  │  ...            │  │  ...            │                 │  │
│  │  └─────────────────┘  └─────────────────┘                 │  │
│  │                                                             │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │  TabBarArea [L2] 高度: 83px (含安全区49px+34px)             │  │ 固定
│  │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐        │  │
│  │  │   🏠    │ │   🔮    │ │   💬    │ │   👤    │        │  │
│  │  │  首页   │ │  发现   │ │  消息   │ │  我的   │        │  │
│  │  │ [默认]  │ │ [选中]  │ │ [默认]  │ │ [默认]  │        │  │
│  │  └─────────┘ └─────────┘ └─────────┘ └─────────┘        │  │
│  └───────────────────────────────────────────────────────────┘  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### TabNavArea 详细布局

```
┌─────────────────────────────────────────────────────────────────┐
│  TabNavArea [L2]                                                 │
│  布局: flex-row | align: center | height: 44px                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│    ┌────────────────────────────────────────────────┐  ┌─────┐  │
│    │                                                │  │     │  │
│    │  ┌────────┐    ┌────────┐    ┌────────┐      │  │ 📷  │  │
│    │  │  关注   │    │  热门   │    │  同城   │      │  │     │  │
│    │  │  ━━━━  │    │        │    │        │      │  └─────┘  │
│    │  └────────┘    └────────┘    └────────┘      │   [TR]    │
│    │   gap:32px      gap:32px                       │  24x24   │
│    │                                                │           │
│    │            [MC - flex-row, center]             │           │
│    └────────────────────────────────────────────────┘           │
│                                                                  │
│  Tab样式：                                                        │
│  ├── 默认: 16sp, #666666, 常规                                   │
│  └── 选中: 18sp, #333333, 加粗, 带下划线(紫色渐变)                │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### ContentCard 详细布局

```
┌─────────────────────────────────────────────────────────────────┐
│  ContentCard [L3]                                                │
│  布局: flex-col | width: (screenWidth-24)/2 | 圆角: 8px          │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  MediaSection [相对定位容器]                              │    │
│  │                                                          │    │
│  │  ┌───────────────────────────────────────────────────┐  │    │
│  │  │                                                   │  │    │
│  │  │                                                   │  │    │
│  │  │              CoverImage                           │  │    │
│  │  │         [width:100%, height:auto]                 │  │    │
│  │  │         [aspectRatio: 动态]                        │  │    │
│  │  │                                                   │  │    │
│  │  │                                     ┌─────────┐   │  │    │
│  │  │                                     │   ▶    │   │  │    │
│  │  │                                     │ PlayIcon│   │  │    │
│  │  │                                     └─────────┘   │  │    │
│  │  │                                      [↗️TR]       │  │    │
│  │  │                                      [条件显示]    │  │    │
│  │  └───────────────────────────────────────────────────┘  │    │
│  │                                                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  TextSection [padding: 8px 8px 4px 8px]                  │    │
│  │                                                          │    │
│  │  请你们看雪                                               │    │
│  │  [14sp, #333333, 最多2行, 行高1.4, 省略号]                │    │
│  │                                                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
│  ┌─────────────────────────────────────────────────────────┐    │
│  │  InfoSection [flex-row, space-between] [padding: 4px 8px 8px] │
│  │                                                          │    │
│  │  ┌─────────────────────┐        ┌─────────────────────┐ │    │
│  │  │  AuthorInfo         │        │  LikeButton         │ │    │
│  │  │  [flex-row, center] │        │  [flex-row, center] │ │    │
│  │  │                     │        │                     │ │    │
│  │  │  ┌────┐  用户名称   │        │    ♡/♥  88         │ │    │
│  │  │  │ 👤 │  [12sp]     │        │    [12sp]           │ │    │
│  │  │  └────┘             │        │                     │ │    │
│  │  │  [24x24] [gap:6px]  │        │    [gap:4px]        │ │    │
│  │  └─────────────────────┘        └─────────────────────┘ │    │
│  │        [ML]                              [MR]           │    │
│  │                                                          │    │
│  └─────────────────────────────────────────────────────────┘    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### 瀑布流容器布局

```
┌─────────────────────────────────────────────────────────────────┐
│  WaterfallContainer                                              │
│  布局: MasonryList / FlatList with columns                       │
│  padding: 8px | gap: 8px | columns: 2                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  padding:8                                                       │
│  ↓                                                               │
│  ┌────────────────────┐ gap:8 ┌────────────────────┐            │
│  │                    │  ←→   │                    │            │
│  │    ContentCard     │       │    ContentCard     │            │
│  │    [height:auto]   │       │    [height:auto]   │            │
│  │                    │       │                    │            │
│  └────────────────────┘       │                    │            │
│         gap:8                 │                    │            │
│          ↕                    └────────────────────┘            │
│  ┌────────────────────┐       ┌────────────────────┐            │
│  │                    │       │                    │            │
│  │    ContentCard     │       │    ContentCard     │            │
│  │    [height:auto]   │       │    [height:auto]   │            │
│  │                    │       │                    │            │
│  │                    │       └────────────────────┘            │
│  │                    │       ┌────────────────────┐            │
│  └────────────────────┘       │                    │            │
│  ┌────────────────────┐       │    ContentCard     │            │
│  │    ContentCard     │       │                    │            │
│  │    ...             │       └────────────────────┘            │
│  └────────────────────┘                                         │
│                                                                  │
│  特点: 两列瀑布流，卡片高度根据图片aspectRatio动态计算            │
│  列宽: (screenWidth - padding*2 - gap) / 2                       │
│       = (375 - 16 - 8) / 2 ≈ 175.5px                             │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

### TabBarArea 详细布局

```
┌─────────────────────────────────────────────────────────────────┐
│  TabBarArea [L2]                                                 │
│  布局: flex-row, 均分 | height: 83px (49+34安全区)               │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌─────────────┐│
│  │             │ │             │ │             │ │             ││
│  │     🏠      │ │     🔮      │ │     💬      │ │     👤      ││
│  │             │ │ [紫色渐变]  │ │             │ │             ││
│  │   首页      │ │   发现      │ │   消息      │ │   我的      ││
│  │  [#999]     │ │ [#7C3AED]   │ │  [#999]     │ │  [#999]     ││
│  │             │ │             │ │             │ │             ││
│  └─────────────┘ └─────────────┘ └─────────────┘ └─────────────┘│
│      flex:1          flex:1          flex:1          flex:1     │
│                                                                  │
│  TabBarItem样式：                                                │
│  ├── 图标: 24x24                                                 │
│  ├── 文字: 10sp                                                  │
│  ├── 间距: icon与text间距4px                                     │
│  ├── 默认: icon灰色, text #999999                                │
│  └── 选中: icon紫色渐变, text #7C3AED                            │
│                                                                  │
│  ──────────────────────────────────────────────────────────────  │
│  │                     SafeArea: 34px                        │  │
│  ──────────────────────────────────────────────────────────────  │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📁 STEP 5: 代码结构

### Modules 模块结构（含布局模板）

```
src/modules/
│
├─ Discover/                              # 发现模块
│   │
│   ├─ api/                               # 模块级API（对应 STEP 6 的接口定义）
│   │   ├─ apiGetDiscoverConfig.ts        # 获取Tab配置
│   │   ├─ apiGetDiscoverList.ts          # 获取内容列表
│   │   └─ apiPostLikeAction.ts           # 点赞/取消点赞
│   │
│   ├─ stores/                            # 模块级状态
│   │   └─ useDiscoverStore.ts            # 发现页状态管理
│   │
│   ├─ hooks/                             # 模块级Hooks
│   │   └─ useDiscoverData.ts             # 发现页数据Hook
│   │
│   │   ┌─────────────────────────────────────────────────────────────┐
│   │   │  📱 页面 [L1] - DiscoverPage/index.tsx                       │
│   │   ├─────────────────────────────────────────────────────────────┤
│   │   │  ┌─────────────────────────────────────────────────────┐   │
│   │   │  │  TabNavArea [L2]                                     │   │ 固定
│   │   │  └─────────────────────────────────────────────────────┘   │
│   │   │  ┌─────────────────────────────────────────────────────┐   │
│   │   │  │  ContentListArea [L2]                               │   │
│   │   │  │    └── ContentCard [L3] x N                         │   │ flex:1
│   │   │  └─────────────────────────────────────────────────────┘   │ 可滚动
│   │   │  ┌─────────────────────────────────────────────────────┐   │
│   │   │  │  TabBarArea [L2] ← 复用 Common/TabBar               │   │ 固定
│   │   │  └─────────────────────────────────────────────────────┘   │
│   │   │  布局: flex-col | 组合 L2 区域                              │
│   │   └─────────────────────────────────────────────────────────────┘
│   │
│   ├─ DiscoverPage/                      # 发现页文件夹
│   │   │
│   │   ├─ index.tsx                      # 页面主文件 [L1]
│   │   ├─ types.ts                       # 页面类型定义
│   │   │
│   │   │   ┌─────────────────────────────────────────────────────────┐
│   │   │   │  🟢 简单区域 [L2] - TabNavArea.tsx                       │
│   │   │   ├─────────────────────────────────────────────────────────┤
│   │   │   │     ┌──────┐  ┌──────┐  ┌──────┐           ┌────┐     │
│   │   │   │     │ 关注 │  │ 热门 │  │ 同城 │           │ 📷 │     │
│   │   │   │     │ ━━━  │  │      │  │      │           │    │     │
│   │   │   │     └──────┘  └──────┘  └──────┘           └────┘     │
│   │   │   │     [选中]    [默认]    [默认]              [TR]       │
│   │   │   │  布局: flex-row, center, gap-32px | 高度: 44px          │
│   │   │   └─────────────────────────────────────────────────────────┘
│   │   │
│   │   ├─ TabNavArea.tsx                 # 🟢 简单区域（单文件）
│   │   │
│   │   │   ┌─────────────────────────────────────────────────────────┐
│   │   │   │  🔵 复杂区域 [L2] - ContentListArea/                     │
│   │   │   ├─────────────────────────────────────────────────────────┤
│   │   │   │  ┌──────────────┐  ┌──────────────┐                    │
│   │   │   │  │  ┌────────┐  │  │  ┌────────┐  │                    │
│   │   │   │  │  │  媒体   │▶│  │  │  媒体   │  │                    │
│   │   │   │  │  └────────┘  │  │  └────────┘  │                    │
│   │   │   │  │  请你们看雪   │  │  请你们看雪   │  ... x N           │
│   │   │   │  │  👤 昵称 ♡88 │  │  👤 昵称 ♥88 │                    │
│   │   │   │  └──────────────┘  └──────────────┘                    │
│   │   │   │       [L3]              [L3]                           │
│   │   │   │  布局: MasonryList, cols-2, gap-8px | 滚动: scroll-y    │
│   │   │   └─────────────────────────────────────────────────────────┘
│   │   │
│   │   └─ ContentListArea/               # 🔵 复杂区域（文件夹）
│   │       │
│   │       ├─ index.tsx                  # 区域主文件
│   │       ├─ types.ts                   # 类型定义
│   │       ├─ constants.ts               # 常量（列数、间距等）
│   │       ├─ useContentListArea.ts      # 状态管理Hook
│   │       │
│   │       │   ┌─────────────────────────────────────────────────────┐
│   │       │   │  🧩 子组件 [L3] - ContentCard/                       │
│   │       │   ├─────────────────────────────────────────────────────┤
│   │       │   │  ┌─────────────────────────────────────────────┐   │
│   │       │   │  │  ┌─────────────────────────────────────┐   │   │
│   │       │   │  │  │           CoverImage                │ ▶ │   │
│   │       │   │  │  │           [条件:video显示PlayIcon]   │   │   │
│   │       │   │  │  └─────────────────────────────────────┘   │   │
│   │       │   │  │  ┌─────────────────────────────────────┐   │   │
│   │       │   │  │  │           Title (最多2行)            │   │   │
│   │       │   │  │  └─────────────────────────────────────┘   │   │
│   │       │   │  │  ┌─────────────────────────────────────┐   │   │
│   │       │   │  │  │  Avatar+Nickname    LikeIcon+Count  │   │   │
│   │       │   │  │  └─────────────────────────────────────┘   │   │
│   │       │   │  └─────────────────────────────────────────────┘   │
│   │       │   │  布局: flex-col | 复用: 1模板 x N实例                │
│   │       │   └─────────────────────────────────────────────────────┘
│   │       │
│   │       └─ ContentCard/               # 子组件文件夹 [L3]
│   │           ├─ index.tsx              # 子组件主文件
│   │           ├─ types.ts               # 类型定义
│   │           └─ onLike.ts              # 点赞事件处理
│   │
│   ├─ SharedComponents/                  # 模块共享组件 [L4]
│   │   └─ (暂无模块内共享组件)
│   │
│   └─ index.ts                           # 模块入口
│
└─ Common/                                # 公共模块
    ├─ components/                        # 全局共享组件
    │   │
    │   │   ┌─────────────────────────────────────────────────────────┐
    │   │   │  🔄 TabBar/ [L4]                                         │
    │   │   ├─────────────────────────────────────────────────────────┤
    │   │   │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │
    │   │   │  │   🏠    │ │   🔮    │ │   💬    │ │   👤    │      │
    │   │   │  │  首页   │ │  发现   │ │  消息   │ │  我的   │      │
    │   │   │  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │
    │   │   │  布局: flex-row, 均分 | 高度: 83px(含安全区)             │
    │   │   └─────────────────────────────────────────────────────────┘
    │   │
    │   ├─ TabBar/
    │   │   ├─ index.tsx
    │   │   ├─ types.ts
    │   │   └─ TabBarItem.tsx
    │   │
    │   ├─ Avatar/                        # 头像组件
    │   ├─ Loading/                       # 加载组件
    │   └─ Empty/                         # 空状态组件
    │
    └─ api/
        └─ apiGetTabbar.ts                # 获取底部导航配置
```

### 文件命名规范

| 类型 | 命名格式 | 示例 |
|------|----------|------|
| 页面主文件 | index.tsx | DiscoverPage/index.tsx |
| 类型定义 | types.ts | types.ts |
| 常量 | constants.ts | constants.ts |
| 状态管理 | use{Name}.ts | useContentListArea.ts |
| 事件处理 | on{Action}.ts | onLike.ts |
| API接口 | api{Method}{Name}.ts | apiGetDiscoverList.ts |

---

## 🗄️ STEP 6: 接口实现与后端设计

### 接口定义

```json
{
  "接口文档信息": {
    "对应页面": "DiscoverPage",
    "对应数据模型": "STEP 1 → PageModel",
    "版本": "v1.0"
  },

  "接口列表": [
    {
      "序号": 1,
      "接口名": "获取发现页列表",
      "接口标识": "discoverList",
      "路径": "GET /api/discover/list",
      "触发时机": "[API:discoverList] 页面初始化/下拉刷新/上拉加载/Tab切换",
      "对应数据模型": "STEP 1 → PageModel.ListData",

      "请求参数": {
        "tab": "String(follow|hot|nearby) - Tab标识 - 来自 STEP 1 → TabItem.key",
        "page": "Number - 页码 - 默认1",
        "pageSize": "Number - 每页数量 - 默认20"
      },

      "响应数据": {
        "code": "Number - 状态码 - 200成功",
        "message": "String - 提示信息",
        "data": {
          "_映射": "→ STEP 1: PageModel.ListData",
          "list": "Array<ContentItem> - 内容列表",
          "hasMore": "Boolean - 是否有更多",
          "total": "Number - 总数"
        }
      },

      "响应示例": {
        "code": 200,
        "message": "success",
        "data": {
          "list": [
            {
              "id": "content_123",
              "type": "video",
              "mediaData": {
                "coverUrl": "https://cdn.example.com/cover/123.jpg",
                "aspectRatio": 0.75,
                "isVideo": true
              },
              "textData": {
                "title": "请你们看雪"
              },
              "authorData": {
                "userId": "user_456",
                "avatar": "https://cdn.example.com/avatar/456.jpg",
                "nickname": "用户名称"
              },
              "statsData": {
                "likeCount": 88,
                "isLiked": false
              },
              "metaData": {
                "createTime": "2025-12-01T10:30:00Z"
              }
            }
          ],
          "hasMore": true,
          "total": 1000
        }
      },

      "数据库映射": {
        "_说明": "此接口数据来源",
        "list[].id": "contents.id",
        "list[].type": "contents.type",
        "list[].mediaData": "contents.cover_url, contents.aspect_ratio",
        "list[].textData": "contents.title",
        "list[].authorData": "JOIN users ON contents.user_id = users.id",
        "list[].statsData.likeCount": "contents.like_count",
        "list[].statsData.isLiked": "LEFT JOIN content_likes 判断当前用户"
      }
    },

    {
      "序号": 2,
      "接口名": "获取Tab配置",
      "接口标识": "discoverConfig",
      "路径": "GET /api/discover/config",
      "触发时机": "[API:discoverConfig] 页面初始化",
      "缓存策略": "本地缓存 30min",
      "对应数据模型": "STEP 1 → PageModel.TabData",

      "请求参数": {},

      "响应数据": {
        "code": "Number - 状态码",
        "data": {
          "_映射": "→ STEP 1: PageModel.TabData",
          "tabs": "Array<TabItem> - Tab列表"
        }
      },

      "响应示例": {
        "code": 200,
        "message": "success",
        "data": {
          "tabs": [
            { "key": "follow", "label": "关注" },
            { "key": "hot", "label": "热门" },
            { "key": "nearby", "label": "同城" }
          ]
        }
      }
    },

    {
      "序号": 3,
      "接口名": "点赞/取消点赞",
      "接口标识": "likeAction",
      "路径": "POST /api/discover/like",
      "触发时机": "[API:likeAction] 点击点赞按钮",
      "对应数据模型": "STEP 1 → SharedModel.ActionResult",

      "请求参数": {
        "contentId": "String - 内容ID - 来自 STEP 1 → ContentItem.id",
        "action": "String(like|unlike) - 操作类型"
      },

      "响应数据": {
        "code": "Number - 状态码",
        "data": {
          "_映射": "→ STEP 1: SharedModel.ActionResult",
          "success": "Boolean - 是否成功",
          "newLikeCount": "Number - 新的点赞数"
        }
      },

      "响应示例": {
        "code": 200,
        "message": "success",
        "data": {
          "success": true,
          "newLikeCount": 89
        }
      },

      "数据库操作": {
        "like": "INSERT INTO content_likes + UPDATE contents.like_count +1",
        "unlike": "DELETE FROM content_likes + UPDATE contents.like_count -1"
      }
    }
  ]
}
```

### 接口汇总表

| 序号 | 接口标识 | 方法 | 路径 | 对应数据模型 | 触发时机 | 缓存 |
|------|----------|------|------|--------------|----------|------|
| 1 | discoverList | GET | /api/discover/list | ListData | 初始化/刷新/加载/Tab切换 | 无 |
| 2 | discoverConfig | GET | /api/discover/config | TabData | 初始化 | 30min |
| 3 | likeAction | POST | /api/discover/like | ActionResult | 点击点赞 | 无 |

### 数据库表设计

```sql
-- ================================================================
-- 用户表（对应 STEP 1: SharedModel.UserInfo / authorData）
-- ================================================================
CREATE TABLE users (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID → authorData.userId',
    nickname        VARCHAR(50) NOT NULL COMMENT '昵称 → authorData.nickname',
    avatar          VARCHAR(500) COMMENT '头像URL → authorData.avatar',
    phone           VARCHAR(20) COMMENT '手机号',
    status          TINYINT DEFAULT 1 COMMENT '状态: 0=禁用, 1=正常',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_phone (phone),
    INDEX idx_status (status)
) COMMENT '用户表 → SharedModel.UserInfo';

-- ================================================================
-- 内容表（对应 STEP 1: ItemModel.ContentItem）
-- ================================================================
CREATE TABLE contents (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '内容ID → ContentItem.id',
    user_id         BIGINT NOT NULL COMMENT '创建者ID → authorData.userId',

    -- 媒体数据 → mediaData
    type            VARCHAR(20) DEFAULT 'image' COMMENT '类型(image/video) → type/mediaData.isVideo',
    cover_url       VARCHAR(500) NOT NULL COMMENT '封面URL → mediaData.coverUrl',
    aspect_ratio    DECIMAL(4,2) DEFAULT 1.0 COMMENT '宽高比 → mediaData.aspectRatio',
    video_url       VARCHAR(500) COMMENT '视频URL [条件:type=video]',
    duration        INT COMMENT '时长(秒) [条件:type=video]',

    -- 文本数据 → textData
    title           VARCHAR(200) NOT NULL COMMENT '标题 → textData.title',

    -- 统计数据 → statsData (冗余字段)
    like_count      INT UNSIGNED DEFAULT 0 COMMENT '点赞数 → statsData.likeCount',
    comment_count   INT UNSIGNED DEFAULT 0 COMMENT '评论数',
    view_count      INT UNSIGNED DEFAULT 0 COMMENT '浏览数',

    -- 元信息 → metaData
    location        VARCHAR(100) COMMENT '位置信息',
    latitude        DECIMAL(10,7) COMMENT '纬度 [用于:同城筛选]',
    longitude       DECIMAL(10,7) COMMENT '经度 [用于:同城筛选]',
    status          TINYINT DEFAULT 1 COMMENT '状态: 0=删除, 1=正常, 2=审核中',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '→ metaData.createTime',
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP,

    INDEX idx_user_id (user_id),
    INDEX idx_status_created (status, created_at DESC),
    INDEX idx_location (latitude, longitude),
    INDEX idx_like_count (like_count DESC)
) COMMENT '内容表 → ItemModel.ContentItem';

-- ================================================================
-- 点赞关系表（用于判断 STEP 1: statsData.isLiked）
-- ================================================================
CREATE TABLE content_likes (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    content_id      BIGINT NOT NULL COMMENT '内容ID',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,

    UNIQUE KEY uk_content_user (content_id, user_id),
    INDEX idx_user_id (user_id)
) COMMENT '点赞关系表 → 判断 statsData.isLiked';

-- ================================================================
-- 关注关系表（用于"关注"Tab筛选）
-- ================================================================
CREATE TABLE user_follows (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    user_id         BIGINT NOT NULL COMMENT '关注者ID',
    follow_user_id  BIGINT NOT NULL COMMENT '被关注者ID',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,

    UNIQUE KEY uk_follow (user_id, follow_user_id),
    INDEX idx_follow_user (follow_user_id)
) COMMENT '关注关系表 → 用于"关注"Tab筛选';

-- ================================================================
-- Tab配置表（对应 STEP 1: TabData）
-- ================================================================
CREATE TABLE discover_tabs (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT,
    tab_key         VARCHAR(50) NOT NULL COMMENT 'Tab标识 → TabItem.key',
    label           VARCHAR(50) NOT NULL COMMENT 'Tab文案 → TabItem.label',
    sort_order      INT DEFAULT 0 COMMENT '排序',
    status          TINYINT DEFAULT 1 COMMENT '状态: 0=禁用, 1=启用',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,

    UNIQUE KEY uk_tab_key (tab_key),
    INDEX idx_status_sort (status, sort_order)
) COMMENT 'Tab配置表 → PageModel.TabData';

-- 初始化Tab数据
INSERT INTO discover_tabs (tab_key, label, sort_order) VALUES
('follow', '关注', 1),
('hot', '热门', 2),
('nearby', '同城', 3);
```

### 核心SQL查询

```sql
-- ================================================================
-- 列表查询（返回 STEP 1: PageModel.ListData）
-- ================================================================

-- 热门Tab查询（按点赞数排序）
SELECT
    -- ContentItem.id
    c.id,

    -- ContentItem.type
    c.type,

    -- ContentItem.mediaData
    c.cover_url AS coverUrl,
    c.aspect_ratio AS aspectRatio,
    IF(c.type = 'video', 1, 0) AS isVideo,

    -- ContentItem.textData
    c.title,

    -- ContentItem.authorData ← JOIN users
    u.id AS authorUserId,
    u.avatar AS authorAvatar,
    u.nickname AS authorNickname,

    -- ContentItem.statsData
    c.like_count AS likeCount,
    IF(cl.id IS NOT NULL, 1, 0) AS isLiked,

    -- ContentItem.metaData
    c.created_at AS createTime

FROM contents c
INNER JOIN users u ON c.user_id = u.id
LEFT JOIN content_likes cl ON c.id = cl.content_id AND cl.user_id = #{currentUserId}
WHERE c.status = 1
ORDER BY c.like_count DESC, c.created_at DESC
LIMIT #{pageSize} OFFSET #{offset};

-- 关注Tab查询（只看关注的人的内容）
SELECT
    c.id,
    c.type,
    c.cover_url AS coverUrl,
    c.aspect_ratio AS aspectRatio,
    IF(c.type = 'video', 1, 0) AS isVideo,
    c.title,
    u.id AS authorUserId,
    u.avatar AS authorAvatar,
    u.nickname AS authorNickname,
    c.like_count AS likeCount,
    IF(cl.id IS NOT NULL, 1, 0) AS isLiked,
    c.created_at AS createTime

FROM contents c
INNER JOIN users u ON c.user_id = u.id
INNER JOIN user_follows uf ON c.user_id = uf.follow_user_id AND uf.user_id = #{currentUserId}
LEFT JOIN content_likes cl ON c.id = cl.content_id AND cl.user_id = #{currentUserId}
WHERE c.status = 1
ORDER BY c.created_at DESC
LIMIT #{pageSize} OFFSET #{offset};

-- 同城Tab查询（按距离排序）
SELECT
    c.id,
    c.type,
    c.cover_url AS coverUrl,
    c.aspect_ratio AS aspectRatio,
    IF(c.type = 'video', 1, 0) AS isVideo,
    c.title,
    u.id AS authorUserId,
    u.avatar AS authorAvatar,
    u.nickname AS authorNickname,
    c.like_count AS likeCount,
    IF(cl.id IS NOT NULL, 1, 0) AS isLiked,
    c.created_at AS createTime,
    -- 计算距离（单位：米）
    ST_Distance_Sphere(
        POINT(c.longitude, c.latitude),
        POINT(#{userLongitude}, #{userLatitude})
    ) AS distance

FROM contents c
INNER JOIN users u ON c.user_id = u.id
LEFT JOIN content_likes cl ON c.id = cl.content_id AND cl.user_id = #{currentUserId}
WHERE c.status = 1
  AND c.latitude IS NOT NULL
  AND c.longitude IS NOT NULL
  AND ST_Distance_Sphere(
        POINT(c.longitude, c.latitude),
        POINT(#{userLongitude}, #{userLatitude})
      ) <= 50000  -- 50公里范围内
ORDER BY distance ASC, c.created_at DESC
LIMIT #{pageSize} OFFSET #{offset};

-- ================================================================
-- 点赞操作（更新 statsData.isLiked 和 statsData.likeCount）
-- ================================================================

-- 点赞
INSERT INTO content_likes (content_id, user_id)
VALUES (#{contentId}, #{userId});

UPDATE contents
SET like_count = like_count + 1
WHERE id = #{contentId};

-- 取消点赞
DELETE FROM content_likes
WHERE content_id = #{contentId} AND user_id = #{userId};

UPDATE contents
SET like_count = GREATEST(like_count - 1, 0)
WHERE id = #{contentId};

-- ================================================================
-- 获取Tab配置
-- ================================================================
SELECT
    tab_key AS `key`,
    label
FROM discover_tabs
WHERE status = 1
ORDER BY sort_order ASC;
```

### MVP清单

| 序号 | 功能 | 对应数据模型 | 涉及表 | 接口 | 优先级 |
|------|------|--------------|--------|------|--------|
| 1 | 热门列表查询 | ListData | users, contents, content_likes | GET /list?tab=hot | P0 |
| 2 | 关注列表查询 | ListData | users, contents, content_likes, user_follows | GET /list?tab=follow | P0 |
| 3 | 同城列表查询 | ListData | users, contents, content_likes | GET /list?tab=nearby | P0 |
| 4 | Tab配置获取 | TabData | discover_tabs | GET /config | P0 |
| 5 | 点赞操作 | ActionResult | content_likes, contents | POST /like | P0 |
| 6 | 分页支持 | PaginationInfo | - | query params | P0 |

**MVP统计：5张表 + 3个接口 = 最小可行后端**

### 性能优化检查点

```
✅ 索引：
   □ contents.status + created_at 联合索引（列表查询）
   □ contents.like_count 索引（热门排序）
   □ contents.latitude + longitude 索引（同城查询）
   □ content_likes.content_id + user_id 唯一索引
   □ user_follows.user_id + follow_user_id 唯一索引

✅ 冗余：
   □ contents.like_count 冗余存储，避免每次COUNT

✅ 分页：
   □ 使用 LIMIT OFFSET（数据量大时需改为游标分页）

✅ N+1：
   □ 已使用 JOIN 避免循环查询

✅ 缓存：
   □ Tab配置缓存 30min
   □ 热门列表首页可缓存 5min
```

---

## 🎯 质量评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 数据模型 | 9 | PageModel、AreaModel、ItemModel、SharedModel 完整定义，字段与UI绑定清晰 |
| 结构完整性 | 9 | 所有UI元素都有层级标注（L1/L2/L3），标准Area命名 |
| 数据绑定 | 9 | 每个动态元素都有 [data:字段名] 标注，与STEP 1对应 |
| 位置精确度 | 8 | 提供了完整的ASCII布局图，标注了九宫格位置和Flex布局 |
| 组件拆分 | 9 | L1→L2→L3层级清晰合理，8个组件 |
| 复用标记 | 9 | TabBar复用Common组件，ContentCard模板复用标记完整 |
| 交互描述 | 9 | 流程图覆盖所有交互，标注[API:xxx]和组件关联 |
| 代码映射 | 9 | 文件结构可直接创建，每个文件有对应布局模板 |
| 接口+后端 | 9 | 接口对应数据模型，SQL可直接执行，MVP清单完整 |

**综合评分：9分**

---

*文档版本：v1.0*
*生成时间：2025-12-01*
*基于模板：纯结构架构图标准模板 v5.4*
