# 🌳 发现页 结构指导文档

> **页面名称：** 发现页（Discover Page）
> **所属模块：** Discover（发现模块）
> **页面类型：** 列表页（Tab切换 + 瀑布流列表）
> **文档版本：** v1.0
> **生成日期：** 2025-12-01

---

## 📋 STEP 1: 页面数据模型

### 设计理念

```
核心思想：数据驱动 UI，先定义"页面需要什么数据"，再考虑"数据从哪来"

发现页数据流：
┌─────────────────────────────────────────────────────────────────┐
│  STEP 1: 数据模型                    STEP 6: 数据实现            │
│  ─────────────────                   ─────────────────           │
│  DiscoverPageModel {            →    GET /api/discover/config    │
│    TabData                           GET /api/discover/list      │
│    ContentListData                   POST /api/discover/like     │
│  }                                   数据库表设计                 │
└─────────────────────────────────────────────────────────────────┘
```

### PageModel（页面级数据模型）

```json
{
  "模型信息": {
    "页面名称": "发现页",
    "页面标识": "DiscoverPage",
    "所属模块": "Discover",
    "页面类型": "列表页"
  },

  "数据依赖图": "TabData + ContentListData → 渲染页面 → 用户操作 → ActionResult",

  "DiscoverPageModel": {
    "_说明": "页面级数据模型 [L1]，组合所有区域数据",

    "TabData": {
      "_说明": "Tab区域数据 → [→TabNavArea]",
      "tabs": "Array<TabItem> - Tab列表 - [UI:TabNavArea]",
      "currentTab": "String(follow|hot|nearby) - 当前选中Tab - [状态:控制列表筛选]",

      "TabItem": {
        "key": "String(follow|hot|nearby) - Tab标识 - [用于:筛选参数]",
        "label": "String - Tab文案 - [UI:TabNavArea.Tab.文字]",
        "isActive": "Boolean - 是否选中 - [状态:选中样式，加粗显示]"
      }
    },

    "ContentListData": {
      "_说明": "内容列表区域数据 → [→ContentListArea]",
      "list": "Array<ContentItem> - 内容列表 - [UI:ContentListArea]",
      "hasMore": "Boolean - 是否有更多 - [控制:加载更多/到底提示]",
      "total": "Number? - 总数 - [可选]",
      "page": "Number - 当前页码 - [状态:分页控制]",
      "pageSize": "Number - 每页数量 - [默认:20]"
    },

    "PageState": {
      "_说明": "页面状态数据",
      "isLoading": "Boolean - 加载中 - [状态:骨架屏/Loading]",
      "isRefreshing": "Boolean - 刷新中 - [状态:下拉刷新指示器]",
      "isLoadingMore": "Boolean - 加载更多中 - [状态:底部Loading]",
      "isError": "Boolean - 加载失败 - [状态:错误提示]",
      "errorMessage": "String? - 错误信息 - [UI:错误提示文案]",
      "isEmpty": "Boolean - 列表为空 - [状态:空状态插画]"
    }
  }
}
```

### ItemModel（列表项数据模型）

```json
{
  "ItemModel": {
    "_说明": "列表项数据模型 [L3]",

    "ContentItem": {
      "_说明": "内容卡片数据 → [→ContentCard]",

      "id": "String - 内容ID - [用于:跳转详情、操作参数]",
      "type": "String(image|video) - 内容类型 - [条件:video时显示播放图标]",

      "mediaData": {
        "_说明": "媒体区域 → [→ContentCard.MediaArea]",
        "coverUrl": "String - 封面URL - [UI:卡片.封面图]",
        "aspectRatio": "Number - 宽高比 - [计算:高度=宽度/ratio，瀑布流核心]",
        "duration": "Number? - 时长(秒) - [UI:卡片.时长标签] [条件:type=video]",
        "width": "Number - 原图宽度 - [用于:计算展示尺寸]",
        "height": "Number - 原图高度 - [用于:计算展示尺寸]"
      },

      "textData": {
        "_说明": "文本区域 → [→ContentCard.TextArea]",
        "title": "String - 标题 - [UI:卡片.标题] [样式:14sp,最多2行,省略]"
      },

      "authorData": {
        "_说明": "作者信息 → [→ContentCard.AuthorArea]",
        "userId": "String - 用户ID - [用于:跳转用户主页]",
        "avatar": "String - 头像URL - [UI:卡片.头像] [样式:24x24圆形]",
        "nickname": "String - 昵称 - [UI:卡片.昵称] [样式:12sp,#666666]"
      },

      "statsData": {
        "_说明": "统计数据 → [→ContentCard.StatsArea]",
        "likeCount": "Number - 点赞数 - [UI:卡片.点赞数] [格式:>=10000显示1.2w]",
        "isLiked": "Boolean - 是否已赞 - [状态:点赞按钮] [true=红心实心,false=空心]"
      },

      "metaData": {
        "_说明": "元信息",
        "createTime": "String(ISO8601) - 创建时间 - [格式:刚刚/X分钟前/MM-DD]",
        "location": "String? - 位置 - [用于:同城筛选] [可选]"
      }
    }
  }
}
```

### SharedModel（共享数据模型）

```json
{
  "SharedModel": {
    "_说明": "跨页面复用的数据模型 [L4]",

    "UserInfo": {
      "_说明": "用户基础信息（多处复用）",
      "userId": "String - 用户ID",
      "nickname": "String - 昵称",
      "avatar": "String - 头像URL"
    },

    "PaginationInfo": {
      "_说明": "分页信息",
      "page": "Number - 当前页码",
      "pageSize": "Number - 每页数量",
      "hasMore": "Boolean - 是否有更多",
      "total": "Number? - 总数"
    },

    "ActionResult": {
      "_说明": "操作结果",
      "success": "Boolean - 是否成功",
      "message": "String? - 提示信息",
      "data": {
        "isLiked": "Boolean? - 新的点赞状态",
        "likeCount": "Number? - 新的点赞数"
      }
    },

    "TabBarData": {
      "_说明": "底部导航数据（全局共享）",
      "tabs": "Array<TabBarItem> - 导航项列表",
      "currentTab": "String - 当前选中项",

      "TabBarItem": {
        "key": "String(home|discover|message|profile) - 标识",
        "label": "String - 文案",
        "icon": "String - 默认图标",
        "activeIcon": "String - 选中图标",
        "badge": "Number? - 角标数 [条件:>0时显示]"
      }
    }
  }
}
```

### 业务逻辑标注速查表

```
┌─────────────────────────────────────────────────────────────────────────────┐
│  标注类型        语法格式                    发现页场景                        │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  条件显示        [条件:type=video]           type=video → 显示播放图标        │
│                  [条件:isLiked=true]         isLiked=true → 显示红心实心      │
│                  [条件:hasMore=false]        hasMore=false → 显示到底提示     │
│                                                                             │
│  状态切换        [状态:isLiked]              true→红心实心, false→空心        │
│                  [状态:isActive]             true→加粗黑色, false→常规灰色    │
│                                                                             │
│  格式化          [格式:数字]                 likeCount: <10000原样,           │
│                                             >=10000显示X.Xw                  │
│                  [格式:时间]                 createTime: <1分钟'刚刚',        │
│                                             <1小时'X分钟前', 其他'MM-DD'      │
│                                                                             │
│  计算字段        [计算:瀑布流高度]           cardHeight = cardWidth / ratio   │
│                                             + titleHeight + authorHeight     │
│                                                                             │
└─────────────────────────────────────────────────────────────────────────────┘
```

### 数据模型汇总表

| 模型名 | 层级 | 对应组件 | 说明 |
|--------|------|----------|------|
| DiscoverPageModel | L1 | DiscoverPage | 页面级数据，组合所有区域 |
| TabData | L2 | TabNavArea | Tab导航数据 |
| TabItem | L3 | TabItem | 单个Tab项数据 |
| ContentListData | L2 | ContentListArea | 内容列表数据 |
| ContentItem | L3 | ContentCard | 单个内容卡片数据 |
| PageState | - | - | 页面状态控制 |
| UserInfo | L4 | - | 共享用户信息 |
| ActionResult | L4 | - | 操作结果 |
| TabBarData | L4 | TabBarArea | 底部导航数据 |

---

## 🔄 STEP 2: 流程图

### 页面完整流程

```
开始
 ↓
【进入发现页】
 ├─→ 路由跳转: /discover
 ├─→ Tab切换: 从首页/消息/我的切换过来
 └─→ 深链接: xypai://discover?tab=hot
 ↓
【页面初始化】
 │
 ├─→ 读取本地缓存 Tab 状态
 │    └─→ 恢复上次选中的 Tab（默认: follow）
 │
 ├─→ 调用 [API:discoverConfig] → 获取Tab配置（可选，配置可硬编码）
 │    └─→ 渲染 [→TabNavArea] Tab列表
 │
 ├─→ 调用 [API:discoverList] (tab=currentTab, page=1) → 获取首屏数据
 │    ├─→ 显示骨架屏 [状态:ContentListArea.loading]
 │    └─→ 渲染 [→ContentListArea] 瀑布流列表
 │
 └─→ 初始化监听
      ├─→ 滚动监听 → [触发:ContentListArea] 加载更多
      └─→ 下拉刷新监听 → [触发:ContentListArea] 刷新
 ↓
用户操作
 │
 ├─→ 【TabNavArea Tab切换】
 │    └─→ 点击 Tab [触发:TabNavArea.TabItem]
 │         ├─→ 更新选中态 [状态:TabNavArea] currentTab
 │         ├─→ 清空当前列表 [状态:ContentListArea]
 │         ├─→ 显示骨架屏 [状态:ContentListArea.loading]
 │         ├─→ 调用 [API:discoverList] (tab=newTab, page=1)
 │         └─→ 替换内容 [状态:ContentListArea]
 │
 ├─→ 【ContentListArea 列表交互】
 │    │
 │    ├─→ 下拉刷新 [触发:ContentListArea]
 │    │    ├─→ 显示刷新指示器 [状态:isRefreshing=true]
 │    │    ├─→ 调用 [API:discoverList] (tab=currentTab, page=1)
 │    │    ├─→ 清空重置列表 [状态:ContentListArea]
 │    │    └─→ 隐藏刷新指示器 [状态:isRefreshing=false]
 │    │
 │    ├─→ 上拉加载 [触发:ContentListArea] [条件:距底<100px且hasMore=true]
 │    │    ├─→ 显示底部Loading [状态:isLoadingMore=true]
 │    │    ├─→ 调用 [API:discoverList] (tab=currentTab, page+1)
 │    │    ├─→ 追加数据到列表 [状态:ContentListArea]
 │    │    └─→ 隐藏底部Loading [状态:isLoadingMore=false]
 │    │
 │    └─→ 滚动浏览 → 图片懒加载（进入视口时加载）
 │
 ├─→ 【ContentCard 单项操作】
 │    │
 │    ├─→ 点击卡片封面/标题 [触发:ContentCard]
 │    │    └─→ 跳转 → 【内容详情页】(携带 contentId)
 │    │
 │    ├─→ 点击用户头像/昵称 [触发:ContentCard.AuthorArea]
 │    │    └─→ 跳转 → 【用户主页】(携带 userId)
 │    │
 │    └─→ 点击点赞按钮 [触发:ContentCard.LikeButton]
 │         ├─→ 乐观更新 [状态:ContentCard]
 │         │    ├─→ isLiked 取反
 │         │    └─→ likeCount ±1
 │         ├─→ 调用 [API:discoverLike] (contentId, action=like/unlike)
 │         ├─→ 成功 → 保持乐观更新状态
 │         └─→ 失败 → 回滚 [状态:ContentCard] + Toast提示
 │
 ├─→ 【HeaderArea 操作】
 │    └─→ 点击相机图标 [触发:HeaderArea.CameraButton]
 │         └─→ 跳转 → 【发布页】或【拍摄页】
 │
 └─→ 【TabBarArea 导航跳转】
      └─→ 点击导航项 [触发:TabBarArea.TabBarItem]
           ├─→ 点击首页 → 跳转【首页】
           ├─→ 点击发现 → 当前页（已选中）
           ├─→ 点击消息 → 跳转【消息页】
           └─→ 点击我的 → 跳转【个人中心】
 ↓
【状态管理】 [状态:ContentListArea]
 ├─→ isLoading=true && page=1 → 显示骨架屏（6个占位卡片）
 ├─→ isLoadingMore=true → 显示底部Loading指示器
 ├─→ isRefreshing=true → 显示下拉刷新指示器
 ├─→ isError=true → 显示错误提示 → 点击重试
 ├─→ list.length=0 → 显示空状态插画
 │    ├─→ follow: "还没有关注的人，去发现更多吧"
 │    ├─→ hot: "暂无热门内容"
 │    └─→ nearby: "附近暂无内容，换个位置试试"
 └─→ hasMore=false && list.length>0 → 显示"已经到底啦~"
 ↓
结束
```

### 流程-组件关联表

| 流程节点 | 触发组件 | 调用接口 | 影响组件 | 状态变化 |
|----------|----------|----------|----------|----------|
| 页面初始化 | - | discoverList | TabNavArea, ContentListArea | loading→loaded |
| Tab切换 | TabNavArea.TabItem | discoverList(tab=X) | TabNavArea, ContentListArea | 切换Tab+内容替换 |
| 下拉刷新 | ContentListArea | discoverList(page=1) | ContentListArea | refreshing→loaded |
| 上拉加载 | ContentListArea | discoverList(page+1) | ContentListArea | loadingMore→loaded |
| 点击卡片 | ContentCard | - | - | 跳转详情页 |
| 点击头像 | ContentCard.AuthorArea | - | - | 跳转用户主页 |
| 点击点赞 | ContentCard.LikeButton | discoverLike | ContentCard | isLiked切换 |
| 点击相机 | HeaderArea.CameraButton | - | - | 跳转发布页 |
| 底部导航 | TabBarArea | - | - | 页面跳转 |

---

## 🌳 STEP 3: 树状图

```
【Discover 发现模块】★★★
│
├── 【DiscoverPage 发现页】📱 [L1] [拆分:4个L2]
│   │   文件: Discover/DiscoverPage/index.tsx
│   │
│   ├── 📍 HeaderArea [L2] [固定顶部] [高度:44px+状态栏]
│   │   │   文件: DiscoverPage/HeaderArea.tsx
│   │   │   布局: flex-row, space-between, center
│   │   │
│   │   ├── 🔲 StatusBar [系统状态栏]
│   │   │   └── [系统提供: 时间、信号、WiFi、电量]
│   │   │
│   │   └── 🔲 NavBar [L3] [高度:44px]
│   │       ├── 📍 LeftPlaceholder [位置:TL] [占位，保持居中]
│   │       │
│   │       ├── 🏷️ TabNavArea [位置:TC] [→见下方独立组件]
│   │       │
│   │       └── 「CameraButton」[位置:TR] [尺寸:24x24]
│   │           └── [图标:相机] → 点击跳转发布页
│   │
│   ├── 🏷️ TabNavArea [L2] [内嵌HeaderArea]
│   │   │   文件: DiscoverPage/TabNavArea.tsx
│   │   │   布局: flex-row, center, gap-24px
│   │   │   数据: [data:TabData.tabs]
│   │   │
│   │   └── 《TabItem》[L3] [1模板x3实例]
│   │       │   数据: [data:TabItem]
│   │       │
│   │       ├── 📝 TabLabel [文字]
│   │       │   └── [data:TabItem.label] "关注" / "热门" / "同城"
│   │       │   └── [样式:选中=16sp黑色加粗, 未选中=16sp#999999]
│   │       │   └── [状态:TabItem.isActive] 控制样式切换
│   │       │
│   │       └── 🔲 Indicator [下划线指示器]
│   │           └── [条件:isActive=true时显示]
│   │           └── [样式:宽度=文字宽度, 高度2px, 圆角, 主题色]
│   │
│   ├── 🏷️ ContentListArea [L2] [flex:1, 可滚动]
│   │   │   文件: DiscoverPage/ContentListArea/
│   │   │   ├── index.tsx
│   │   │   ├── types.ts
│   │   │   ├── useContentList.ts
│   │   │   └── ContentCard/
│   │   │
│   │   │   布局: 瀑布流 grid-cols-2, gap-8px, padding-12px
│   │   │   数据: [data:ContentListData.list]
│   │   │
│   │   ├── 🔄 RefreshControl [下拉刷新]
│   │   │   └── [状态:isRefreshing] 控制显示
│   │   │
│   │   ├── 📋 WaterfallList [瀑布流容器]
│   │   │   │   布局: MasonryList / WaterfallFlow
│   │   │   │
│   │   │   └── 《ContentCard》[L3] [1模板xN实例]
│   │   │       │   文件: ContentListArea/ContentCard/index.tsx
│   │   │       │   数据: [data:ContentItem]
│   │   │       │   布局: flex-col, 圆角12px, 白色背景
│   │   │       │
│   │   │       ├── 🖼️ MediaArea [媒体区域]
│   │   │       │   │   布局: relative, 圆角12px(上)
│   │   │       │   │   数据: [data:ContentItem.mediaData]
│   │   │       │   │
│   │   │       │   ├── 🖼️ CoverImage [封面图]
│   │   │       │   │   └── [data:mediaData.coverUrl]
│   │   │       │   │   └── [计算:高度=宽度*height/width]
│   │   │       │   │   └── [样式:圆角12px(上), resizeMode=cover]
│   │   │       │   │
│   │   │       │   └── 🔲 PlayIcon [播放图标] [位置:MC]
│   │   │       │       └── [条件:type=video时显示]
│   │   │       │       └── [样式:40x40, 半透明白色圆形背景, 播放三角]
│   │   │       │
│   │   │       ├── 📝 TextArea [文本区域]
│   │   │       │   │   布局: padding-8px
│   │   │       │   │   数据: [data:ContentItem.textData]
│   │   │       │   │
│   │   │       │   └── 📝 Title [标题]
│   │   │       │       └── [data:textData.title]
│   │   │       │       └── [样式:14sp, #333333, 最多2行, 省略号]
│   │   │       │
│   │   │       └── 👤 AuthorArea [作者+互动区域]
│   │   │           │   布局: flex-row, space-between, center
│   │   │           │   布局: padding-horizontal-8px, padding-bottom-8px
│   │   │           │
│   │   │           ├── 👤 AuthorInfo [作者信息] [位置:ML]
│   │   │           │   │   布局: flex-row, center, gap-4px
│   │   │           │   │   数据: [data:ContentItem.authorData]
│   │   │           │   │
│   │   │           │   ├── 🖼️ Avatar [头像]
│   │   │           │   │   └── [data:authorData.avatar]
│   │   │           │   │   └── [样式:20x20圆形]
│   │   │           │   │
│   │   │           │   └── 📝 Nickname [昵称]
│   │   │           │       └── [data:authorData.nickname]
│   │   │           │       └── [样式:12sp, #666666, 最多1行]
│   │   │           │
│   │   │           └── ❤️ LikeButton [点赞按钮] [位置:MR]
│   │   │               │   布局: flex-row, center, gap-2px
│   │   │               │   数据: [data:ContentItem.statsData]
│   │   │               │
│   │   │               ├── ❤️ LikeIcon [点赞图标]
│   │   │               │   └── [状态:isLiked] true=红心实心, false=空心
│   │   │               │   └── [样式:16x16]
│   │   │               │
│   │   │               └── 📝 LikeCount [点赞数]
│   │   │                   └── [data:statsData.likeCount]
│   │   │                   └── [格式:>=10000显示X.Xw]
│   │   │                   └── [样式:12sp, #666666]
│   │   │
│   │   ├── 🔄 LoadMoreIndicator [加载更多指示器]
│   │   │   └── [条件:isLoadingMore=true] 显示Loading
│   │   │
│   │   ├── 📝 EndMessage [到底提示]
│   │   │   └── [条件:hasMore=false且list.length>0]
│   │   │   └── [文案:"已经到底啦~"]
│   │   │
│   │   └── 🖼️ EmptyState [空状态]
│   │       └── [条件:list.length=0且isLoading=false]
│   │       └── [插画+文案，根据Tab显示不同文案]
│   │
│   └── 📍 TabBarArea [L2] [固定底部] [高度:50px+安全区]
│       │   文件: Common/components/TabBar/index.tsx (全局共享)
│       │   布局: flex-row, 均分, 白色背景, 顶部边框
│       │   数据: [data:TabBarData]
│       │
│       └── 《TabBarItem》[L3] [1模板x4实例]
│           │   数据: [data:TabBarItem]
│           │   布局: flex-col, center, gap-2px
│           │
│           ├── 🖼️ Icon [图标]
│           │   └── [状态:isActive] 切换 icon/activeIcon
│           │   └── [样式:24x24]
│           │   └── 发现Tab: 选中时为粉紫渐变特殊图标
│           │
│           ├── 📝 Label [文案]
│           │   └── [data:TabBarItem.label]
│           │   └── "首页" / "发现" / "消息" / "我的"
│           │   └── [样式:选中=主题色, 未选中=#999999, 10sp]
│           │
│           └── 🔴 Badge [角标]
│               └── [条件:badge>0时显示]
│               └── [位置:图标右上角]
│
└── 🔧 服务层（对应 STEP 6 的接口）
    ├── 📡 GET /api/discover/list      → 列表服务 [→ ContentListData]
    ├── 📡 POST /api/discover/like     → 点赞服务 [→ ActionResult]
    └── 📡 GET /api/common/tabbar      → TabBar配置 [→ TabBarData]
```

### 列表控制（瀑布流特有）

```
🔄 WaterfallListControl [适用于 ContentListArea]
├── 瀑布流布局: 2列, 根据图片宽高比动态计算卡片高度
├── 加载更多: 上滑 [距底100px] → 调用列表接口(page+1)
├── 下拉刷新: [阈值60px] → 调用列表接口(page=1)
├── 骨架屏: [条件:isLoading且page=1] 显示6个占位卡片
├── 加载指示: [条件:isLoadingMore=true] 显示底部loading
├── 空状态: [条件:list.length=0] 显示空状态插画
├── 到底提示: [条件:hasMore=false] "已经到底啦~"
└── 图片懒加载: 进入视口时加载图片
```

### 组件拆分规则

```
发现页组件统计：
├── L1-页面: 1个 (DiscoverPage)
├── L2-区域: 4个 (HeaderArea, TabNavArea, ContentListArea, TabBarArea)
├── L3-基础: 6个 (TabItem, ContentCard, TabBarItem, MediaArea, AuthorArea, LikeButton)
└── 总计: 11个组件

复用标记：
├── TabBarArea ← 复用 Common/components/TabBar（全局共享）
├── ContentCard [1模板xN实例] 数据驱动
├── TabItem [1模板x3实例] 固定3个Tab
└── TabBarItem [1模板x4实例] 固定4个导航项
```

---

## 📍 STEP 4: 布局定位

### 页面整体布局

```
┌─────────────────────────────────────────────────────────────────┐
│                        StatusBar [系统]                          │ 固定
│                        高度: 动态(刘海屏约44px)                    │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────┐      ┌─────────────────────┐      ┌─────┐             │
│  │     │      │   关注  热门  同城    │      │ 📷  │             │ 固定
│  │     │      │         ━━━         │      │     │             │ 44px
│  └─────┘      └─────────────────────┘      └─────┘             │
│   [TL]              [TC] TabNavArea          [TR]               │
│                     CameraButton                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │                  │  │                  │                    │
│  │    CoverImage    │  │    CoverImage    │                    │
│  │    (动态高度)     │  │    (动态高度)     │                    │
│  │       ▶️         │  │                  │                    │
│  │                  │  ├──────────────────┤                    │
│  ├──────────────────┤  │  请你们看雪       │                    │
│  │  请你们看雪       │  ├──────────────────┤                    │ flex:1
│  ├──────────────────┤  │  👤昵称    ❤️88  │                    │ 可滚动
│  │  👤昵称    ❤️88  │  └──────────────────┘                    │
│  └──────────────────┘                                          │
│  ┌──────────────────┐  ┌──────────────────┐                    │
│  │                  │  │                  │                    │
│  │    CoverImage    │  │    CoverImage    │                    │
│  │                  │  │                  │                    │
│  .                  .  .                  .                    │
│  .    (瀑布流继续)   .  .    (瀑布流继续)   .                    │
│                                                                 │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐              │
│  │   🏠    │ │   🔮    │ │   💬    │ │   👤    │              │ 固定
│  │  首页   │ │  发现   │ │  消息   │ │  我的   │              │ 50px
│  └─────────┘ └─────────┘ └─────────┘ └─────────┘              │ +安全区
│              [选中态]                                           │
└─────────────────────────────────────────────────────────────────┘

页面布局规则:
├── 整体: flex-col
├── HeaderArea: 固定顶部, 高度=状态栏+44px
├── ContentListArea: flex:1, 填充剩余空间, scroll-y
└── TabBarArea: 固定底部, 高度=50px+底部安全区
```

### HeaderArea 布局详图

```
┌─────────────────────────────────────────────────────────────────┐
│                         HeaderArea                               │
│                     高度: 44px (不含状态栏)                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌────────┐                                      ┌────────┐    │
│  │        │     ┌────┐  ┌────┐  ┌────┐         │   📷   │    │
│  │  占位   │     │关注│  │热门│  │同城│         │  24x24 │    │
│  │  44x44 │     │ ━━ │  │    │  │    │         │        │    │
│  └────────┘     └────┘  └────┘  └────┘         └────────┘    │
│     [TL]              [TC]                         [TR]        │
│                    gap-24px                      padding-16px   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

布局规则:
├── 容器: flex-row, space-between, align-center
├── 左侧占位: width=44px (保持Tab居中)
├── 中间TabNav: flex-row, gap-24px, align-center
└── 右侧相机: width=44px, padding-right=16px
```

### TabNavArea 布局详图

```
┌─────────────────────────────────────────────────────────────────┐
│                         TabNavArea                               │
│                      布局: flex-row, center                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│           ┌──────────┐   ┌──────────┐   ┌──────────┐          │
│           │   关注    │   │   热门    │   │   同城    │          │
│           │   ━━━    │   │          │   │          │          │
│           │ [选中态]  │   │ [默认态]  │   │ [默认态]  │          │
│           └──────────┘   └──────────┘   └──────────┘          │
│              16sp           16sp           16sp                │
│             #000000        #999999        #999999               │
│              加粗           常规            常规                  │
│                                                                 │
│           ←──────────── gap-24px ────────────→                 │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

TabItem 布局:
├── 容器: flex-col, center
├── 文字: 16sp
│   ├── 选中: #000000, font-weight-600
│   └── 未选中: #999999, font-weight-400
└── 下划线: [条件显示]
    ├── 宽度: 与文字等宽或固定20px
    ├── 高度: 2px
    ├── 圆角: 1px
    └── 颜色: 主题色
```

### ContentCard 布局详图

```
┌─────────────────────────────────────────────────────────────────┐
│                         ContentCard                              │
│                 宽度: (屏幕宽-padding*2-gap)/2                     │
│                 约 (375-24-8)/2 ≈ 171.5px                        │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │                      MediaArea                          │   │
│  │                   (高度=宽度*ratio)                       │   │
│  │                                                         │   │
│  │                        ┌───────┐                        │   │
│  │                        │  ▶️   │  ← [条件:type=video]   │   │
│  │                        │ 40x40 │                        │   │
│  │                        └───────┘                        │   │
│  │                          [MC]                           │   │
│  │                                                         │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │  请你们看雪                                              │   │
│  │  [14sp, #333, 2行省略]                       TextArea    │   │
│  │                                            padding-8px   │   │
│  └─────────────────────────────────────────────────────────┘   │
│  ┌─────────────────────────────────────────────────────────┐   │
│  │                                                         │   │
│  │  ┌──────────────────┐              ┌──────────────┐    │   │
│  │  │ 👤  用户名称      │              │  ❤️  88      │    │   │
│  │  │ 20x20  12sp      │              │  16x16 12sp  │    │   │
│  │  └──────────────────┘              └──────────────┘    │   │
│  │      [ML] AuthorInfo                [MR] LikeButton    │   │
│  │                                                         │   │
│  │                      AuthorArea                         │   │
│  │           padding: 0 8px 8px 8px                        │   │
│  └─────────────────────────────────────────────────────────┘   │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

ContentCard 布局规则:
├── 容器: flex-col, 白色背景, 圆角12px
├── MediaArea:
│   ├── 宽度: 100%
│   ├── 高度: 动态(原图高度/原图宽度*卡片宽度)
│   ├── 圆角: 12px 12px 0 0 (仅上方)
│   └── PlayIcon: absolute, center
├── TextArea:
│   ├── padding: 8px
│   └── Title: 14sp, #333, max-lines=2
└── AuthorArea:
    ├── flex-row, space-between, align-center
    ├── padding: 0 8px 8px 8px
    ├── AuthorInfo: flex-row, gap-4px
    │   ├── Avatar: 20x20, 圆形
    │   └── Nickname: 12sp, #666
    └── LikeButton: flex-row, gap-2px
        ├── Icon: 16x16, [状态切换]
        └── Count: 12sp, #666
```

### 瀑布流布局详图

```
┌─────────────────────────────────────────────────────────────────┐
│                     ContentListArea 瀑布流                       │
│                    padding: 12px                                │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│     Column 0                    Column 1                        │
│     ┌─────────────┐            ┌─────────────┐                 │
│     │             │            │             │                 │
│     │   Card 0    │            │   Card 1    │                 │
│     │   高度:H0   │            │   高度:H1   │                 │
│     │             │            │             │                 │
│     └─────────────┘            │             │                 │
│           ↓ gap-8px            │             │                 │
│     ┌─────────────┐            └─────────────┘                 │
│     │             │                  ↓ gap-8px                  │
│     │   Card 2    │            ┌─────────────┐                 │
│     │   高度:H2   │            │             │                 │
│     │             │            │   Card 3    │                 │
│     │             │            │   高度:H3   │                 │
│     │             │            │             │                 │
│     └─────────────┘            └─────────────┘                 │
│           ↓                          ↓                          │
│     ...继续...                  ...继续...                       │
│                                                                 │
│     ←─ gap-8px ─→                                               │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘

瀑布流布局规则:
├── 容器: MasonryList / RecyclerView + StaggeredGridLayoutManager
├── 列数: 2
├── 列间距: 8px
├── 行间距: 8px
├── 内边距: 12px
├── 卡片宽度: (容器宽度 - 左右padding*2 - 列间距) / 2
├── 卡片高度: 动态计算
│   └── 总高度 = 图片高度 + 标题区高度 + 作者区高度
│       ├── 图片高度 = 卡片宽度 * (原图高度 / 原图宽度)
│       ├── 标题区高度 ≈ 8+14*2+8 = 44px (2行标题)
│       └── 作者区高度 ≈ 20+8 = 28px
└── 新卡片插入: 插入到当前高度较矮的列
```

### TabBarArea 布局详图

```
┌─────────────────────────────────────────────────────────────────┐
│                         TabBarArea                               │
│                   高度: 50px + 底部安全区                          │
├─────────────────────────────────────────────────────────────────┤
│                         顶部分割线 0.5px                          │
│  ┌─────────────┐ ┌─────────────┐ ┌─────────────┐ ┌───────────┐ │
│  │     🏠      │ │     🔮      │ │     💬      │ │    👤     │ │
│  │    24x24    │ │    24x24    │ │    24x24    │ │   24x24   │ │
│  │             │ │   [渐变色]   │ │    [🔴]    │ │           │ │
│  │    首页     │ │    发现     │ │    消息     │ │   我的    │ │
│  │    10sp     │ │ 10sp 主题色 │ │    10sp     │ │   10sp    │ │
│  │   #999999   │ │             │ │   #999999   │ │  #999999  │ │
│  └─────────────┘ └─────────────┘ └─────────────┘ └───────────┘ │
│       flex:1          flex:1          flex:1         flex:1     │
│                                                                 │
│                      底部安全区 (iPhone约34px)                    │
└─────────────────────────────────────────────────────────────────┘

TabBarItem 布局:
├── 容器: flex:1, flex-col, center, gap-2px
├── 图标: 24x24
│   ├── 默认: 灰色图标
│   └── 选中: 彩色/主题色图标 (发现Tab为渐变特殊图标)
├── 文案: 10sp
│   ├── 默认: #999999
│   └── 选中: 主题色
└── 角标: [条件显示]
    ├── 位置: 图标右上角, 偏移(-4px, -4px)
    ├── 尺寸: min-width 16px, height 16px
    └── 样式: 红色背景, 白色文字, 8sp
```

---

## 📁 STEP 5: 代码结构

```
src/modules/
│
├─ Discover/                              # 发现模块
│   │
│   ├─ api/                               # 模块级API（对应 STEP 6）
│   │   ├─ apiGetDiscoverList.ts          # 获取发现列表
│   │   └─ apiPostLike.ts                 # 点赞/取消点赞
│   │
│   ├─ stores/                            # 模块级状态（可选）
│   │   └─ discoverStore.ts               # 发现页状态管理
│   │
│   ├─ hooks/                             # 模块级Hooks
│   │   └─ useDiscoverList.ts             # 列表数据Hook
│   │
│   │   ┌─────────────────────────────────────────────────────────────┐
│   │   │  📱 页面 [L1] - DiscoverPage/index.tsx                       │
│   │   ├─────────────────────────────────────────────────────────────┤
│   │   │  ┌─────────────────────────────────────────────────────┐   │
│   │   │  │  HeaderArea [L2]     [Tab导航+相机按钮]               │   │ 固定
│   │   │  └─────────────────────────────────────────────────────┘   │
│   │   │  ┌─────────────────────────────────────────────────────┐   │
│   │   │  │  ContentListArea [L2]                               │   │
│   │   │  │    └── ContentCard [L3] x N (瀑布流)                 │   │ flex:1
│   │   │  └─────────────────────────────────────────────────────┘   │ 可滚动
│   │   │  ┌─────────────────────────────────────────────────────┐   │
│   │   │  │  TabBarArea [L2]     ← 复用 Common/TabBar            │   │ 固定
│   │   │  └─────────────────────────────────────────────────────┘   │
│   │   │  布局: flex-col | 组合 L2 区域                              │
│   │   └─────────────────────────────────────────────────────────────┘
│   │
│   ├─ DiscoverPage/                      # 发现页文件夹
│   │   │
│   │   ├─ index.tsx                      # 页面主文件 [L1]
│   │   ├─ types.ts                       # 页面类型定义
│   │   │
│   │   │   ┌─────────────────────────────────────────────────────────┐
│   │   │   │  🟢 简单区域 [L2] - HeaderArea.tsx                       │
│   │   │   ├─────────────────────────────────────────────────────────┤
│   │   │   │  ┌────┐      ┌──────────────────┐      ┌────┐          │
│   │   │   │  │    │      │ 关注  热门  同城  │      │ 📷 │          │
│   │   │   │  └────┘      │       ━━━        │      └────┘          │
│   │   │   │  [TL]             [TC]                 [TR]            │
│   │   │   │  布局: flex-row, space-between | 高度: 44px             │
│   │   │   └─────────────────────────────────────────────────────────┘
│   │   │
│   │   ├─ HeaderArea.tsx                 # 🟢 简单区域（单文件）
│   │   │
│   │   │   ┌─────────────────────────────────────────────────────────┐
│   │   │   │  🟢 简单区域 [L2] - TabNavArea.tsx                       │
│   │   │   ├─────────────────────────────────────────────────────────┤
│   │   │   │     ┌──────┐  ┌──────┐  ┌──────┐                       │
│   │   │   │     │ 关注 │  │ 热门 │  │ 同城 │                       │
│   │   │   │     │ ━━━  │  │      │  │      │                       │
│   │   │   │     └──────┘  └──────┘  └──────┘                       │
│   │   │   │     [选中]    [默认]    [默认]                          │
│   │   │   │  布局: flex-row, center, gap-24px                       │
│   │   │   └─────────────────────────────────────────────────────────┘
│   │   │
│   │   ├─ TabNavArea.tsx                 # 🟢 简单区域（单文件）
│   │   │
│   │   │   ┌─────────────────────────────────────────────────────────┐
│   │   │   │  🔵 复杂区域 [L2] - ContentListArea/                     │
│   │   │   ├─────────────────────────────────────────────────────────┤
│   │   │   │  ┌──────────────┐  ┌──────────────┐                    │
│   │   │   │  │  ┌────────┐  │  │  ┌────────┐  │                    │
│   │   │   │  │  │  封面   │  │  │  │  封面   │  │                    │
│   │   │   │  │  │   ▶️   │  │  │  └────────┘  │                    │
│   │   │   │  │  └────────┘  │  │  标题文字    │  ... x N           │
│   │   │   │  │  标题文字    │  │  👤 昵称 ❤️  │                    │
│   │   │   │  │  👤 昵称 ❤️  │  └──────────────┘                    │
│   │   │   │  └──────────────┘       [L3]                           │
│   │   │   │       [L3]                                             │
│   │   │   │  布局: 瀑布流 grid-cols-2, gap-8px | 滚动: scroll-y      │
│   │   │   └─────────────────────────────────────────────────────────┘
│   │   │
│   │   └─ ContentListArea/               # 🔵 复杂区域（文件夹）
│   │       │
│   │       ├─ index.tsx                  # 区域主文件
│   │       ├─ types.ts                   # 类型定义
│   │       ├─ useContentList.ts          # 状态管理Hook
│   │       │
│   │       │   ┌─────────────────────────────────────────────────────┐
│   │       │   │  🧩 子组件 [L3] - ContentCard/                       │
│   │       │   ├─────────────────────────────────────────────────────┤
│   │       │   │  ┌─────────────────────────────────────────────┐   │
│   │       │   │  │  ┌─────────────────────────────────────┐   │   │
│   │       │   │  │  │           封面图片                   │   │   │
│   │       │   │  │  │           [▶️ 播放图标]              │   │   │
│   │       │   │  │  └─────────────────────────────────────┘   │   │
│   │       │   │  │  ┌─────────────────────────────────────┐   │   │
│   │       │   │  │  │           标题文字                   │   │   │
│   │       │   │  │  └─────────────────────────────────────┘   │   │
│   │       │   │  │  ┌─────────────────────────────────────┐   │   │
│   │       │   │  │  │  👤昵称                    ❤️ 88    │   │   │
│   │       │   │  │  └─────────────────────────────────────┘   │   │
│   │       │   │  └─────────────────────────────────────────────┘   │
│   │       │   │  布局: flex-col, 圆角12px | 复用: 1模板 x N实例      │
│   │       │   └─────────────────────────────────────────────────────┘
│   │       │
│   │       └─ ContentCard/               # 子组件文件夹 [L3]
│   │           ├─ index.tsx              # 卡片主文件
│   │           ├─ types.ts               # 卡片类型
│   │           └─ onLike.ts              # 点赞事件处理
│   │
│   └─ index.ts                           # 模块入口
│
└─ Common/                                # 公共模块
    ├─ components/                        # 全局共享组件
    │   │
    │   │   ┌─────────────────────────────────────────────────────────┐
    │   │   │  🔄 TabBar/ [L4]                                         │
    │   │   ├─────────────────────────────────────────────────────────┤
    │   │   │  ┌─────────┐ ┌─────────┐ ┌─────────┐ ┌─────────┐      │
    │   │   │  │   🏠    │ │   🔮    │ │   💬    │ │   👤    │      │
    │   │   │  │  首页   │ │  发现   │ │  消息   │ │  我的   │      │
    │   │   │  └─────────┘ └─────────┘ └─────────┘ └─────────┘      │
    │   │   │  布局: flex-row, 均分 | 高度: 50px + 安全区              │
    │   │   └─────────────────────────────────────────────────────────┘
    │   │
    │   ├─ TabBar/
    │   │   ├─ index.tsx
    │   │   ├─ types.ts
    │   │   └─ TabBarItem.tsx
    │   │
    │   ├─ WaterfallList/                 # 瀑布流组件
    │   │   ├─ index.tsx
    │   │   └─ types.ts
    │   │
    │   ├─ Loading/                       # 加载组件
    │   │   └─ index.tsx
    │   │
    │   ├─ Empty/                         # 空状态组件
    │   │   └─ index.tsx
    │   │
    │   └─ Skeleton/                      # 骨架屏组件
    │       └─ ContentCardSkeleton.tsx
    │
    └─ api/
        └─ apiGetTabbar.ts                # 获取底部导航配置
```

### 文件命名规范

| 类型 | 命名格式 | 示例 |
|------|----------|------|
| 页面主组件 | index.tsx | DiscoverPage/index.tsx |
| 类型定义 | types.ts | ContentListArea/types.ts |
| 状态管理 | use{Name}.ts | useContentList.ts |
| 事件处理 | on{Action}.ts | onLike.ts |
| API接口 | api{Action}.ts | apiGetDiscoverList.ts |
| 区域组件 | {Name}Area.tsx | HeaderArea.tsx |
| 子组件文件夹 | {Name}/ | ContentCard/ |

---

## 🗄️ STEP 6: 接口实现与后端设计

### 接口定义

```json
{
  "接口文档信息": {
    "对应页面": "DiscoverPage",
    "对应数据模型": "STEP 1 → DiscoverPageModel",
    "版本": "v1.0"
  },

  "接口列表": [
    {
      "序号": 1,
      "接口名": "获取发现列表",
      "接口标识": "discoverList",
      "路径": "GET /api/discover/list",
      "触发时机": "[API:discoverList] 页面初始化/下拉刷新/上拉加载/Tab切换",
      "对应数据模型": "STEP 1 → DiscoverPageModel.ContentListData",

      "请求参数": {
        "tab": "String(follow|hot|nearby) - Tab标识 - 来自 TabItem.key",
        "page": "Number - 页码 - 默认1",
        "pageSize": "Number - 每页数量 - 默认20",
        "latitude": "Number? - 纬度 - [可选] tab=nearby时需要",
        "longitude": "Number? - 经度 - [可选] tab=nearby时需要"
      },

      "响应数据": {
        "code": "Number - 状态码 - 200成功",
        "message": "String - 提示信息",
        "data": {
          "_映射": "→ STEP 1: ContentListData",
          "list": "Array<ContentItem> - 内容列表",
          "hasMore": "Boolean - 是否有更多",
          "total": "Number - 总数"
        }
      },

      "响应示例": {
        "code": 200,
        "message": "success",
        "data": {
          "list": [
            {
              "id": "content_001",
              "type": "video",
              "mediaData": {
                "coverUrl": "https://cdn.example.com/cover/001.jpg",
                "aspectRatio": 0.75,
                "duration": 60,
                "width": 1080,
                "height": 1440
              },
              "textData": {
                "title": "请你们看雪"
              },
              "authorData": {
                "userId": "user_001",
                "avatar": "https://cdn.example.com/avatar/001.jpg",
                "nickname": "用户名称"
              },
              "statsData": {
                "likeCount": 88,
                "isLiked": false
              },
              "metaData": {
                "createTime": "2025-01-15T10:30:00Z",
                "location": "北京市"
              }
            }
          ],
          "hasMore": true,
          "total": 1000
        }
      },

      "数据库映射": {
        "list[].id": "contents.id",
        "list[].type": "contents.type",
        "list[].mediaData": "contents.cover_url, contents.aspect_ratio, contents.duration, contents.width, contents.height",
        "list[].textData": "contents.title",
        "list[].authorData": "JOIN users ON contents.user_id = users.id",
        "list[].statsData.likeCount": "contents.like_count (冗余字段)",
        "list[].statsData.isLiked": "LEFT JOIN content_likes 判断当前用户",
        "list[].metaData": "contents.created_at, contents.location"
      }
    },

    {
      "序号": 2,
      "接口名": "点赞/取消点赞",
      "接口标识": "discoverLike",
      "路径": "POST /api/discover/like",
      "触发时机": "[API:discoverLike] 点击点赞按钮",
      "对应数据模型": "STEP 1 → SharedModel.ActionResult",

      "请求参数": {
        "contentId": "String - 内容ID - 来自 ContentItem.id",
        "action": "String(like|unlike) - 操作类型"
      },

      "响应数据": {
        "code": "Number - 状态码",
        "message": "String - 提示信息",
        "data": {
          "_映射": "→ STEP 1: SharedModel.ActionResult",
          "success": "Boolean - 是否成功",
          "isLiked": "Boolean - 新的点赞状态",
          "likeCount": "Number - 新的点赞数"
        }
      },

      "响应示例": {
        "code": 200,
        "message": "success",
        "data": {
          "success": true,
          "isLiked": true,
          "likeCount": 89
        }
      },

      "数据库操作": {
        "like": "INSERT INTO content_likes + UPDATE contents.like_count +1",
        "unlike": "DELETE FROM content_likes + UPDATE contents.like_count -1"
      }
    },

    {
      "序号": 3,
      "接口名": "获取底部导航配置",
      "接口标识": "tabbarConfig",
      "路径": "GET /api/common/tabbar",
      "触发时机": "[API:tabbarConfig] App启动时",
      "缓存策略": "本地缓存 24h",
      "对应数据模型": "STEP 1 → SharedModel.TabBarData",

      "请求参数": {},

      "响应数据": {
        "code": "Number - 状态码",
        "data": {
          "_映射": "→ STEP 1: SharedModel.TabBarData",
          "tabs": "Array<TabBarItem> - 导航项列表"
        }
      },

      "响应示例": {
        "code": 200,
        "data": {
          "tabs": [
            { "key": "home", "label": "首页", "icon": "icon_home", "activeIcon": "icon_home_active", "badge": 0 },
            { "key": "discover", "label": "发现", "icon": "icon_discover", "activeIcon": "icon_discover_active", "badge": 0 },
            { "key": "message", "label": "消息", "icon": "icon_message", "activeIcon": "icon_message_active", "badge": 5 },
            { "key": "profile", "label": "我的", "icon": "icon_profile", "activeIcon": "icon_profile_active", "badge": 0 }
          ]
        }
      }
    }
  ]
}
```

### 接口汇总表

| 序号 | 接口标识 | 方法 | 路径 | 对应数据模型 | 触发时机 | 缓存 |
|------|----------|------|------|--------------|----------|------|
| 1 | discoverList | GET | /api/discover/list | ContentListData | 初始化/刷新/加载/切换Tab | 无 |
| 2 | discoverLike | POST | /api/discover/like | ActionResult | 点击点赞 | 无 |
| 3 | tabbarConfig | GET | /api/common/tabbar | TabBarData | App启动 | 24h |

### 数据库表设计

```sql
-- ================================================================
-- 用户表（对应 STEP 1: SharedModel.UserInfo）
-- ================================================================
CREATE TABLE users (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '用户ID → userId',
    nickname        VARCHAR(50) NOT NULL COMMENT '昵称 → nickname',
    avatar          VARCHAR(500) COMMENT '头像URL → avatar',
    phone           VARCHAR(20) UNIQUE COMMENT '手机号',
    status          TINYINT DEFAULT 1 COMMENT '状态: 0=禁用, 1=正常',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间',
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

    INDEX idx_phone (phone),
    INDEX idx_status (status)
) COMMENT '用户表 → SharedModel.UserInfo' ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;


-- ================================================================
-- 内容表（对应 STEP 1: ItemModel.ContentItem）
-- ================================================================
CREATE TABLE contents (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '内容ID → id',
    user_id         BIGINT NOT NULL COMMENT '创建者ID → authorData.userId',

    -- 媒体数据 → mediaData
    type            VARCHAR(20) DEFAULT 'image' COMMENT '类型 → type (image|video)',
    cover_url       VARCHAR(500) NOT NULL COMMENT '封面URL → mediaData.coverUrl',
    aspect_ratio    DECIMAL(4,2) DEFAULT 1.0 COMMENT '宽高比 → mediaData.aspectRatio',
    duration        INT DEFAULT 0 COMMENT '时长(秒) → mediaData.duration [条件:type=video]',
    width           INT DEFAULT 0 COMMENT '原图宽度 → mediaData.width',
    height          INT DEFAULT 0 COMMENT '原图高度 → mediaData.height',

    -- 文本数据 → textData
    title           VARCHAR(200) NOT NULL COMMENT '标题 → textData.title',

    -- 统计数据 → statsData (冗余字段，提高查询性能)
    like_count      INT UNSIGNED DEFAULT 0 COMMENT '点赞数 → statsData.likeCount',

    -- 元信息 → metaData
    location        VARCHAR(100) COMMENT '位置 → metaData.location',
    latitude        DECIMAL(10, 7) COMMENT '纬度 [用于:同城筛选]',
    longitude       DECIMAL(10, 7) COMMENT '经度 [用于:同城筛选]',

    -- 系统字段
    status          TINYINT DEFAULT 1 COMMENT '状态: 0=删除, 1=正常, 2=审核中',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间 → metaData.createTime',
    updated_at      DATETIME DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',

    INDEX idx_user_id (user_id),
    INDEX idx_status_created (status, created_at DESC),
    INDEX idx_location (latitude, longitude),

    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) COMMENT '内容表 → ItemModel.ContentItem' ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;


-- ================================================================
-- 点赞关系表（用于判断 STEP 1: statsData.isLiked）
-- ================================================================
CREATE TABLE content_likes (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
    content_id      BIGINT NOT NULL COMMENT '内容ID',
    user_id         BIGINT NOT NULL COMMENT '用户ID',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '点赞时间',

    UNIQUE KEY uk_content_user (content_id, user_id),
    INDEX idx_user_id (user_id),

    FOREIGN KEY (content_id) REFERENCES contents(id) ON DELETE CASCADE,
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE
) COMMENT '点赞关系表 → 判断 isLiked' ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;


-- ================================================================
-- 关注关系表（用于 tab=follow 筛选）
-- ================================================================
CREATE TABLE user_follows (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
    follower_id     BIGINT NOT NULL COMMENT '关注者ID (当前用户)',
    following_id    BIGINT NOT NULL COMMENT '被关注者ID',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP COMMENT '关注时间',

    UNIQUE KEY uk_follow (follower_id, following_id),
    INDEX idx_following_id (following_id),

    FOREIGN KEY (follower_id) REFERENCES users(id) ON DELETE CASCADE,
    FOREIGN KEY (following_id) REFERENCES users(id) ON DELETE CASCADE
) COMMENT '关注关系表 → tab=follow筛选' ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;


-- ================================================================
-- 热门内容表/缓存表（用于 tab=hot 筛选，可选）
-- ================================================================
CREATE TABLE hot_contents (
    id              BIGINT PRIMARY KEY AUTO_INCREMENT COMMENT '主键',
    content_id      BIGINT NOT NULL COMMENT '内容ID',
    score           DECIMAL(10,2) DEFAULT 0 COMMENT '热度分数',
    rank_date       DATE NOT NULL COMMENT '排名日期',
    created_at      DATETIME DEFAULT CURRENT_TIMESTAMP,

    UNIQUE KEY uk_content_date (content_id, rank_date),
    INDEX idx_date_score (rank_date, score DESC),

    FOREIGN KEY (content_id) REFERENCES contents(id) ON DELETE CASCADE
) COMMENT '热门内容表 → tab=hot筛选' ENGINE=InnoDB DEFAULT CHARSET=utf8mb4;
```

### 核心SQL查询

```sql
-- ================================================================
-- 发现列表查询（返回 STEP 1: ContentListData）
-- ================================================================

-- 1. 关注Tab列表 (tab=follow)
SELECT
    -- ContentItem.id
    c.id,

    -- ContentItem.type
    c.type,

    -- ContentItem.mediaData
    c.cover_url AS coverUrl,
    c.aspect_ratio AS aspectRatio,
    c.duration,
    c.width,
    c.height,

    -- ContentItem.textData
    c.title,

    -- ContentItem.authorData ← JOIN users
    u.id AS userId,
    u.nickname,
    u.avatar,

    -- ContentItem.statsData
    c.like_count AS likeCount,
    IF(cl.id IS NOT NULL, 1, 0) AS isLiked,  -- ← LEFT JOIN content_likes

    -- ContentItem.metaData
    c.created_at AS createTime,
    c.location

FROM contents c
INNER JOIN users u ON c.user_id = u.id
INNER JOIN user_follows uf ON c.user_id = uf.following_id AND uf.follower_id = #{currentUserId}
LEFT JOIN content_likes cl ON c.id = cl.content_id AND cl.user_id = #{currentUserId}
WHERE c.status = 1
ORDER BY c.created_at DESC
LIMIT #{pageSize} OFFSET #{offset};


-- 2. 热门Tab列表 (tab=hot)
SELECT
    c.id,
    c.type,
    c.cover_url AS coverUrl,
    c.aspect_ratio AS aspectRatio,
    c.duration,
    c.width,
    c.height,
    c.title,
    u.id AS userId,
    u.nickname,
    u.avatar,
    c.like_count AS likeCount,
    IF(cl.id IS NOT NULL, 1, 0) AS isLiked,
    c.created_at AS createTime,
    c.location

FROM contents c
INNER JOIN users u ON c.user_id = u.id
LEFT JOIN content_likes cl ON c.id = cl.content_id AND cl.user_id = #{currentUserId}
WHERE c.status = 1
ORDER BY c.like_count DESC, c.created_at DESC  -- 按点赞数排序
LIMIT #{pageSize} OFFSET #{offset};


-- 3. 同城Tab列表 (tab=nearby)
SELECT
    c.id,
    c.type,
    c.cover_url AS coverUrl,
    c.aspect_ratio AS aspectRatio,
    c.duration,
    c.width,
    c.height,
    c.title,
    u.id AS userId,
    u.nickname,
    u.avatar,
    c.like_count AS likeCount,
    IF(cl.id IS NOT NULL, 1, 0) AS isLiked,
    c.created_at AS createTime,
    c.location,
    -- 计算距离（单位：米）
    ST_Distance_Sphere(
        POINT(c.longitude, c.latitude),
        POINT(#{longitude}, #{latitude})
    ) AS distance

FROM contents c
INNER JOIN users u ON c.user_id = u.id
LEFT JOIN content_likes cl ON c.id = cl.content_id AND cl.user_id = #{currentUserId}
WHERE c.status = 1
  AND c.latitude IS NOT NULL
  AND c.longitude IS NOT NULL
  AND ST_Distance_Sphere(
        POINT(c.longitude, c.latitude),
        POINT(#{longitude}, #{latitude})
      ) <= 50000  -- 50公里范围内
ORDER BY distance ASC, c.created_at DESC
LIMIT #{pageSize} OFFSET #{offset};


-- ================================================================
-- 点赞操作（更新 isLiked 和 likeCount）
-- ================================================================

-- 点赞 (action=like)
INSERT INTO content_likes (content_id, user_id)
VALUES (#{contentId}, #{currentUserId});

UPDATE contents
SET like_count = like_count + 1, updated_at = NOW()
WHERE id = #{contentId};


-- 取消点赞 (action=unlike)
DELETE FROM content_likes
WHERE content_id = #{contentId} AND user_id = #{currentUserId};

UPDATE contents
SET like_count = GREATEST(like_count - 1, 0), updated_at = NOW()
WHERE id = #{contentId};


-- ================================================================
-- 获取列表总数（用于返回 total）
-- ================================================================

-- 关注Tab总数
SELECT COUNT(*) AS total
FROM contents c
INNER JOIN user_follows uf ON c.user_id = uf.following_id AND uf.follower_id = #{currentUserId}
WHERE c.status = 1;

-- 热门Tab总数
SELECT COUNT(*) AS total
FROM contents
WHERE status = 1;

-- 同城Tab总数
SELECT COUNT(*) AS total
FROM contents
WHERE status = 1
  AND latitude IS NOT NULL
  AND longitude IS NOT NULL
  AND ST_Distance_Sphere(
        POINT(longitude, latitude),
        POINT(#{longitude}, #{latitude})
      ) <= 50000;
```

### MVP清单

| 序号 | 功能 | 对应数据模型 | 涉及表 | 接口 | 优先级 |
|------|------|--------------|--------|------|--------|
| 1 | 热门列表查询 | ContentListData | users, contents | GET /list?tab=hot | P0 |
| 2 | 关注列表查询 | ContentListData | users, contents, user_follows | GET /list?tab=follow | P0 |
| 3 | 同城列表查询 | ContentListData | users, contents | GET /list?tab=nearby | P1 |
| 4 | 分页支持 | PaginationInfo | - | page, pageSize | P0 |
| 5 | 点赞操作 | ActionResult | content_likes, contents | POST /like | P0 |
| 6 | 点赞状态判断 | isLiked | content_likes | LEFT JOIN | P0 |
| 7 | 底部导航配置 | TabBarData | - (硬编码或配置表) | GET /tabbar | P2 |

**MVP统计：4张表 + 3个接口 = 最小可行后端**

### 性能优化检查点

```
✅ 已优化:
├── [索引] contents 表: idx_status_created, idx_user_id, idx_location
├── [索引] content_likes 表: uk_content_user, idx_user_id
├── [索引] user_follows 表: uk_follow, idx_following_id
├── [冗余] like_count 存储在 contents 表，避免 COUNT 查询
└── [分页] 使用 LIMIT OFFSET（中小数据量可用）

⚠️ 待优化（数据量大时）:
├── [分页] 大数据量改用游标分页（基于 id 或 created_at）
├── [缓存] 热门列表可缓存到 Redis，定时刷新
├── [N+1] 确保使用 JOIN 而非循环查询
└── [地理] 同城查询可使用 GeoHash 或 Redis GEO 优化
```

---

## 🎯 质量评估

| 维度 | 评分 | 说明 |
|------|------|------|
| 数据模型 | 9/10 | PageModel + ItemModel + SharedModel 完整，字段与UI绑定清晰 |
| 结构完整性 | 9/10 | 所有UI元素都有层级标注，L1→L2→L3 清晰 |
| 数据绑定 | 9/10 | 所有动态数据都有 [data:字段] 标注 |
| 位置精确度 | 8/10 | 提供九宫格、行列、Flex/Grid 布局规则，有ASCII可视化 |
| 组件拆分 | 9/10 | 11个组件，层级清晰，复用关系标记完整 |
| 复用标记 | 9/10 | TabBar复用、ContentCard模板复用标记完整 |
| 交互描述 | 9/10 | 流程图覆盖所有交互，包含乐观更新和错误处理 |
| 代码映射 | 9/10 | 完整文件结构，可直接创建文件夹 |
| 接口+后端 | 9/10 | 接口对应数据模型，SQL可直接执行，MVP清单完整 |

**综合评分：8.8/10** ✅ 达到优秀标准

---

*文档生成时间: 2025-12-01*
*模板版本: 纯结构架构图标准模板 v5.4*
