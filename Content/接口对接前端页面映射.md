# 🔗 动态接口对接前端页面映射文档

**创建日期：** 2025-01-15  
**后端服务：** xypai-content (端口:9402)  
**前端项目：** XiangYuPai-RNExpoAPP

---

## 📊 对接概览

| 后端模块 | 前端页面数量 | API服务状态 |
|---------|------------|------------|
| 发现页面公开接口 | 3个主页面 | ✅ 已定义 (discoveryApi.ts) |
| 内容发布管理 | 2个主页面 | ✅ 已定义 (contentApi.ts) |
| 内容互动操作 | 4个功能区 | ✅ 已定义 (contentApi.ts) |
| 评论管理 | 2个功能区 | ✅ 已定义 (commentApi.ts) |
| 草稿管理 | 1个页面 | ⚠️ 待定义 |

---

## 🎯 一、发现页面公开接口（Discovery）

### 后端接口路径：`/api/v1/discovery/**`
### 前端API服务：`discoveryApi.ts` ✅
### 权限要求：✅ 无需登录（公开访问）

---

### 1.1 发现页主页面（Discover Tab）
**文件路径：** `app/(tabs)/discover.tsx`  
**功能组件：** `src/features/Discovery/MainPage/index.tsx`

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 🔥 获取热门内容 | GET | `/api/v1/discovery/hot` | `discoveryApi.getHotContents()` | 热门Tab加载数据 |
| ⭐ 获取推荐内容 | GET | `/api/v1/discovery/recommended` | `discoveryApi.getRecommendedContents()` | 关注Tab加载数据 |
| 🏘️ 获取同城内容 | GET | `/api/v1/discovery/local` | `discoveryApi.getLocalContents()` | 同城Tab加载数据 |
| 📍 获取附近内容 | GET | `/api/v1/discovery/nearby` | `discoveryApi.getNearbyContents()` | 基于地理位置展示附近动态 |

**对接示例代码：**
```typescript
// src/features/Discovery/MainPage/index.tsx

import { discoveryApi } from '@/services/api';

// 加载热门内容
const loadHotFeeds = async () => {
  const feeds = await discoveryApi.getHotContents({ 
    type: 1,  // 1=动态
    limit: 20 
  });
  setHotFeeds(feeds);
};

// 加载附近内容
const loadNearbyFeeds = async (longitude: number, latitude: number) => {
  const feeds = await discoveryApi.getNearbyContents(
    longitude, 
    latitude, 
    5000,  // 半径5000米
    1,     // 类型：动态
    20     // 数量限制
  );
  setLocalFeeds(feeds);
};
```

**Zustand Store集成：**
```typescript
// stores/discoveryStore.ts - 已存在

export const useDiscoveryStore = create((set) => ({
  feedData: {
    hotFeeds: [],
    followFeeds: [],
    localFeeds: []
  },
  
  loadFeedList: async (tab: 'hot' | 'follow' | 'local') => {
    let feeds = [];
    switch(tab) {
      case 'hot':
        feeds = await discoveryApi.getHotContents({ limit: 20 });
        break;
      case 'follow':
        feeds = await discoveryApi.getRecommendedContents({ limit: 20 });
        break;
      case 'local':
        feeds = await discoveryApi.getLocalContents({ limit: 20 });
        break;
    }
    set(state => ({
      feedData: {
        ...state.feedData,
        [`${tab}Feeds`]: feeds
      }
    }));
  }
}));
```

---

### 1.2 动态详情页面（Feed Detail）
**文件路径：** `app/feed/[id].tsx`  
**功能组件：** `src/features/Discovery/DetailPage/index.tsx`

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 📄 获取内容详情 | GET | `/api/v1/discovery/{contentId}` | `discoveryApi.getContentDetail(contentId)` | 打开动态详情时加载完整内容 |

**对接示例代码：**
```typescript
// src/features/Discovery/DetailPage/index.tsx

import { discoveryApi } from '@/services/api';

const DetailPage = ({ feedId }: { feedId: string }) => {
  const [detail, setDetail] = useState<ContentDetailVO | null>(null);
  
  useEffect(() => {
    const loadDetail = async () => {
      const data = await discoveryApi.getContentDetail(feedId);
      setDetail(data);
    };
    loadDetail();
  }, [feedId]);
  
  return (
    <ScrollView>
      {detail && (
        <>
          <Text>{detail.content}</Text>
          <Image source={{ uri: detail.coverImage }} />
          {/* ... 其他内容展示 */}
        </>
      )}
    </ScrollView>
  );
};
```

---

### 1.3 搜索页面（Search）
**文件路径：** `app/(tabs)/homepage/search.tsx`  
**潜在路由：** `/discover/search` (待创建)

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 🔍 搜索内容 | GET | `/api/v1/discovery/search` | `discoveryApi.searchContents({ keyword, type, limit })` | 用户输入关键词搜索动态 |

**对接示例代码：**
```typescript
// 搜索页面组件

import { discoveryApi } from '@/services/api';

const SearchPage = () => {
  const [keyword, setKeyword] = useState('');
  const [results, setResults] = useState<ContentListVO[]>([]);
  
  const handleSearch = async () => {
    if (keyword.trim()) {
      const data = await discoveryApi.searchContents({
        keyword: keyword.trim(),
        type: 1,  // 动态类型
        limit: 20
      });
      setResults(data);
    }
  };
  
  return (
    <View>
      <TextInput 
        value={keyword}
        onChangeText={setKeyword}
        onSubmitEditing={handleSearch}
        placeholder="搜索动态..."
      />
      <FlatList 
        data={results}
        renderItem={({ item }) => <FeedCard feed={item} />}
      />
    </View>
  );
};
```

---

### 1.4 用户主页（查看他人动态）
**文件路径：** `app/profile/[userId].tsx`  
**功能组件：** `src/features/Profile/OtherUserProfilePage/index.tsx`

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 👤 获取用户内容 | GET | `/api/v1/discovery/user/{userId}` | `discoveryApi.getUserContents(userId, type, limit)` | 查看某个用户发布的所有动态 |
| 🏙️ 获取城市内容 | GET | `/api/v1/discovery/city/{cityId}` | `discoveryApi.getContentsByCity(cityId, type, limit)` | 查看某个城市的所有动态 |

**对接示例代码：**
```typescript
// src/features/Profile/OtherUserProfilePage/index.tsx

import { discoveryApi } from '@/services/api';

const OtherUserProfilePage = ({ userId }: { userId: string }) => {
  const [userContents, setUserContents] = useState<ContentListVO[]>([]);
  
  useEffect(() => {
    const loadUserContents = async () => {
      const contents = await discoveryApi.getUserContents(
        parseInt(userId),
        1,  // 类型：动态
        20  // 数量
      );
      setUserContents(contents);
    };
    loadUserContents();
  }, [userId]);
  
  return (
    <FlatList
      data={userContents}
      renderItem={({ item }) => <FeedCard feed={item} />}
    />
  );
};
```

---

## ✏️ 二、内容发布管理（Content Management）

### 后端接口路径：`/api/v1/contents/app/**`
### 前端API服务：`contentApi.ts` ✅
### 权限要求：🔒 需要登录

---

### 2.1 发布页面（Publish Page）
**文件路径：** `app/publish.tsx`

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| ✏️ 发布内容 | POST | `/api/v1/contents/app/publish` | `contentApi.publish(data)` | 用户点击"发布"按钮提交新动态 |

**对接示例代码：**
```typescript
// app/publish.tsx

import { contentApi } from '@/services/api';
import type { ContentPublishDTO } from '@/services/api/contentApi';

const PublishPage = () => {
  const [title, setTitle] = useState('');
  const [content, setContent] = useState('');
  const [images, setImages] = useState<string[]>([]);
  const [location, setLocation] = useState('');
  const [longitude, setLongitude] = useState<number>();
  const [latitude, setLatitude] = useState<number>();
  
  const handlePublish = async () => {
    if (!content.trim()) {
      Alert.alert('提示', '请输入动态内容');
      return;
    }
    
    try {
      const publishData: ContentPublishDTO = {
        title: title.trim() || undefined,
        content: content.trim(),
        type: 1,  // 1=图文动态
        mediaUrls: images,
        location,
        longitude,
        latitude,
      };
      
      const contentId = await contentApi.publish(publishData);
      
      Alert.alert('成功', '发布成功！', [
        { text: '确定', onPress: () => router.back() }
      ]);
    } catch (error) {
      Alert.alert('错误', '发布失败，请重试');
    }
  };
  
  return (
    <View>
      <TextInput 
        placeholder="添加标题（可选）"
        value={title}
        onChangeText={setTitle}
      />
      <TextInput 
        placeholder="分享你的精彩瞬间..."
        value={content}
        onChangeText={setContent}
        multiline
      />
      {/* ... 图片选择、地点选择等 */}
      <TouchableOpacity onPress={handlePublish}>
        <Text>发布</Text>
      </TouchableOpacity>
    </View>
  );
};
```

---

### 2.2 个人主页（我的动态）
**文件路径：** `app/(tabs)/profile.tsx`  
**功能组件：** `src/features/Profile/MainPage/index.tsx`

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 📋 获取我的内容 | GET | `/api/v1/contents/app/my` | `contentApi.getMyContents({ type, status })` | 个人主页"动态"Tab展示自己发布的内容 |
| ✏️ 编辑内容 | PUT | `/api/v1/contents/app/{contentId}` | `contentApi.update(data)` | 编辑自己的动态 |
| 🗑️ 删除内容 | DELETE | `/api/v1/contents/app/{contentId}` | `contentApi.delete(contentId)` | 删除自己的动态 |

**对接示例代码：**
```typescript
// src/features/Profile/MainPage/TabContentArea.tsx (动态Tab)

import { contentApi } from '@/services/api';

const DynamicTab = () => {
  const [myContents, setMyContents] = useState<ContentListVO[]>([]);
  
  // 加载我的动态
  useEffect(() => {
    const loadMyContents = async () => {
      const contents = await contentApi.getMyContents({
        type: 1,      // 动态类型
        status: 1,    // 已发布
        pageNum: 1,
        pageSize: 20
      });
      setMyContents(contents);
    };
    loadMyContents();
  }, []);
  
  // 删除动态
  const handleDelete = async (contentId: number) => {
    Alert.alert('确认', '确定要删除这条动态吗？', [
      { text: '取消', style: 'cancel' },
      { 
        text: '删除', 
        style: 'destructive',
        onPress: async () => {
          const success = await contentApi.delete(contentId);
          if (success) {
            // 从列表中移除
            setMyContents(prev => prev.filter(item => item.id !== contentId));
            Alert.alert('成功', '删除成功');
          }
        }
      }
    ]);
  };
  
  return (
    <FlatList
      data={myContents}
      renderItem={({ item }) => (
        <FeedCard 
          feed={item}
          onDelete={() => handleDelete(item.id)}
          showActions  // 显示编辑/删除按钮
        />
      )}
    />
  );
};
```

---

## 💬 三、内容互动操作（Actions）

### 后端接口路径：`/api/v1/actions/**`
### 前端API服务：`contentApi.ts` ✅
### 权限要求：🔒 需要登录

这些接口在多个页面都会用到，是核心的互动功能。

---

### 3.1 动态卡片组件（Feed Card）
**文件路径：** `src/features/Discovery/MainPage/components/FeedCard.tsx`  
**使用页面：** 发现页、个人主页、搜索结果页

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 👍 点赞/取消点赞 | POST | `/api/v1/actions/like` | `contentApi.toggleLike(contentId)` | 用户点击点赞按钮 |
| ⭐ 收藏/取消收藏 | POST | `/api/v1/actions/collect` | `contentApi.toggleCollect(contentId)` | 用户点击收藏按钮 |
| 📤 分享内容 | POST | `/api/v1/actions/share` | `contentApi.share(contentId)` | 用户点击分享按钮 |

**对接示例代码：**
```typescript
// src/features/Discovery/MainPage/components/FeedCard.tsx

import { contentApi } from '@/services/api';

const FeedCard = ({ feed }: { feed: ContentListVO }) => {
  const [isLiked, setIsLiked] = useState(feed.isLiked);
  const [isCollected, setIsCollected] = useState(feed.isCollected);
  const [likeCount, setLikeCount] = useState(feed.likeCount);
  const [collectCount, setCollectCount] = useState(feed.collectCount);
  
  // 点赞
  const handleLike = async () => {
    const success = await contentApi.toggleLike(feed.id);
    if (success) {
      setIsLiked(!isLiked);
      setLikeCount(prev => isLiked ? prev - 1 : prev + 1);
    }
  };
  
  // 收藏
  const handleCollect = async () => {
    const success = await contentApi.toggleCollect(feed.id);
    if (success) {
      setIsCollected(!isCollected);
      setCollectCount(prev => isCollected ? prev - 1 : prev + 1);
    }
  };
  
  // 分享
  const handleShare = async () => {
    const success = await contentApi.share(feed.id);
    if (success) {
      Alert.alert('成功', '分享成功');
    }
  };
  
  return (
    <View style={styles.card}>
      {/* 内容展示 */}
      <Text>{feed.content}</Text>
      
      {/* 互动按钮区 */}
      <View style={styles.actions}>
        <TouchableOpacity onPress={handleLike}>
          <Icon name={isLiked ? 'heart' : 'heart-outline'} />
          <Text>{likeCount}</Text>
        </TouchableOpacity>
        
        <TouchableOpacity onPress={handleCollect}>
          <Icon name={isCollected ? 'star' : 'star-outline'} />
          <Text>{collectCount}</Text>
        </TouchableOpacity>
        
        <TouchableOpacity onPress={handleShare}>
          <Icon name="share-outline" />
        </TouchableOpacity>
      </View>
    </View>
  );
};
```

---

### 3.2 个人主页 - 点赞Tab
**文件路径：** `src/features/Profile/LikesPage/index.tsx`

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 📋 获取我的点赞 | GET | `/api/v1/actions/like/my` | `contentApi.getMyLikes(pageNum, pageSize)` | 个人主页"点赞"Tab展示点赞过的动态 |
| ❓ 检查点赞状态 | GET | `/api/v1/actions/like/status/{contentId}` | `contentApi.checkLikeStatus(contentId)` | 检查单个内容的点赞状态 |
| 📦 批量检查点赞 | POST | `/api/v1/actions/like/batch-status` | `contentApi.batchCheckLikeStatus(contentIds)` | 批量检查多个内容的点赞状态 |

**对接示例代码：**
```typescript
// src/features/Profile/LikesPage/index.tsx

import { contentApi } from '@/services/api';

const LikesPage = () => {
  const [likedContents, setLikedContents] = useState<ContentListVO[]>([]);
  
  useEffect(() => {
    const loadLikedContents = async () => {
      const contents = await contentApi.getMyLikes(1, 20);
      setLikedContents(contents);
    };
    loadLikedContents();
  }, []);
  
  return (
    <FlatList
      data={likedContents}
      renderItem={({ item }) => <FeedCard feed={item} />}
    />
  );
};
```

---

### 3.3 个人主页 - 收藏Tab
**文件路径：** `src/features/Profile/CollectionPage/index.tsx`

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 📋 获取我的收藏 | GET | `/api/v1/actions/collect/my` | `contentApi.getMyCollects(pageNum, pageSize)` | 个人主页"收藏"Tab展示收藏的动态 |
| ❓ 检查收藏状态 | GET | `/api/v1/actions/collect/status/{contentId}` | `contentApi.checkCollectStatus(contentId)` | 检查单个内容的收藏状态 |
| 📦 批量检查收藏 | POST | `/api/v1/actions/collect/batch-status` | `contentApi.batchCheckCollectStatus(contentIds)` | 批量检查多个内容的收藏状态 |

**对接示例代码：**
```typescript
// src/features/Profile/CollectionPage/index.tsx

import { contentApi } from '@/services/api';

const CollectionPage = () => {
  const [collectedContents, setCollectedContents] = useState<ContentListVO[]>([]);
  
  useEffect(() => {
    const loadCollectedContents = async () => {
      const contents = await contentApi.getMyCollects(1, 20);
      setCollectedContents(contents);
    };
    loadCollectedContents();
  }, []);
  
  return (
    <FlatList
      data={collectedContents}
      renderItem={({ item }) => <FeedCard feed={item} />}
    />
  );
};
```

---

## 💬 四、评论管理（Comments）

### 后端接口路径：`/api/v1/comments/app/**`
### 前端API服务：`commentApi.ts` ✅
### 权限要求：部分需要登录

---

### 4.1 动态详情页 - 评论区
**文件路径：** `src/features/Discovery/DetailPage/index.tsx`  
**子组件：** `CommentSection.tsx` (待创建)

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 📋 获取评论列表 | GET | `/api/v1/comments/app/content/{contentId}` | `commentApi.getList({ contentId, pageNum, pageSize })` | 展示动态的评论列表（无需登录） |
| 💬 获取评论回复 | GET | `/api/v1/comments/app/{parentId}/replies` | `commentApi.getReplies(parentId)` | 展开查看某条评论的所有回复（无需登录） |
| 📊 统计评论数 | GET | `/api/v1/comments/app/count/{contentId}` | `commentApi.count(contentId)` | 显示评论总数（无需登录） |
| ✏️ 发表评论 | POST | `/api/v1/comments/app` | `commentApi.add(data)` | 用户发表评论或回复（需要登录） |
| 👍 评论点赞 | POST | `/api/v1/comments/app/{commentId}/like` | `commentApi.toggleLike(commentId)` | 点赞评论（需要登录） |
| 🗑️ 删除评论 | DELETE | `/api/v1/comments/app/{commentId}` | `commentApi.delete(commentId)` | 删除自己的评论（需要登录） |

**对接示例代码：**
```typescript
// src/features/Discovery/DetailPage/components/CommentSection.tsx

import { commentApi } from '@/services/api';
import type { CommentVO, CommentAddDTO } from '@/services/api/commentApi';

const CommentSection = ({ contentId }: { contentId: number }) => {
  const [comments, setComments] = useState<CommentVO[]>([]);
  const [commentText, setCommentText] = useState('');
  const [replyingTo, setReplyingTo] = useState<CommentVO | null>(null);
  
  // 加载评论列表
  useEffect(() => {
    const loadComments = async () => {
      const list = await commentApi.getList({
        contentId,
        pageNum: 1,
        pageSize: 20
      });
      setComments(list);
    };
    loadComments();
  }, [contentId]);
  
  // 发表评论
  const handleSubmitComment = async () => {
    if (!commentText.trim()) return;
    
    const commentData: CommentAddDTO = {
      contentId,
      content: commentText.trim(),
      parentId: replyingTo?.id ? parseInt(replyingTo.id) : undefined,
      replyToUserId: replyingTo?.userId ? parseInt(replyingTo.userId) : undefined,
    };
    
    try {
      const commentId = await commentApi.add(commentData);
      
      // 重新加载评论列表
      const list = await commentApi.getList({ contentId, pageNum: 1, pageSize: 20 });
      setComments(list);
      
      setCommentText('');
      setReplyingTo(null);
      Alert.alert('成功', '评论发表成功');
    } catch (error) {
      Alert.alert('失败', '评论发表失败');
    }
  };
  
  // 加载回复
  const handleLoadReplies = async (comment: CommentVO) => {
    const replies = await commentApi.getReplies(parseInt(comment.id));
    
    // 更新评论列表，将回复添加到对应评论下
    setComments(prev => prev.map(c => 
      c.id === comment.id ? { ...c, replies } : c
    ));
  };
  
  // 点赞评论
  const handleLikeComment = async (commentId: string) => {
    const liked = await commentApi.toggleLike(parseInt(commentId));
    
    // 乐观更新UI
    setComments(prev => prev.map(c => 
      c.id === commentId 
        ? { ...c, isLiked: liked, likeCount: liked ? c.likeCount + 1 : c.likeCount - 1 }
        : c
    ));
  };
  
  // 删除评论
  const handleDeleteComment = async (commentId: string) => {
    Alert.alert('确认', '确定要删除这条评论吗？', [
      { text: '取消', style: 'cancel' },
      { 
        text: '删除', 
        style: 'destructive',
        onPress: async () => {
          const success = await commentApi.delete(parseInt(commentId));
          if (success) {
            setComments(prev => prev.filter(c => c.id !== commentId));
            Alert.alert('成功', '删除成功');
          }
        }
      }
    ]);
  };
  
  return (
    <View style={styles.container}>
      {/* 评论列表 */}
      <FlatList
        data={comments}
        renderItem={({ item }) => (
          <CommentItem
            comment={item}
            onReply={() => setReplyingTo(item)}
            onLoadReplies={() => handleLoadReplies(item)}
            onLike={() => handleLikeComment(item.id)}
            onDelete={() => handleDeleteComment(item.id)}
          />
        )}
      />
      
      {/* 评论输入框 */}
      <View style={styles.inputArea}>
        {replyingTo && (
          <View style={styles.replyTag}>
            <Text>回复 @{replyingTo.userName}</Text>
            <TouchableOpacity onPress={() => setReplyingTo(null)}>
              <Text>×</Text>
            </TouchableOpacity>
          </View>
        )}
        <TextInput
          value={commentText}
          onChangeText={setCommentText}
          placeholder={replyingTo ? `回复 @${replyingTo.userName}` : '发表评论...'}
          multiline
        />
        <TouchableOpacity onPress={handleSubmitComment}>
          <Text>发送</Text>
        </TouchableOpacity>
      </View>
    </View>
  );
};
```

---

### 4.2 消息中心 - 评论通知Tab
**文件路径：** `src/features/Messages/CommentsPage/index.tsx`

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 📋 获取评论列表 | GET | `/api/v1/comments/app/content/{contentId}` | `commentApi.getList()` | 展示收到的评论通知 |

**说明：** 这里主要展示别人对我的动态的评论，需要结合"我的动态ID列表"来获取评论。

---

## 💾 五、草稿管理（Drafts）

### 后端接口路径：`/api/v1/drafts/**`
### 前端API服务：⚠️ **需要创建 `draftApi.ts`**
### 权限要求：🔒 需要登录+权限

---

### 5.1 发布页面 - 自动保存草稿
**文件路径：** `app/publish.tsx`

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 💾 保存草稿 | POST | `/api/v1/drafts/save` | `draftApi.save(data)` | 每10秒自动保存草稿 |
| 📤 发布草稿 | POST | `/api/v1/drafts/{draftId}/publish` | `draftApi.publish(draftId)` | 将草稿发布为正式内容 |

**需要创建的API服务文件：**
```typescript
// services/api/draftApi.ts (待创建)

import { apiClient } from './client';

export interface DraftSaveDTO {
  title?: string;
  content: string;
  type?: number;
  mediaUrls?: string[];
  location?: string;
  longitude?: number;
  latitude?: number;
}

export interface DraftVO {
  id: string;
  title?: string;
  content: string;
  type: number;
  mediaUrls?: string[];
  createdAt: number;
  updatedAt: number;
}

class DraftAPI {
  async save(data: DraftSaveDTO): Promise<number> {
    const response = await apiClient.post<number>('/xypai-content/api/v1/drafts/save', data);
    return response.data;
  }
  
  async getMyDrafts(limit: number = 10): Promise<DraftVO[]> {
    const response = await apiClient.get<DraftVO[]>(`/xypai-content/api/v1/drafts/my?limit=${limit}`);
    return response.data || [];
  }
  
  async getDraft(draftId: number): Promise<DraftVO | null> {
    const response = await apiClient.get<DraftVO>(`/xypai-content/api/v1/drafts/${draftId}`);
    return response.data || null;
  }
  
  async delete(draftId: number): Promise<boolean> {
    await apiClient.delete(`/xypai-content/api/v1/drafts/${draftId}`);
    return true;
  }
  
  async publish(draftId: number): Promise<number> {
    const response = await apiClient.post<number>(`/xypai-content/api/v1/drafts/${draftId}/publish`);
    return response.data;
  }
  
  async count(): Promise<number> {
    const response = await apiClient.get<number>('/xypai-content/api/v1/drafts/count');
    return response.data || 0;
  }
}

export const draftApi = new DraftAPI();
```

**对接示例代码（发布页面自动保存）：**
```typescript
// app/publish.tsx

import { draftApi } from '@/services/api/draftApi';

const PublishPage = () => {
  const [title, setTitle] = useState('');
  const [content, setContent] = useState('');
  const [draftId, setDraftId] = useState<number | null>(null);
  
  // 自动保存草稿（每10秒）
  useEffect(() => {
    const autoSave = setInterval(async () => {
      if (content.trim()) {
        try {
          const id = await draftApi.save({
            title: title.trim() || undefined,
            content: content.trim(),
            type: 1,
          });
          setDraftId(id);
          console.log('草稿已自动保存');
        } catch (error) {
          console.error('草稿保存失败', error);
        }
      }
    }, 10000); // 10秒
    
    return () => clearInterval(autoSave);
  }, [title, content]);
  
  // 发布草稿
  const handlePublishDraft = async () => {
    if (draftId) {
      try {
        const contentId = await draftApi.publish(draftId);
        Alert.alert('成功', '发布成功！', [
          { text: '确定', onPress: () => router.back() }
        ]);
      } catch (error) {
        Alert.alert('失败', '发布失败');
      }
    }
  };
  
  return (
    // ... UI
  );
};
```

---

### 5.2 草稿列表页面
**文件路径：** `app/drafts.tsx` (待创建)  
**或集成在个人主页：** `src/features/Profile/MainPage/index.tsx` (新增草稿Tab)

#### 对接接口：

| 接口 | HTTP方法 | 后端路径 | 前端API方法 | 使用场景 |
|------|----------|----------|------------|---------|
| 📋 获取草稿列表 | GET | `/api/v1/drafts/my` | `draftApi.getMyDrafts(limit)` | 查看所有草稿 |
| 📄 获取草稿详情 | GET | `/api/v1/drafts/{draftId}` | `draftApi.getDraft(draftId)` | 继续编辑草稿 |
| 🗑️ 删除草稿 | DELETE | `/api/v1/drafts/{draftId}` | `draftApi.delete(draftId)` | 删除不需要的草稿 |
| 📊 统计草稿数 | GET | `/api/v1/drafts/count` | `draftApi.count()` | 显示草稿数量角标 |

---

## 📊 对接优先级建议

### 🔴 P0（最高优先级）- 核心功能
1. **发现页面主流展示**
   - `discoveryApi.getHotContents()` - 热门Tab
   - `discoveryApi.getRecommendedContents()` - 关注Tab
   - `discoveryApi.getLocalContents()` - 同城Tab

2. **动态详情查看**
   - `discoveryApi.getContentDetail()` - 详情展示

3. **内容互动**
   - `contentApi.toggleLike()` - 点赞
   - `contentApi.toggleCollect()` - 收藏
   - `contentApi.share()` - 分享

4. **发布动态**
   - `contentApi.publish()` - 发布内容

---

### 🟡 P1（高优先级）- 重要功能
1. **评论系统**
   - `commentApi.getList()` - 评论列表
   - `commentApi.add()` - 发表评论
   - `commentApi.toggleLike()` - 评论点赞

2. **我的内容管理**
   - `contentApi.getMyContents()` - 我的动态
   - `contentApi.delete()` - 删除动态
   - `contentApi.update()` - 编辑动态

3. **个人主页Tab**
   - `contentApi.getMyLikes()` - 我的点赞
   - `contentApi.getMyCollects()` - 我的收藏

---

### 🟢 P2（中优先级）- 增强功能
1. **搜索功能**
   - `discoveryApi.searchContents()` - 内容搜索

2. **地理位置功能**
   - `discoveryApi.getNearbyContents()` - 附近内容
   - `discoveryApi.getContentsByCity()` - 城市内容

3. **用户主页**
   - `discoveryApi.getUserContents()` - 查看他人动态

---

### 🔵 P3（低优先级）- 辅助功能
1. **草稿系统**
   - `draftApi.save()` - 保存草稿
   - `draftApi.getMyDrafts()` - 草稿列表
   - `draftApi.publish()` - 发布草稿

2. **批量状态检查**
   - `contentApi.batchCheckLikeStatus()` - 批量检查点赞
   - `contentApi.batchCheckCollectStatus()` - 批量检查收藏

3. **评论高级功能**
   - `commentApi.getReplies()` - 加载更多回复
   - `commentApi.count()` - 评论数统计

---

## 🛠️ 待创建的文件清单

### API服务文件
- ✅ `services/api/discoveryApi.ts` - 已存在
- ✅ `services/api/contentApi.ts` - 已存在
- ✅ `services/api/commentApi.ts` - 已存在
- ❌ `services/api/draftApi.ts` - **需要创建**

### 前端页面/组件
- ✅ `app/(tabs)/discover.tsx` - 已存在（发现页）
- ✅ `app/publish.tsx` - 已存在（发布页）
- ✅ `app/feed/[id].tsx` - 已存在（详情页）
- ✅ `src/features/Discovery/MainPage/` - 已存在
- ❌ `src/features/Discovery/DetailPage/components/CommentSection.tsx` - **需要创建**（评论区组件）
- ❌ `app/drafts.tsx` - **需要创建**（草稿列表页）或集成到个人主页

---

## 📝 集成步骤建议

### 第一阶段：核心流展示（1-2天）
1. 在 `discoveryStore.ts` 中集成 `discoveryApi`
2. 更新 `Discovery/MainPage` 的三个Tab的数据加载逻辑
3. 测试热门、推荐、同城三个Tab的数据展示

### 第二阶段：互动功能（1-2天）
1. 在 `FeedCard` 组件中集成点赞、收藏、分享接口
2. 实现乐观更新机制（先更新UI，再调用API）
3. 添加错误回滚机制（API失败时恢复UI状态）

### 第三阶段：详情页和评论（2-3天）
1. 完善 `DetailPage` 的内容展示
2. 创建 `CommentSection` 组件
3. 集成评论的CRUD操作和点赞功能
4. 实现评论的二级回复展开/收起

### 第四阶段：发布和编辑（2-3天）
1. 完善 `publish.tsx` 的发布接口集成
2. 实现图片上传功能（需要配合文件服务）
3. 添加地点选择功能
4. 在个人主页集成编辑/删除功能

### 第五阶段：草稿系统（可选，1-2天）
1. 创建 `draftApi.ts`
2. 在发布页添加自动保存逻辑
3. 创建草稿列表页面或集成到个人主页

---

## 🔗 相关文档链接

- 后端接口文档：`xypai-content/README.md`
- 后端API详细说明：`xypai-content/docs/API_USAGE_GUIDE_v7.1.md`
- 前端项目结构：`XiangYuPai-RNExpoAPP/START_HERE.md`
- 发现页模块文档：`src/features/Discovery/README.md`
- 个人主页模块文档：`src/features/Profile/README.md`

---

## ✅ 完成检查清单

- [ ] 发现页三Tab数据加载
- [ ] 动态详情页展示
- [ ] 点赞/收藏/分享功能
- [ ] 发布动态功能
- [ ] 评论系统（发表、展示、点赞）
- [ ] 我的动态管理（编辑、删除）
- [ ] 我的点赞/收藏列表
- [ ] 搜索功能
- [ ] 附近内容/城市内容
- [ ] 草稿系统

---

**文档版本：** v1.0  
**最后更新：** 2025-01-15  
**维护者：** AI Assistant

有任何问题，欢迎随时询问！🚀

